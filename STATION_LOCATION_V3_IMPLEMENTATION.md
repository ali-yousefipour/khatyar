# اصلاحات فرایند ثبت موقعیت ایستگاه — V3

- Wizard شش مرحله‌ای: موقعیت، خط، تابلوها، تصویر محل، آدرس، تأیید نهایی.
- GPS با Highest و چند نمونه‌برداری و امکان انتخاب دستی روی نقشه.
- پنج خط نزدیک ابتدا نمایش داده می‌شوند و سایر خطوط با جستجو در دسترس هستند.
- شناسه خط و شناسه ایستگاه در UI نمایش داده نمی‌شوند.
- چند تابلو با انتخاب نوع و تصویربرداری مرحله‌ای پشتیبانی می‌شود.
- تصویر محل و تکمیل همه مراحل اجباری است.
- ایستگاه‌های ثبت‌شده من به‌صورت آیتم مستقل و قابل ویرایش اضافه شده است.
- API جدید `station-wizard-v3.php` برای ثبت، ویرایش، جزئیات، خطوط نزدیک و ایستگاه‌های من اضافه شده است.
- زمان روی `Asia/Tehran` و تاریخ خروجی شمسی است.
- `station-image-v3.php` برای رفع 404 تصاویر اضافه شده است.
- `station-admin-v3.php` و `station-map-v3-admin.js` برای نمایش کامل خطوط، وضعیت ثبت، تابلوها و تصاویر در نقشه اضافه شده‌اند.
- تنظیمات آیتم‌های اپ برای `StationCapture` و `MyStations` مستقل شده است.
- پنل به asset version `1.3.73` همگام شده است.
- خروجی موجود XLSX پروژه از قبل مکانیزم Embed تصویر دارد و باید روی Production با Excel Desktop تست شود.

## تست Production باقی‌مانده
1. ثبت واقعی ایستگاه و چند تابلو.
2. نمایش تصاویر و رفع 404 احتمالی.
3. باز شدن XLSX و وجود تصاویر داخل فایل.
4. استقلال دسترسی نقش‌ها در آیتم‌های اپ.
5. تست Android 9، Camera، GPS و Background Location.
