# راهنمای نصب

## روش ۱) داکر (ساده‌ترین)
```bash
docker compose up -d
```
سپس مرورگر را باز کنید: **http://localhost:4000/install**
نصاب وب به‌صورت گام‌به‌گام اجرا می‌شود: بررسی پیش‌نیازها → اتصال دیتابیس → ساخت حساب مدیرکل → نصب.
پس از پایان، سرور را یک‌بار ری‌استارت کنید: `docker compose restart api`

## روش ۲) نصب دستی
```bash
cd backend
npm install
npm start                 # سرور بالا می‌آید و چون هنوز نصب نشده، به /install هدایت می‌کند
```
به **http://localhost:4000/install** بروید و نصاب را کامل کنید.
(جایگزین خط‌فرمان: `node install/cli-install.js`)

نصاب به‌صورت خودکار: جداول را می‌سازد، نقش‌ها و ۶۸ حساب پرسنل (رمز ۱۲۳۴۵۶) و حساب مدیرکل را ایجاد می‌کند،
فایل `.env` را می‌نویسد و سپس خودش را قفل می‌کند (فایل `.installed`). برای نصب مجدد، این فایل را حذف کنید.

## بارگذاری دادهٔ اکسل پس از نصب
از پنل: منوی «ورود اطلاعات (اکسل)» — یا خط‌فرمان:
```bash
python scripts/import_excel.py drivers "/path/فایل.xlsx"
```

## اپ موبایل — تنظیمات پیش از ساخت APK
```bash
cd mobile
cp .env.example .env       # API_BASE (آدرس سرور)، نام اپ، نام پکیج، قابلیت‌ها را تنظیم کنید
npm install
eas build -p android --profile preview
```
کاربر هنگام **اولین اجرا** هم می‌تواند آدرس سرور را در صفحهٔ «اتصال به سرور» وارد و تست کند،
بنابراین یک APK واحد می‌تواند به هر سروری متصل شود (بدون نیاز به build مجدد).

## هشدار خودکار انقضای پروانه (cron)
برای ارسال روزانهٔ هشدارِ پروانه‌های رو به انقضا (مثلاً ۳۰ روز مانده):
```
0 7 * * *  cd /path/backend && node scripts/check_expiries.js 30
```
