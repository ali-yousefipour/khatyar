# سیاست نهایی تاریخ در خطیار

## اصل اصلی

تاریخ و زمان‌های سیستمی و قابل مرتب‌سازی باید در MySQL/MariaDB به صورت میلادی استاندارد ISO/Gregorian ذخیره شوند:

- `DATE` برای تاریخ بدون زمان: `YYYY-MM-DD`
- `DATETIME` / `TIMESTAMP` برای تاریخ و زمان: `YYYY-MM-DD HH:MM:SS`
- منطقه زمانی Backend: `Asia/Tehran`

تقویم شمسی فقط لایه نمایش و ورود کاربر است.

## DatePicker فعلی

`php/app/assets/persian-date-picker.js` تقویم جلالی را نمایش می‌دهد، اما هنگام انتخاب تاریخ مقدار فیلد واقعی را با `jToIso()` به میلادی تبدیل می‌کند؛ یعنی برای تاریخ جلالی انتخاب‌شده، مقدار ارسال‌شده به Backend از جنس `YYYY-MM-DD` است.

## استثنای کنترل‌شده: jdate

برخی فیلدهای دامنه‌ای پروژه مانند `holidays.jdate` و بازه‌های `user_shifts.from_jdate` / `user_shifts.to_jdate` برای منطق تقویم کاری به عنوان «کلید روز شمسی» نگهداری می‌شوند. این فیلدها timestamp نیستند و نباید با `created_at`، `check_in` و سایر زمان‌های سیستمی اشتباه گرفته شوند.

گزارش تردد مدیریتی نیز همین تفکیک را رعایت می‌کند: `staff_attendance.check_in/check_out` را به صورت Timestamp میلادی می‌خواند و سپس برای گروه‌بندی روزانه آن‌ها را به `jdate` تبدیل می‌کند.

## ممنوع

- ذخیره تاریخ شمسی در ستون‌های `DATE`/`DATETIME`/`TIMESTAMP`.
- مقایسه مستقیم رشته شمسی با ستون زمانی میلادی.
- استفاده از الگوریتم تقریبی مستقل برای تبدیل جلالی/میلادی در فایل‌های کمکی.

## نتیجه

معماری توصیه‌شده: **Gregorian in DB + Jalali in UI**، با نگهداری `jdate` فقط در جدول‌ها و منطق‌هایی که صراحتاً به «روز تقویم شمسی» به عنوان شناسه کسب‌وکار نیاز دارند.
