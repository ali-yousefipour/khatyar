# ===== تنظیمات تولیدی برای app.yousefipour.ir =====
# این فایل را روی هاست به نام .env تغییر نام دهید.

# --- دیتابیس (PostgreSQL) ---
# نکته: کاراکتر @ در رمز باید در URL به‌صورت %40 کدگذاری شود (انجام شده).
DATABASE_URL=postgres://h301194_app:%40Li09158048604@localhost:5432/h301194_app

# اگر اتصال با URL مشکل داشت، این مقادیر مجزا هم توسط درایور pg خوانده می‌شوند:
PGHOST=localhost
PGPORT=5432
PGUSER=h301194_app
PGPASSWORD=@Li09158048604
PGDATABASE=h301194_app

# --- امنیت توکن (به‌صورت تصادفی ساخته شد؛ محرمانه نگه دارید) ---
JWT_SECRET=855b171f06aaab8fc898dbd174897184646d20cb977d4322cf9f5557f3441073
JWT_ACCESS_TTL=15m
JWT_REFRESH_TTL=30d

# --- سرور ---
PORT=4000
PUBLIC_URL=https://app.yousefipour.ir

# --- حساب مدیرکل اولیه (برای نصب غیرتعاملی) ---
# پس از اولین ورود حتماً رمز را تغییر دهید.
ADMIN_USER=0920424635
ADMIN_PASS=Admin@1404
ADMIN_FIRST=مدیر
ADMIN_LAST=کل
