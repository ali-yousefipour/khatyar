# گزارش اصلاحات V162

- حذف کامل آیتم اسکن پلاک و اسکن کارت ملی از اپ.
- حذف ScannerScreen، ocr.js، وابستگی ML Kit و پلاگین keep مربوط به آن.
- حفظ جستجوی دستی کد ملی و پلاک.
- حذف تنظیمات پلاک‌خوان از پنل.
- حذف BUILD-ONE-CLICK-README.txt.
- اصلاح README و اعتبارسنج فاز ۵ برای حذف ارجاعات OCR منسوخ.
- نقشه آفلاین اکنون پس از دانلود، تایل‌های محلی را از حافظه می‌خواند و در صفحه ثبت حضور استفاده می‌کند.
- حالت هوشمند در نبود اینترنت به تایل‌های محلی سوییچ می‌کند؛ حالت فقط آفلاین نیز مستقیماً از کش استفاده می‌کند.
- دکمه آزمون سلامت نقشه آفلاین اضافه شد.
- متادیتای کش با provider ذخیره می‌شود تا کش نامرتبط معتبر شناخته نشود.
- بازه بررسی GPS و VPN از تنظیمات سایت دریافت می‌شود.
- رخداد GPS خاموش به API telemetry ارسال می‌شود تا منطق اعلان بالادستی سرور اجرا شود.
- تنظیمات GPS/VPN/GSM در API تنظیمات اپ در دسترس قرار گرفت.
- دسترسی سریع اپ در تب‌های میدانی، عملیات، پیام‌ها، شخصی و سایر گروه‌بندی شد.
- تنظیمات سایت همچنان به‌صورت تب‌بندی‌شده است و تب منسوخ پلاک‌خوان حذف شد.
- نسخه اپ 1.3.32، versionCode 10332، پنل v162.

## اعتبارسنجی انجام‌شده
- PHP lint همه فایل‌های PHP: موفق
- Node syntax check فایل‌های JavaScript: موفق
- ساخت مجدد panel.bundle.js: موفق
- فقط build-release.ps1 در ریشه mobile باقی مانده است.
