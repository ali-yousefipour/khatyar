// این فایل فقط رفتار «autolinking» (پیوند خودکار ماژول‌های بومی) را کنترل می‌کند
// و هیچ ارتباطی با package.json/package-lock.json ندارد — پس تغییر آن هیچ‌وقت باعث
// ناهماهنگی npm ci نمی‌شود.
//
// چرا این فایل لازم شد:
// با مقایسهٔ نسخهٔ فعلی با آخرین نسخهٔ سالمی که روی اندروید‌های قدیمی مشکلی نداشت،
// مشخص شد تنها وابستگی بومیِ (native) جدیدی که در این فاصله اضافه شده «expo-audio» است
// (نسخهٔ قبلی اصلاً به کتابخانهٔ صوتی متصل نبود). ماژول‌های بومی Expo/React Native بدون
// توجه به این‌که کد جاوااسکریپت آن‌ها را import کند یا نه، در همان ابتدای راه‌اندازی برنامه
// (پیش از اجرای هر خط جاوااسکریپت) به‌صورت خودکار ثبت می‌شوند. اگر پیاده‌سازی بومیِ یک
// ماژول با نسخهٔ اندروید دستگاه سازگار نباشد، همین‌جا و دقیقاً در همان لحظهٔ «لود اولیه/
// اسپلش‌اسکرین» کرش رخ می‌دهد و چون پیش از اجرای جاوااسکریپت است، هیچ try/catch سمت
// جاوااسکریپتی (که در soundFx.js هم برای احتیاط وجود دارد) قدرت جلوگیری از آن را ندارد.
//
// راه‌حل: پیوند بومیِ اندرویدِ expo-audio غیرفعال شد (فقط اندروید؛ iOS دست‌نخورده می‌ماند).
// بستهٔ جاوااسکریپتی همچنان نصب است (پس package-lock.json دست‌نخورده می‌ماند)، فقط کد
// بومی آن دیگر در برنامهٔ اندروید کامپایل/ثبت نمی‌شود. نتیجه: پخش صدا در اندروید به‌شکل
// امن (بی‌صدا) غیرفعال می‌شود، اما دیگر هیچ ریسک کرش بومی از این ماژول وجود نخواهد داشت.
// (soundFx.js از قبل هر خطای احتمالی را با try/catch می‌گیرد، پس این تغییر باعث خطای
// جاوااسکریپتی هم نمی‌شود — فقط توابع پخش صدا در اندروید بی‌اثر (no-op) خواهند بود.)
module.exports = {
  dependencies: {
    'expo-audio': {
      platforms: {
        android: null,
      },
    },
  },
};
