name: One-time React JDate migration

on:
  workflow_dispatch:

# Retained only as historical documentation. The calendar migration is already
# integrated into the main panel source and bundle. Do not re-run automatically.
