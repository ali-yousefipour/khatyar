name: Calendar classic bundle repair

on:
  workflow_dispatch:

# Historical one-time repair workflow retained for reference only.
# Normal calendar maintenance is performed in php/tools/panel_source.jsx
# and the normal panel build pipeline.
