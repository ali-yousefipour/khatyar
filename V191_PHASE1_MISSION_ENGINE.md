# v191 — فاز یک موتور مأموریت و الگوهای سمت

## پیاده‌سازی‌شده

- موتور مأموریت درصدی برای دوره‌های روزانه، هفتگی و ماهانه.
- الگوی مأموریت برای سمت‌های:
  - ناظر خط
  - گشت موتوری
  - بازرس گشت خودرویی
  - بازرس مقیم
  - سربازرس
- اهداف قابل تنظیم شامل ثبت حضور، چک‌لیست، تذکر اعتبار منقضی، تذکر بدهی آبونمان، بازدید خطوط، ارزیابی زیرمجموعه، بازدید ایستگاه/پایانه و گزارش پایان شیفت.
- امکان تعیین درصد هدف، وزن، اجباری بودن و حداقل تعداد برای هر شاخص.
- پیشنهاد تکمیلی «الگوی سمت + مأموریت اختصاصی کاربر» به‌صورت واقعی اعمال شد:
  - ابتدا مأموریت اختصاصی فعال کاربر بررسی می‌شود.
  - در نبود آن، الگوی فعال سمت اعمال می‌شود.
  - نوع بازرس بر اساس تنظیم دستی `inspector_modes` و در نبود آن بر اساس تعداد خطوط تشخیص داده می‌شود.
- پنل وب «مدیریت مأموریت‌ها» برای ساخت و حذف الگوها و ثبت مأموریت اختصاصی.
- صفحه موبایل «مأموریت روزانه من» برای مشاهده اهداف مؤثر کاربر.
- APIهای مدیریت و اپلیکیشن.
- الگوهای اولیه قابل ویرایش با درصدهای پیشنهادی در Migration ایجاد می‌شوند.

## APIها

- `GET /api/my/missions/effective?period=daily`
- `GET /api/my/missions/today`
- `GET /api/admin/mission-metrics`
- `GET /api/admin/mission-templates`
- `POST /api/admin/mission-templates`
- `DELETE /api/admin/mission-templates/:id`
- `GET /api/admin/user-mission-overrides`
- `PUT /api/admin/user-mission-overrides/:userId`
- `DELETE /api/admin/user-mission-overrides/:userId?period=daily`

## نصب دیتابیس

از پوشه `backend` اجرا شود:

```bash
npm run migrate:v191
```

یا فایل زیر در PostgreSQL اجرا شود:

`db/upgrade_v191_mission_engine.sql`

## مرز فاز یک

در این فاز تعریف، اعمال و نمایش اهداف کامل است. محاسبه مخرج واقعی هر درصد، اعتبارسنجی بازدید واقعی، پوشش خط و امتیاز نهایی در فازهای بعدی به داده‌های عملیاتی متصل می‌شود. شمارنده‌های خام امروز برای حضور، چک‌لیست، تذکر و ارزیابی در API نمایش داده می‌شوند.
