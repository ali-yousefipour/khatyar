
## v192 - اجرای مأموریت و پوشش معتبر خطوط
- اتصال مأموریت‌های روزانه به حضور، چک‌لیست، تذکر، بازدید و ارزیابی واقعی.
- شروع/پایان بازدید خط با GPS، تصویر مستقیم و گزارش.
- محاسبه پوشش معتبر، درصد تحقق وزنی و کار باقی‌مانده.
- Timeline روزانه و گزارش مدیریتی گشت‌ها و بازرسان.
- افزودن Migration و تنظیمات اجرایی مأموریت.
## v172 - 2026-07-27
- آیکون اختصاصی و غیرتکراری برای آیتم‌های داشبورد اپ و نگاشت گسترده‌تر آیکون‌های پنل سایت.
- افزایش ارتفاع و عرض کادر کد ملی و نمایش کامل ۱۰ رقم.
- ساماندهی منوی حساب کاربری اپ در گروه‌های حساب کاربری، تنظیمات و خروج.
- انتقال اطلاعات تکمیلی به صفحه ویرایش اطلاعات من.
- حفظ اصلاحات مسیر کوتاه Gradle/CMake و فونت وزیر نسخه قبل.


## v171 - Restore bundled Vazirmatn fonts
- Restored Expo font loading from mobile/assets/fonts.
- Restored Vazirmatn and Vazirmatn-Bold font family names.
- Kept short Gradle cache path and CMake/Ninja cleanup fixes from v170.
- Static asset validation now verifies the two font files before Gradle starts.

# v166
- حذف پس‌زمینه سفید آیکون‌های سه‌بعدی و تبدیل حاشیه به شفاف.
- اصلاح خروجی Excel تذکرات بدون بازکردن لینک فاقد هدر احراز هویت.
- افزودن خروجی Excel عملکرد سالیانه و ریز ورود و خروج ماه انتخابی.


## v165 — آیکون‌های سه‌بعدی آفلاین و نمایش گزارش عملکرد تاکسیران
- افزودن بسته آیکون‌های سه‌بعدی CC0 از پروژه 3Dicons و قرار دادن فایل‌ها در سایت و اپ.
- حذف وابستگی نمایش آیکون‌ها به اینترنت؛ آیکون‌های اپ داخل APK/AAB بسته‌بندی می‌شوند.
- تنظیم کش یک‌ساله آیکون‌های سایت.
- رفع اشکال نحوی منوی داشبورد اپ و اتصال همه آیتم‌های اصلی به آیکون‌های محلی.
- نمایش همیشگی «عملکرد و تذکرات تاکسیران» برای کاربران مدیریتی سایت، مستقل از تنظیمات قدیمی role_perms.


## v164 — اصلاح تنظیمات، GSM fallback و آیکون‌های محلی
- رفع خطای `MaintenanceModeSettings is not defined` در صفحه تنظیمات پنل.
- حذف گزارش مستقل موقعیت GSM و تنظیمات جداگانه آن از رابط پنل.
- حفظ موقعیت شبکه/GSM فقط به‌عنوان جایگزین هنگام در دسترس نبودن GPS برای نمایش روی نقشه و سایر بخش‌ها.
- حذف گزینه GSM از روش‌های قابل انتخاب ثبت حضور.
- تغییر آیکون جستجو به فایل محلی داخل سایت و اپ برای کارکرد بدون اینترنت؛ سایر منوها از آیکون‌های داخلی/ایموجی استفاده می‌کنند و به اینترنت وابسته نیستند.
# CHANGELOG یکپارچه پروژه

این فایل حاصل ادغام همه فایل‌های تغییرات نسخه‌های قبلی است.

## نسخه v161 — 2026-07-26

- یکسان‌سازی نسخه سایت روی 125 و نسخه اپ روی 1.3.31 با versionCode برابر 10331.
- اصلاح شناسه بیلد پنل و کش فایل‌های پنل به v161.
- حذف تعریف‌های تکراری مسیرهای API.
- ثبت خطا برای catchهای کاملاً خالی.
- ایجاد تنها اسکریپت بیلد `mobile/build-release.ps1` و حذف اسکریپت‌های اضافی موبایل.
- ادغام همه ChangeLogهای پراکنده در همین فایل.

---

## منبع: `CHANGELOG_BALE_BOT_INTEGRATION.md`

# تغییرات اتصال ربات پیام‌رسان بله

## ماژول‌های اضافه‌شده
- افزودن کلاس `php/lib/Bale.php` برای اتصال به API ربات بله.
- افزودن Webhook عمومی: `/api/bale/webhook/{secret}`.
- افزودن تنظیمات مدیریتی ربات در پنل: «تنظیمات → ربات بله».
- افزودن جدول‌های دیتابیس:
  - `bale_subscribers`: اتصال شماره همراه کاربران/رانندگان به `chat_id` بله.
  - `bale_message_log`: گزارش ارسال پیام از ربات بله.
- افزودن فایل ارتقای دیتابیس: `php/db/upgrade_bale_bot.sql`.

## امکانات مدیریتی
- فعال/غیرفعال‌سازی ربات بله.
- ثبت توکن ربات و آدرس API.
- تعریف کلید محرمانه Webhook.
- تنظیم متن خوش‌آمد ربات.
- فعال/غیرفعال‌سازی آیتم‌های ربات:
  - پیام‌های عمومی سامانه
  - تبریک تولد
  - اعلان‌های حضور و غیاب
  - قبوض و بدهی رانندگان
  - تذکرها و اخطارها
- نمایش کاربران/رانندگان متصل به ربات.
- نمایش گزارش ارسال پیام‌های بله.
- ارسال پیام آزمایشی بر اساس شماره همراه.

## اتصال کاربران و رانندگان
کاربر یا راننده باید ابتدا وارد ربات بله شود و شماره همراه ثبت‌شده در سامانه یا کد ملی خود را ارسال کند. پس از تطبیق شماره یا کد ملی با دیتابیس، `chat_id` ذخیره می‌شود و از آن به بعد پیام‌ها بر اساس شماره همراه به همان حساب بله ارسال می‌شوند.

## پیام‌رسانی سامانه
- در صفحه «پیام‌رسانی»، گزینه ارسال هم‌زمان پیام از طریق ربات بله اضافه شد.
- پیام فقط برای کاربرانی ارسال می‌شود که قبلاً ربات را Start کرده و حسابشان به شماره همراه سامانه متصل شده باشد.

## تبریک تولد
- تبریک تولد علاوه بر Push و پیامک، در صورت فعال بودن آیتم Birthday در تنظیمات بله، از طریق ربات بله هم ارسال می‌شود.

## نکته مهم
ربات‌های بله مانند بسیاری از Bot APIها معمولاً نمی‌توانند بدون شروع گفتگو از سمت کاربر، پیام را صرفاً با شماره همراه ارسال کنند. به همین دلیل در این نسخه شماره همراه به `chat_id` کاربر/راننده پس از Start و احراز شماره یا کد ملی متصل می‌شود.

---

## منبع: `CHANGELOG_BALE_MIRROR_ALL_CHANNELS.md`

# تغییرات ارسال هم‌زمان پیام‌ها به ربات بله

## هدف
از این نسخه، هر پیامی که قبلاً از مسیرهای زیر برای کاربر یا راننده ارسال می‌شد، در صورت اتصال مخاطب به ربات بله، از طریق ربات بله نیز ارسال می‌شود:

- پیامک‌ها
- Push Notification
- اعلان‌های داخل برنامه
- پیام‌های عمومی سامانه
- تبریک تولد
- هشدارهای حضور و غیاب
- قبوض و بدهی‌ها
- تذکرها و اخطارها

## اصلاحات فنی

### 1. `php/lib/Sms.php`
بعد از ارسال موفق پیامک، همان متن برای شماره‌های گیرنده از طریق ربات بله نیز ارسال می‌شود.

### 2. `php/lib/Push.php`
بعد از ارسال Push، پیام برای کاربران متصل به ربات بله نیز ارسال می‌شود.

### 3. `php/lib/Bale.php`
توابع جدید اضافه شد:

- `BaleBot::itemEnabled()`
- `BaleBot::sendToUserIds()`
- `BaleBot::sendToMobiles()`

### 4. `php/lib/routes.php`
در ثبت تذکر برای راننده، پیام بله مستقل از انتخاب پیامک ارسال می‌شود؛ یعنی اگر راننده با شماره موبایل خود در ربات بله متصل شده باشد، ثبت تذکر برای او در بله ارسال می‌شود.

## نکته اجرایی
برای ارسال موفق پیام در بله، کاربر یا راننده باید قبلاً ربات را Start کرده و با شماره موبایل یا کد ملی خود به سامانه متصل شده باشد.

## تست پیشنهادی
1. راننده ربات را Start کند.
2. شماره همراه یا کد ملی ثبت‌شده در سامانه را ارسال کند.
3. پیام «اتصال شما با سامانه انجام شد» دریافت شود.
4. برای همان راننده تذکر ثبت شود.
5. پیام تذکر باید در ربات بله دریافت شود.

---

## منبع: `CHANGELOG_BRANDING_STARS_ATTENDANCE.md`

# تغییرات اعمال‌شده در این نسخه

## ۱) برندینگ سایت و برنامه
- اضافه شدن تنظیم «عنوان سایت» در مسیر: تنظیمات سامانه ← لوگو و عنوان سایت و اداره.
- اضافه شدن امکان بارگذاری لوگوی سایت برای صفحه ورود و سایدبار پنل مدیریت.
- حفظ تنظیم جداگانه لوگوی سربرگ چاپ و خروجی‌ها.
- اعمال عنوان سایت روی `document.title` مرورگر.
- نمایش لوگوی تعریف‌شده در صفحه ورود پنل و وب‌اپ میدانی.
- محدودسازی نوع فایل لوگو به تصویر و کنترل حجم برای جلوگیری از آپلودهای ناامن.

## ۲) درجه ستاره اختصاصی هر کاربر
- اضافه شدن ستون `rank_stars` برای کاربران.
- پیش‌فرض قبلی حفظ شد:
  - ناظر خط و اپراتور: ۱ ستاره
  - بازرس: ۲ ستاره
  - سربازرس: ۴ ستاره
- در فرم ویرایش کاربر، فیلد «تعداد ستاره درجه» اضافه شد.
- اگر مقدار این فیلد خالی باشد، درجه از سمت کاربر محاسبه می‌شود.
- اگر مقدار اختصاصی ثبت شود، همان تعداد ستاره برای آن فرد در پنل و اپ نمایش داده می‌شود.
- بازه مجاز ستاره‌ها از ۰ تا ۵ محدود شده است.

## ۳) بهبودهای مرتبط با حضور و غیاب کاربران
- داده درجه کاربر در پاسخ ورود و پروفایل کامل برگشت داده می‌شود تا اپ موبایل و وب‌اپ از مقدار اختصاصی استفاده کنند.
- مسیر عمومی امن `/api/settings/public` برای دریافت تنظیمات غیرحساس برندینگ و محدودیت‌های فایل اضافه/تکمیل شد.
- گزارش تردد پرسنل و حضور نیروها بدون تغییر منطق قبلی، با داده‌های جدید کاربر سازگار شد.
- نسخه سایت به ۸۴ افزایش یافت تا کش مرورگر فایل جدید را دریافت کند.

## ۴) تغییرات دیتابیس
- MySQL/MariaDB: اضافه شدن `users.rank_stars` در schema و فایل upgrade.
- PostgreSQL: اضافه شدن `users.rank_stars` و تنظیمات پیش‌فرض `site_title`, `site_logo`, `org_logo`.
- تنظیمات برندینگ در جدول `app_settings` ذخیره می‌شود.

## ۵) فایل‌های اصلی تغییرکرده
- `php/lib/routes.php`
- `php/public/panel.html`
- `php/public/app.html`
- `php/db/mysql_schema.sql`
- `php/db/upgrade.sql`
- `db/schema.sql`
- `backend/src/routes/admin.js`
- `backend/src/routes/auth.js`
- `mobile/src/screens/DashboardScreen.js`

---

## منبع: `CHANGELOG_CUSTOM_AUDIO_ALERTS.md`

# تغییرات صداهای اختصاصی برنامه

## فایل‌های صوتی اضافه‌شده
فایل‌های صوتی ارسال‌شده در مسیر زیر قرار گرفتند:

`mobile/assets/sounds/`

نام‌های فنی استفاده‌شده:

- `presence_validation_alert.mp3` — هشدار انجام صحت‌سنجی حضور
- `presence_selfie.mp3` — راهنمای گرفتن سلفی صحت‌سنجی
- `presence_station_photo.mp3` — راهنمای گرفتن تصویر ایستگاه تاکسی
- `presence_success.mp3` — صحت‌سنجی با موفقیت انجام شد
- `notification_new.mp3` — اعلان جدید
- `message_new.mp3` — پیام جدید
- `report_received.mp3` — دریافت گزارش جدید
- `report_sent_success.mp3` — گزارش با موفقیت ارسال گردید
- `official_presence_registered.mp3` — ثبت شدن حضور مسئول در خط

## منطق آلارم صحت‌سنجی حضور
- پس از فعال‌شدن صحت‌سنجی، آلارم با تأخیر ۵ ثانیه‌ای شروع می‌شود.
- اگر کاربر در این ۵ ثانیه وارد فرآیند شود، صدا اصلاً پخش نمی‌شود.
- اگر کاربر وارد نشود، صدای هشدار به‌صورت تکرارشونده پخش می‌شود.
- با زدن دکمه شروع صحت‌سنجی، ورود به مرحله سلفی، ارسال موفق یا پایان فرآیند، آلارم قطع می‌شود.

## کانال‌های صدای اندروید
- `presence_alarm` برای صحت‌سنجی حضور
- `messages` برای پیام جدید
- `reports` برای گزارش جدید
- `default` برای اعلان‌های عمومی

## فایل‌های تغییر یافته
- `mobile/src/presenceAlarm.js`
- `mobile/src/PresenceCheckModal.js`
- `mobile/src/soundFx.js`
- `mobile/src/notify.js`
- `mobile/src/push.js`
- `mobile/app.config.js`
- `backend/src/push.js`
- `php/lib/Push.php`

---

## منبع: `CHANGELOG_DRIVER_NATIONAL_SEARCH_CRASH_FIX.md`

# اصلاح کرش جستجوی تاکسیران با کد ملی

## مشکل
در بعضی جستجوهای کد ملی، مخصوصاً زمانی که اطلاعات خودرو/راننده ناقص یا پاسخ سرور غیرهمسان بود، اپ موبایل در صفحه نتیجه جستجوی راننده کرش می‌کرد.

## اصلاحات انجام‌شده
- مقاوم‌سازی `SearchScreen.js` در برابر پاسخ ناقص یا نامعتبر سرور.
- نرمال‌سازی اطلاعات راننده قبل از انتقال به صفحه راننده.
- جلوگیری از ارسال `vehicle=null` و `warnings/temp_lines` نامعتبر به صفحه راننده.
- مقاوم‌سازی `DriverScreen.js` در برابر مقادیر null، object، عددی و ناقص.
- رفع ریسک کرش ناشی از اجرای شرط بازگشت قبل از Hookهای React.
- افزودن پیام مناسب در صورت ناقص بودن شناسه داخلی راننده.
- جلوگیری از نمایش مستقیم object در کامپوننت Text که باعث کرش React Native می‌شود.

## فایل‌های تغییر کرده
- `mobile/src/screens/SearchScreen.js`
- `mobile/src/screens/DriverScreen.js`

---

## منبع: `CHANGELOG_EXPIRY_NOTIFICATION_SETTINGS.md`

# اعلان‌های پایان اعتبار خطوط مجاز کاربر

- اعلان پایان اعتبار پروانه تاکسیرانی حفظ و تکمیل شد.
- اعلان پروانه بهره‌برداری راننده، معاینه فنی خودرو و بیمه شخص ثالث خودرو اضافه شد.
- دامنه داده‌ها همچنان فقط خطوط مجاز همان کاربر است.
- هر هشدار شناسه پایدار بر پایه نوع و راننده/خودرو دارد.
- خواندن یک هشدار، همان هشدار را به مدت پیش‌فرض ۳۰ روز پنهان می‌کند.
- «خواندن همه» تمام هشدارهای فعلی را برای دوره تعیین‌شده پنهان می‌کند.
- صفحه تنظیمات کاربری برای فعال/غیرفعال کردن هر نوع اعلان، تعداد روزهای بررسی پیش از انقضا و دوره ارسال مجدد اضافه شد.
- بازه‌های ورودی در سرور اعتبارسنجی می‌شوند: بررسی ۰ تا ۳۶۵ روز و تکرار ۱ تا ۳۶۵ روز.
- نسخه SDK، Expo، React Native، Gradle، پلاگین‌ها و وابستگی‌ها تغییر نکرده‌اند.

---

## منبع: `CHANGELOG_FIXES_V148.md`

# CHANGELOG - رفع باگ‌های V148

## ۱. تکمیل تنظیم دسترسی «آیتم‌های اپ هر سمت»
- **فایل‌ها:** `php/tools/panel_source.jsx` (سورس)، `php/public/assets/panel.bundle.js` (بازتولید شده از سورس)
- ۴ آیتم `CompanyRequests`، `Cultural`، `Welfare`، `SalarySlips` که در نسخهٔ قبل اضافه ادعا شده بودند اما در پنل قابل تنظیم نبودند، به لیست اضافه شدند.
- **پیامد قبلی:** با هر محدودیت روی یک سمت، این ۳ آیتم برای همیشه غیرقابل‌فعال‌سازی مخفی می‌شدند و فیش حقوقی اصلاً قابل محدودسازی نبود.
- **`mobile/src/screens/AccountScreens.js`:** آیتم «فیش‌های حقوقی من» اکنون به همان مکانیزم `allowed`/`/my/app-items` وصل است.
- **`php/lib/routes.php`:** تابع `_user_app_item_allowed()` اضافه شد و روی endpoint های `/api/my/salary-slips` و `/api/my/salary-slips/{id}/download` به‌عنوان لایهٔ حفاظتی سمت سرور اعمال شد.

## ۲. علت‌یابی خطای ۵۰۰ در «ارسال برای شرکت» / مدارک / پرداخت بله
- کد `routes.php` مربوط به `company_requests` سالم است؛ اما **هیچ فایل SQL مستقلی برای ساخت جداول `company_requests*` وجود ندارد** — این جداول فقط با اجرای `php/public/upgrade.php` ساخته می‌شوند.
- **اقدام لازم روی سرور:** آدرس `/upgrade.php` را روی دامنهٔ production باز/اجرا کنید تا مطمئن شوید جداول `company_requests`, `company_request_types`, `company_request_files`, `company_request_payments`, `company_card_payments` و ستون‌های مرتبط ساخته شده‌اند.
- همچنین بررسی کنید: افزونهٔ PHP `fileinfo` فعال باشد (برای بررسی نوع فایل مدارک)، و در تنظیمات پنل مقادیر `bale_payment_enabled`، `bale_provider_token` و اتصال ربات بله تنظیم شده باشند.

## ۳. نمایش تصاویر گزارش‌های چند‌پیوستی (اپ + سایت)
- **`mobile/src/components/ImageViewer.js`:** از حالت تک‌عکسی به گالری چندعکسی (با پیمایش، شمارنده، و نوار thumbnail) ارتقا یافت؛ سازگار با کدهای قدیمی که `uri` تکی می‌فرستند.
- **`mobile/src/screens/InboxScreen.js`:** اکنون همهٔ پیوست‌ها (اصلی + آرایهٔ `attachments`) به‌صورت thumbnail قابل‌لمس نمایش داده می‌شوند، نه فقط تصویر اول.
- **`mobile/src/screens/ReportsScreen.js`:** در فرم ارسال گزارش، پیش‌نمایش thumbnail برای همهٔ تصاویر انتخابی نمایش داده می‌شود (قبلاً فقط اولی).
- **`php/tools/panel_source.jsx` → `panel.bundle.js`:** پنل وب هم اکنون گالری کامل پیوست‌های گزارش را نمایش می‌دهد (قبلاً فقط پیوست اصلی).

## ۴. سرعت و پایداری ارسال گزارش
- **`mobile/src/screens/ReportsScreen.js`:** تابع `send()` با `try/catch` کامل شد (قبلاً خطاهای غیرشبکه‌ای بی‌صدا از بین می‌رفتند و کاربر هیچ پیامی نمی‌دید)، و وضعیت `sending` اضافه شد تا از ارسال تکراری با دابل‌تپ جلوگیری شود.

## ۵. حذف اسپلش اسکرین
- **`mobile/app.config.js`:** کلید `splash` (تصویر برند هنگام باز شدن اپ) حذف شد.
- **`mobile/App.js`:** صفحهٔ بارگذاری برندشدهٔ `SplashLoading` با یک صفحهٔ خالی جایگزین شد (فقط زمان لازم برای لود فونت‌ها حفظ شده تا از فلش محتوای بی‌استایل جلوگیری شود).

## ۶. متن نوتیفیکیشن سرویس پس‌زمینه
- **`mobile/src/location.js`:** متن نوتیفیکیشن دائمی سرویس موقعیت‌مکانی به «نرم افزار خطیار فعال و به سرور متصل است» تغییر یافت. `killServiceOnDestroy: false` از قبل برای اجرای مداوم در پس‌زمینه فعال بود.

## ۷. اولویت مخازن بیلد اندروید (کش محلی → مخزن ایرانی)
- **`mobile/myket.init.gradle`** و **`mobile/plugins/withMyketMirror.js`:** `mavenLocal()` به‌عنوان اولین مخزن (پیش از mirror میکت و گوگل/مِیوِن‌سنترال) در همهٔ بلاک‌های `repositories` اضافه شد؛ یعنی گریدل ابتدا کش محلی، سپس مخزن ایرانی میکت، و در نهایت مخازن رسمی را بررسی می‌کند.

---
### موارد نیازمند تأیید/اقدام روی سرور (خارج از دسترسی من در این محیط)
1. اجرای `php/public/upgrade.php` برای اطمینان از وجود جداول `company_requests*`.
2. بررسی فعال بودن افزونهٔ `fileinfo` در PHP سرور.
3. بررسی تنظیمات ربات بله (توکن پرداخت، فعال بودن) در پنل مدیریت.
4. اجرای `bash php/tools/build_panel_bundle.sh` در صورت هرگونه ادیت بعدی روی `panel_source.jsx` (این نسخه از قبل بازتولید و تست شده است).

---
## بخش دوم — بررسی تکمیل بودن امکانات نسبت به V147 + رفع باگ صدا + قابلیت حالت تعمیر

### ۸. رفع کامل نبودن پخش صدا (باگ بحرانی)
با مقایسهٔ changelogهای نسخهٔ v144/v147 با فایل پایه (OK.ZIP) مشخص شد این فایل پایه از قبل از افزوده‌شدن قابلیت پخش صدا بوده:
- **`mobile/package.json`, `mobile/package-lock.json`:** وابستگی `expo-audio@~57.0.2` اضافه شد (سازگار با Expo SDK 57). `package-lock.json` با نسخهٔ سالم و تست‌شدهٔ v144 (که دقیقاً همان مجموعه وابستگی‌ها را دارد) جایگزین شد تا خطای `npm ci` رخ ندهد.
- **`mobile/src/soundFx.js`:** با نسخهٔ تست‌شده و مبتنی بر `expo-audio` (به‌جای `expo-av` غایب) جایگزین شد. پخش صدای دریافت گزارش، ارسال موفق گزارش، پیام جدید، اعلان جدید و رویدادهای حضور اکنون واقعاً کار می‌کند (قبلاً به‌صورت خاموش/fail-safe هیچ صدایی پخش نمی‌شد چون کتابخانه اصلاً نصب نبود، هرچند بیلد کرش نمی‌کرد).
- فایل‌های صوتی (`mobile/assets/sounds/*.mp3`) و تنظیمات `expo-notifications` (`sounds:[...]`) در `app.config.js` از قبل صحیح بودند و دست‌نخورده ماندند.

### ۹. بررسی سایر موارد پرسیده‌شده (بدون نیاز به تغییر — سالم تأیید شدند)
- **فیلتر پیام خروج از خط (`_field_alert_targets`)**: منطق فیلتر بر اساس سمت/کاربر خاص و مسیر سلسله‌مراتبی مدیران صحیح و کامل است.
- **`GpsGuard.js`**: هر ۶۰ ثانیه و هنگام بازگشت به اپ GPS را بررسی و در صورت خاموش بودن کاربر را با صفحهٔ مسدودکننده به تنظیمات GPS هدایت می‌کند.
- **بک‌گراند/فورگراند/آنلاین/آفلاین**: `telemetry.js` رویدادهای ورود/خروج از پیش‌زمینه را ثبت می‌کند؛ `OfflineBanner.js` هر ۸ ثانیه وضعیت شبکه را چک و صف درخواست‌های آفلاین را به‌محض اتصال مجدد خودکار همگام می‌کند؛ `location.js` با `killServiceOnDestroy:false` سرویس موقعیت را در پس‌زمینه نگه می‌دارد.

### ۱۰. قابلیت جدید: حالت تعمیر (قطع دسترسی همه بجز مدیران)
- **`php/lib/routes.php`:** تابع `_maintenance_status()` + مسیرهای `GET /api/system/maintenance-status` (عمومی، بدون نیاز به ورود)، `GET/POST /api/admin/maintenance` (مخصوص مدیران) اضافه شد. در روت لاگین، تلاش ورود کاربران غیرمدیر هنگام فعال بودن این حالت با پیام دلخواه مسدود می‌شود.
- **`php/public/index.php`:** در دیسپچر مرکزی، بلافاصله بعد از احراز هویت هر درخواست، اگر حالت تعمیر فعال باشد و کاربر مدیر نباشد، درخواست با کد ۵۰۳ و پیام دلخواه رد می‌شود — یعنی **نشست‌های از‌قبل واردشده هم قطع می‌شوند**، نه فقط ورودهای جدید. مدیران کاملاً مستثنا هستند.
- **`php/tools/panel_source.jsx` → `panel.bundle.js`:** بخش «⛔ حالت تعمیر» به تب «امنیت و نسخه اپ» پنل اضافه شد؛ تیک فعال/غیرفعال + متن پیام قابل‌ویرایش.
- **`mobile/src/MaintenanceGuard.js` (جدید):** برای کاربران **از‌قبل واردشدهٔ** غیرمدیر، هر ۳۰ ثانیه و هنگام بازگشت به اپ وضعیت را چک و در صورت فعال بودن، صفحهٔ تمام‌صفحهٔ مسدودکننده با پیام دلخواه نمایش می‌دهد.
- **نکتهٔ ایمنی مهم:** این گارد **صفحهٔ ورود (Login) را هرگز مسدود نمی‌کند** — چون در آن لحظه هنوز مشخص نیست کاربر مدیر است یا نه؛ مسدودسازی ورود کاربران غیرمدیر فقط توسط خود API لاگین (بعد از احراز هویت) انجام می‌شود. این طراحی از قفل‌شدن ادمین پشت صفحهٔ ورود جلوگیری می‌کند.

### نحوهٔ استفاده
1. در پنل → تنظیمات → تب «امنیت و نسخه اپ» → بخش «حالت تعمیر»، تیک فعال‌سازی را بزنید و پیام دلخواه را بنویسید → ذخیره.
2. همهٔ کاربران غیرمدیر (چه در حال ورود، چه از‌قبل واردشده در اپ/پنل) پیام شما را می‌بینند و امکان استفاده ندارند.
3. برای بازگردانی، همان‌جا تیک را بردارید و ذخیره کنید.

---

## منبع: `CHANGELOG_MOBILE_BIRTHDATE_CALENDAR_NAN_FIX.md`

# اصلاح تقویم تاریخ تولد اپ موبایل

## مشکل
در صفحه «ویرایش اطلاعات من»، تقویم تاریخ تولد عنوان سال را به صورت `NaN` نمایش می‌داد.

## علت
کامپوننت `JDatePicker` مقدار اولیه تاریخ را به صورت آرایه انتظار داشت، اما صفحه پروفایل مقدار را به صورت آبجکت `{jy, jm, jd}` ارسال می‌کرد. در نتیجه سال و ماه `undefined` شده و عنوان تقویم `NaN` نمایش داده می‌شد.

## اصلاحات
- پشتیبانی کامل `JDatePicker` از مقدار اولیه به شکل آرایه، آبجکت و رشته تاریخ.
- جلوگیری از نمایش `NaN` با مقدار پیش‌فرض امن.
- افزودن انتخاب مستقیم سال.
- افزودن انتخاب مستقیم ماه.
- مناسب‌سازی تقویم برای تاریخ تولد با بازه سال ۱۳۰۰ تا ۱۴۱۰.
- حفظ خروجی تاریخ به شکل شمسی برای ذخیره در پروفایل.

---

## منبع: `CHANGELOG_MOBILE_MY_PROFILE_BIRTHDATE.md`

# اصلاح اطلاعات من در اپ موبایل

- افزودن آیتم تاریخ تولد به صفحه «ویرایش اطلاعات من» در اپ موبایل.
- اتصال تاریخ تولد به API `/api/me/profile`.
- نمایش تاریخ تولد ذخیره‌شده هنگام مراجعه مجدد به ویرایش اطلاعات.
- استفاده از تقویم شمسی موجود در اپ برای انتخاب تاریخ تولد.

## فیلدهای اطلاعات فردی موجود در سایت که فعلاً به اپ اضافه نشده‌اند

برای جلوگیری از شلوغ شدن صفحه، این فیلدها فقط شناسایی شده‌اند و بعد از تأیید مدیر می‌توانند به اپ اضافه شوند:

- کد ملی
- کد پرسنلی
- تاریخ شروع سابقه / شروع همکاری
- تعداد ستاره اختصاصی / درجه نمایشی
- الزام صحت‌سنجی حضور
- مجوز ارسال پیامک
- مجوز جانشینی
- مجوز ثبت رفاهیات
- مجوز ثبت فعالیت‌های فرهنگی
- مجوز مدیریت رانندگان موقت
- وضعیت فعال بودن حساب
- مجوز ورود از اندروید / وب
- معافیت امنیتی

فیلدهای نوع «مجوز و دسترسی» بهتر است در اپ فقط نمایشی باشند و ویرایش آن‌ها فقط از پنل مدیریت سایت انجام شود.

---

## منبع: `CHANGELOG_MOBILE_SELFIE_LOCATION_NOTIFY.md`

# تغییرات سلفی، رفاهیات/فرهنگی، موقعیت مکانی و اعلان خروج از محدوده

## 1) حذف سلفی خودکار در اپ موبایل
- حذف اجرای سلفی هنگام ورود به برنامه.
- حذف اجرای سلفی هنگام ثبت حضور.
- حذف سلفی دوره‌ای/زمان‌بندی‌شده از منطق اپ.
- سلفی فقط با دستور صریح سرور/مدیر اجرا می‌شود.
- Poll دستور سلفی از سرور به ۳۰ ثانیه کاهش یافت تا فرمان مدیر سریع‌تر اعمال شود.

## 2) رفاهیات و فعالیت‌های فرهنگی راننده
- وقتی کاربر از نتیجه جستجوی تاکسیران وارد صفحه رفاهیات یا فعالیت فرهنگی شود، کادر جستجوی کد ملی حذف می‌شود.
- ثبت و نمایش سوابق بر اساس همان راننده انتخاب‌شده انجام می‌شود.
- در حالت ورود مستقیم به صفحه، امکان جستجوی کد ملی همچنان باقی است تا صفحه از کار نیفتد.

## 3) بهبود دقت موقعیت مکانی
- ثبت حضور و خروج به جای موقعیت کش‌شده/تقریبی، چند نمونه GPS دقیق می‌گیرد و بهترین نمونه را انتخاب می‌کند.
- در ردیابی foreground و background دقت از Balanced به Highest تغییر کرد.
- distanceInterval کاهش یافت تا جابه‌جایی کاربر دقیق‌تر ثبت شود.
- موقعیت کش‌شده فقط وقتی استفاده می‌شود که تازه و نسبتاً دقیق باشد.

## 4) اعلان خروج از محدوده خط
- تنظیم جدید `station_exit_notify` اضافه شد.
- امکان فعال/غیرفعال کردن ارسال اعلان خروج از محدوده وجود دارد.
- حالت ارسال اعلان:
  - بر اساس سلسله‌مراتب: `manager_id` و جدول `user_managers`
  - ارسال به یک کاربر مشخص با `user_id`
- هنگام خروج کاربر از محدوده ایستگاه/خط، اعلان داخل برنامه برای مقصد تنظیم‌شده ثبت می‌شود.

## 5) تنظیمات پنل سایت
- بخش تنظیمات سلفی به حالت «فقط دستور سرور» تغییر کرد.
- گزینه‌های سلفی هنگام ورود، سلفی هنگام ثبت حضور و سلفی دوره‌ای از تنظیمات پنل حذف شدند.
- آیتم جدید «ارسال خروج از محدوده کاربر به مسئول بالادستی» به تنظیمات اضافه شد.

## نکته نصب/آپدیت
پس از جایگزینی فایل‌ها، اسکریپت‌های SQL داخل مسیر `php/db/upgrade.sql` را اجرا کنید تا کلیدهای تنظیمات جدید به دیتابیس اضافه شوند.

---

## منبع: `CHANGELOG_NATIONAL_CODE_SEARCH_BIRTHDAY_NOTIFY.md`

# اصلاح جستجوی کد ملی، اطلاعات من و اعلان تولد

## موارد اصلاح‌شده

1. **رفع کرش جستجوی راننده با کد ملی**
   - صفحه جزئیات راننده در اپ موبایل در برابر نبودن یا ناقص بودن اطلاعات خودرو، هشدارها و خطوط موقت مقاوم شد.
   - اگر سرور برای یک راننده خودرو یا بعضی فیلدها را برنگرداند، برنامه دیگر کرش نمی‌کند و اطلاعات موجود را نمایش می‌دهد.

2. **افزودن کد ملی به اطلاعات من در اپ**
   - فیلد «کد ملی» به صفحه ویرایش اطلاعات من اضافه شد.
   - ورودی فقط عددی و حداکثر ۱۰ رقم است.
   - مسیر `/api/me/profile` اجازه ذخیره `national_code` را دارد و طول ۱۰ رقمی آن را کنترل می‌کند.
   - مسیر `/api/me/full-profile` از قبل `national_code` را برمی‌گرداند و اکنون در اپ نمایش و ذخیره می‌شود.

3. **اصلاح اعلان تبریک تولد داخل برنامه**
   - تبریک تولد علاوه بر پیامک، همیشه داخل جدول اعلان‌های برنامه ثبت می‌شود.
   - برای کاربر Push notification نیز ارسال می‌شود.
   - مشکل حالت‌هایی که تنظیم تولد روی پیامک بود و اعلان داخل اپ ایجاد نمی‌شد، برطرف شد.

## فایل‌های تغییرکرده

- `mobile/src/screens/DriverScreen.js`
- `mobile/src/screens/AccountScreens.js`
- `php/lib/routes.php`
- `php/public/cron.php`

---

## منبع: `CHANGELOG_OFFLINE_MAP_MANUAL_DOWNLOAD.md`

# اصلاح دانلود نقشه آفلاین

- دانلود خودکار و اجباری نقشه آفلاین در اولین ورود به داشبورد حذف شد.
- دانلود نقشه فقط با انتخاب کاربر در مسیر «حساب کاربری > تنظیمات نقشه > شروع دانلود نقشه آفلاین» انجام می‌شود.
- امکان مشاهده وضعیت دانلود، پیشرفت دانلود و دانلود مجدد در صفحه تنظیمات حفظ شده است.
- هیچ تغییری در نسخه Expo/React Native، SDK، Gradle، پلاگین‌ها، dependencyها، package.json یا package-lock.json انجام نشده است.
- در بک‌اند هیچ مسیر یا وظیفه‌ای برای دانلود اجباری نقشه یافت نشد و تغییری در بک‌اند لازم نبود.

---

## منبع: `CHANGELOG_PHASE1_ATTENDANCE_REPORTS.md`

# فاز ۱ — حضور، شیفت خودکار و گزارش‌ها

## ثبت حضور من
- انتخاب خط در سمت سرور مقاوم شد؛ اگر اپ خط اشتباه ارسال کند اما کاربر داخل یکی از خطوط مجاز خودش باشد، سرور خط صحیح را تشخیص می‌دهد.
- دقت GPS گوشی در محدوده خطای مجاز لحاظ می‌شود تا خطای لحظه‌ای GPS باعث غیرفعال شدن ثبت حضور نشود.
- اپ هنگام ثبت ورود، دقت موقعیت (`accuracy`) را به سرور ارسال می‌کند.

## شیفت خودکار
- برای کاربرانی که شیفت اختصاصی ندارند، شیفت خودکار فعال شد.
- موظفی روزانه: ۷ ساعت و ۳۳ دقیقه.
- اپراتور و رئیس خط: ۲۷ دقیقه اضافه‌کار مجاز، بیشتر از آن مازاد حضور.
- بازرس، سربازرس و سربازرس ارشد: ۲ ساعت و ۲۷ دقیقه اضافه‌کار مجاز، بیشتر از آن مازاد حضور.
- نیروی اداری: ۴ ساعت اضافه‌کار مجاز، بیشتر از آن مازاد حضور.
- شب‌کاری خودکار از ۲۲:۰۰ تا ۰۶:۰۰ محاسبه می‌شود.
- جمعه‌کاری و تعطیل‌کاری رسمی خودکار محاسبه می‌شود.
- گزارش تردد و خلاصه کارکرد شامل مازاد حضور و مازاد تبدیل‌شده به اضافه‌کار شد.

## تایمر اپ
- در «ثبت حضور من» تایمر وضعیت موظفی، اضافه‌کار و مازاد حضور نمایش داده می‌شود.
- پس از پر شدن سقف اضافه‌کار، هشدار متنی ثبت خروج نمایش داده می‌شود.

## تحویل شیفت با QR
- تحویل‌دهنده می‌تواند کد تحویل شیفت بسازد.
- تحویل‌گیرنده با اسکن کد، برای نفر قبلی خروج و برای خودش ورود ثبت می‌کند.
- جدول `shift_handovers` برای لاگ تحویل شیفت اضافه شد.

## گزارش‌ها
- ارسال‌کننده گزارش می‌تواند گزارش خود را ویرایش یا حذف کند.
- دریافت‌کننده می‌تواند گزارش بی‌مورد را فقط از کارتابل خودش حذف کند.
- گزارش‌های بایگانی‌شده دیگر در شمارنده کارتابل گزارش‌های دریافتی محاسبه نمی‌شوند.
- تاریخچه ویرایش گزارش در جدول `report_edits` ذخیره می‌شود.

## فایل دیتابیس
- `php/db/upgrade_phase1_attendance_reports.sql`

---

## منبع: `CHANGELOG_PHASE2_REPORTS_NOTIFICATIONS_DASHBOARD.md`

# تغییرات فاز ۲ — گزارش‌ها، اعلان‌ها، داشبورد و نسخه‌بندی

## نسخه‌ها
- نسخه سایت در کد: `SITE_VERSION = 86`
- نسخه اپ اندروید در کد: `APP_VERSION = 0.5.60`
- نسخه Expo در `mobile/app.config.js`: `0.5.60`
- نسخه پکیج موبایل در `mobile/package.json`: `0.5.60`
- نسخه backend Node در `backend/package.json`: `0.2.0`

## گزارش‌ها
- افزودن اولویت گزارش: عادی، مهم، فوری
- امکان پیوست چند فایل برای گزارش‌ها
- پشتیبانی API از چند پیوست تصویر/PDF
- ذخیره پیوست‌ها در جدول `report_attachments`
- امکان ویرایش گزارش توسط ارسال‌کننده تا قبل از مشاهده دریافت‌کننده
- ثبت تاریخچه ویرایش‌ها در `report_edits`
- ثبت کامل رویدادهای گزارش در `report_audit`
- ثبت مشاهده گزارش توسط دریافت‌کننده در `report_reads`
- امکان حذف گزارش توسط ارسال‌کننده طبق محدودیت مشاهده
- امکان حذف شخصی گزارش توسط دریافت‌کننده
- امکان رد/حذف گزارش بی‌مورد با ثبت علت
- اصلاح منطق شمارنده گزارش‌های بایگانی/حذف‌شده با جدول‌های جداگانه

## اپ موبایل
- افزودن انتخاب اولویت در ارسال گزارش
- افزودن چند پیوست تصویر و PDF در ارسال گزارش
- نمایش اولویت و تعداد پیوست در فهرست گزارش‌ها
- نمایش پیوست‌های چندگانه در جزئیات گزارش
- افزودن ویرایش گزارش در جزئیات گزارش برای ارسال‌کننده
- افزودن حذف گزارش
- افزودن رد گزارش بی‌مورد با ثبت علت برای دریافت‌کننده
- افزودن حذف اعلان از داخل اپ

## اعلان‌ها
- افزودن API حذف اعلان کاربر
- افزودن API خوانده‌شدن اعلان‌ها بر اساس نوع
- آماده‌سازی برای همگام‌سازی بهتر اعلان‌ها بین وب، اپ و کانال‌های پیام‌رسان

## داشبورد مدیریتی
- افزودن API داشبورد لحظه‌ای فاز ۲:
  - `/api/admin/phase2-dashboard`
- افزودن API وضعیت آنلاین شیفت‌ها:
  - `/api/admin/shift-live`

## دیتابیس
فایل ارتقا:
`php/db/upgrade_phase2_reports_notifications.sql`

این فایل قبل از استفاده از نسخه جدید باید روی دیتابیس اجرا شود.

---

## منبع: `CHANGELOG_PHASE3_REAPPLIED_ON_UPLOADED_FILE.md`

# اعمال فاز ۳ روی فایل ارسالی

نسخه سایت در کد: `SITE_VERSION = 88`

نسخه اپ اندروید در کد: `APP_VERSION = 0.5.71`

نسخه Expo و package موبایل: `0.5.71`

## اقدامات
- فایل ارسالی به‌عنوان مبنای نسخه جدید استفاده شد.
- تغییرات فاز ۳ روی همین نسخه تثبیت شد.
- نسخه سایت از داخل `php/lib/routes.php` افزایش یافت.
- نسخه اپ اندروید از داخل `mobile/app.config.js` و `mobile/package.json` افزایش یافت.
- فایل changelog فاز ۳ نیز با نسخه جدید به‌روزرسانی شد.

## یادآوری
در صورت وجود APK قبلی روی گوشی، پس از ساخت APK جدید با versionCode بالاتر، نسخه جدید بدون خطای downgrade نصب می‌شود.

---

## منبع: `CHANGELOG_PHASE3_TEMP_DRIVERS_CHECKIN_UI.md`

# فاز ۳ — رانندگان موقت، ثبت حضور و تکمیل کارکرد

نسخه سایت در کد: `SITE_VERSION = 88`

نسخه اپ اندروید در کد: `APP_VERSION = 0.5.71`

نسخه Expo و package موبایل: `0.5.71`

## تغییرات
- مقاوم‌سازی جستجوی راننده موقت با کد ملی فارسی/انگلیسی و حذف جداکننده‌ها.
- بهبود جستجوی خودرو بر اساس پلاک، کد بهره‌برداری، خط، نام راننده، موبایل و کد ملی.
- ثبت تاریخچه افزودن و پایان تخصیص راننده موقت در `temp_line_driver_history`.
- ثبت لاگ علت رد شدن «ثبت حضور من» در `attendance_reject_logs` برای عیب‌یابی مواردی که کاربر داخل محدوده است ولی ثبت رد می‌شود.
- افزایش نسخه سایت و اپ از داخل کدنویسی‌ها.
- افزودن فایل ارتقای دیتابیس: `php/db/upgrade_phase3_temp_drivers_checkin_ui.sql`.

## بعد از نصب
فایل SQL بالا را یک بار روی دیتابیس اجرا کنید و سپس کش مرورگر/اپ را پاک کنید.


## اعمال مجدد فاز ۳ روی فایل ارسالی
- فاز ۳ روی نسخه ارسالی مجدداً اعمال و نسخه‌های واقعی داخل کد افزایش داده شد.
- مسیرهای اصلی نسخه بررسی شد: `php/lib/routes.php`، `mobile/app.config.js`، `mobile/package.json`.

---

## منبع: `CHANGELOG_PHASE4_VERSION_STATUS_AUDIT.md`

# گزارش وضعیت انجام‌شده و باقی‌مانده سامانه — فاز ۴

نسخه این خروجی:
- نسخه سایت در کد: `SITE_VERSION = 89`
- نسخه اپ اندروید در کد: `APP_VERSION = 1.0.1`
- نسخه `mobile/app.config.js`: `1.0.1`
- نسخه `mobile/package.json`: `1.0.1`
- `versionCode`: `10001`

## موارد انجام‌شده

1. موتور حضور و غیاب و شیفت خودکار:
   - تشخیص نزدیک‌ترین خط
   - ثبت علت رد شدن حضور
   - تحویل شیفت با QR
   - محاسبه موظفی، اضافه‌کار، مازاد حضور، شب‌کاری، جمعه‌کاری و تعطیل‌کاری

2. گزارش‌ها و اعلان‌ها:
   - ویرایش و حذف گزارش
   - بایگانی و اصلاح شمارنده
   - داشبورد گزارش‌ها
   - اعلان درون‌برنامه‌ای، Push و اتصال بله

3. رانندگان موقت:
   - جستجوی خودرو
   - انتخاب بهره‌بردار و کمکی
   - تاریخچه انتصاب
   - رفع مشکل جداکننده کد ملی

4. اپ موبایل:
   - فیش حقوقی
   - اطلاعات من
   - تاریخ تولد و کد ملی
   - صداهای اختصاصی
   - آلارم صحت‌سنجی

5. ربات بله:
   - اتصال کاربر/راننده
   - ارسال پیام‌های سامانه از کانال بله
   - لاگ پیام‌ها

## موارد باقی‌مانده پیشنهادی

1. حذف Babel داخل مرورگر و تبدیل پنل وب به Build تولیدی.
2. تست خودکار APIها، حضور، گزارش‌ها و محاسبات شیفت.
3. صف پایدار ارسال پیامک، Push و بله با Retry.
4. ایمپورت رسمی تقویم تعطیلات هر سال.
5. همگام‌سازی آفلاین موبایل برای حضور، گزارش و موقعیت.
6. پنل Rule Engine برای تنظیم سقف موظفی/اضافه‌کار هر سمت بدون تغییر کد.
7. مانیتورینگ فنی، لاگ خطاها و داشبورد سلامت سرور.

## تغییرات فاز ۴

- تثبیت نسخه پایه عمومی روی `1.0.1`.
- افزودن API وضعیت پروژه:
  - `GET /api/project-version`
  - `GET /api/admin/project-status`
- افزودن اسکریپت دیتابیس:
  - `php/db/upgrade_phase4_version_status.sql`

---

## منبع: `CHANGELOG_PHASE5_STABILITY_RULES_UPGRADE.md`

# CHANGELOG_PHASE5_STABILITY_RULES_UPGRADE.md

## نسخه
- نسخه سایت: `SITE_VERSION = 90`
- نسخه اپلیکیشن اندروید: `1.0.2`
- Android `versionCode`: `10002`

## تغییرات انجام‌شده

### 1) تجمیع ارتقای دیتابیس
- فایل `php/public/upgrade.php` اضافه شد.
- از این نسخه به بعد به‌روزرسانی‌های دیتابیس در فایل جداگانه SQL قرار داده نمی‌شود.
- ارتقاها به‌صورت idempotent انجام می‌شوند؛ یعنی اجرای چندباره فایل باعث خطای تکراری نمی‌شود.
- اجرای ارتقا با کلید `UPGRADE_KEY` محافظت شده است.

### 2) صف پایدار پیام‌ها
- فایل `php/lib/DeliveryQueue.php` اضافه شد.
- جدول `delivery_queue` برای صف ارسال پیامک، Push و Bale ایجاد می‌شود.
- پیام‌های ناموفق در صف ثبت می‌شوند و با Retry قابل ارسال مجدد هستند.
- API مدیریتی مشاهده و پردازش صف اضافه شد:
  - `GET /api/admin/delivery-queue`
  - `POST /api/admin/delivery-queue/process`

### 3) Rule Engine پایه برای کارکرد
- جدول `role_work_rules` اضافه شد.
- قوانین پیش‌فرض برای اپراتور، رئیس خط، بازرس، سربازرس، سربازرس ارشد و نیروی اداری تعریف شد.
- API مدیریت قوانین اضافه شد:
  - `GET /api/admin/rule-engine/roles`
  - `PUT /api/admin/rule-engine/roles/{role_key}`

### 4) وضعیت سلامت و مانیتورینگ
- جدول `system_health_logs` اضافه شد.
- API وضعیت سلامت اضافه شد:
  - `GET /api/admin/system-health`

### 5) صف آفلاین موبایل
- صف آفلاین اپلیکیشن به نسخه `offline_queue_v2` ارتقا یافت.
- endpoint تجمیعی دریافت صف آفلاین اضافه شد:
  - `POST /api/mobile/offline-sync`
- فایل‌های `mobile/src/offline.js` و `mobile/src/api.js` اصلاح شدند.

### 6) نسخه‌ها
- `php/lib/routes.php`
- `mobile/app.config.js`
- `mobile/package.json`
- `mobile/.env`
- `mobile/.env.example`

به نسخه جدید به‌روزرسانی شدند.

## وضعیت باقی‌مانده پس از این نسخه
- ساخت نهایی Production Bundle پنل و حذف کامل Babel از مرورگر نیازمند اجرای Build در محیط توسعه دارای Node/Vite است.
- تست میدانی GPS، Push، QR و آلارم باید روی گوشی واقعی انجام شود.
- تست خودکار کامل APIها و سناریوهای حضور/گزارش/شیفت هنوز می‌تواند تکمیل‌تر شود.
- اتصال به منبع رسمی تعطیلات در صورت ارائه API معتبر قابل انجام است.

---

## منبع: `CHANGELOG_PHASE6_MONITORING_HOLIDAYS_QUEUE.md`

# فاز ۶ — پایش، تعطیلات، صف پیام و تشخیص خطای موبایل

نسخه سایت: 91  
نسخه اپ: 1.0.3  
versionCode: 10003

## انجام‌شده
- افزایش نسخه‌ها از داخل کد.
- ادامه سیاست تجمیع دیتابیس در `php/public/upgrade.php`.
- افزودن API مدیریت تعطیلات رسمی و ایمپورت گروهی تاریخ‌های شمسی.
- افزودن Dead-letter برای پیام‌های ناموفق صف ارسال.
- افزودن API کرون پردازش صف پیام و انتقال پیام‌های ناموفق قطعی.
- افزودن لاگ خطاهای اپ موبایل و helper سمت اپ برای ارسال خطاها.
- افزودن API محاسبه آزمایشی Rule Engine برای موظفی/اضافه‌کار/مازاد حضور.
- افزودن Health Check نسخه ۲ برای بررسی دیتابیس، صف پیام، پیام‌های شکست‌خورده و خطاهای موبایل.
- افزودن پاکسازی کش API.

## باقی‌مانده برای فاز بعد
- Build واقعی پنل و حذف کامل Babel مرورگر در محیط توسعه دارای Node/Vite.
- تست میدانی APK روی گوشی واقعی برای GPS، Push و آلارم.
- تست بارگذاری سرور نهایی و تنظیم منابع هاست.

---

## منبع: `CHANGELOG_PHASE7_PART10_REAL_PLATE_MODEL_TRAINING.md`

# تغییرات نسخه 1.0.13 — آموزش واقعی مدل پلاک و پنل مدیریت نمونه‌ها

## نسخه‌ها
- نسخه اپ اندروید: `1.0.13`
- نسخه سایت: `SITE_VERSION = 101`
- نسخه کد اندروید: `versionCode = 10013`
- نسخه ارتقای دیتابیس: `phase7-part10-1.0.13`

## تغییرات اصلی
- پیاده‌سازی پنل مدیریت نمونه‌های آموزشی پلاک در پنل مدیریت سایت.
- افزودن گردش کار بررسی نمونه‌ها: `verified`، `pending`، `rejected`.
- افزودن امکان اصلاح دستی دو رقم اول و سه رقم آخر پلاک از پنل مدیریت.
- نمایش عکس برش‌خورده پلاک و تصویر اصلی خودرو در پنل مدیریت.
- افزودن خروجی Manifest از نمونه‌های تأییدشده برای آموزش مدل.
- پیاده‌سازی اسکریپت آموزش واقعی مدل OCR پنج‌رقمی پلاک تاکسی ۱۲/ت.
- پیاده‌سازی مدل [قابلیت منسوخ و حذف‌شده] برای تشخیص ارقام پلاک از تصویر برش‌خورده.
- ثبت اجرای آموزش در جدول `plate_model_runs`.
- نگهداری مسیر مدل، متادیتا، دقت، تعداد نمونه و کلاس‌های دیده‌شده.
- افزودن API تشخیص پلاک با مدل آموزش‌دیده روی سرور.
- اتصال اپ موبایل به مدل آموزش‌دیده سرور به‌عنوان اولویت اول در اسکن پلاک.
- حفظ OCR محلی به‌عنوان fallback اگر مدل سرور فعال نباشد یا اینترنت قطع باشد.

## APIهای جدید
- `GET /api/admin/plate-training/status`
- `GET /api/admin/plate-training/samples`
- `POST /api/admin/plate-training/samples/{id}/review`
- `GET /api/admin/plate-training/export-manifest`
- `POST [قابلیت منسوخ و حذف‌شده]`
- `POST [قابلیت منسوخ و حذف‌شده]`
- `GET /api/admin/phase7-part10/status`
- `GET /api/project-version-v7p10`

## دیتابیس
### ستون‌های جدید در `plate_scan_samples`
- `review_note`
- `reviewed_by`
- `reviewed_at`
- `exported_at`

### جدول جدید
- `plate_model_runs`

## فایل‌های جدید
- `php/tools/[قابلیت منسوخ و حذف‌شده]`
- `php/tools/[قابلیت منسوخ و حذف‌شده]`

## نکته اجرایی
آموزش واقعی مدل نیازمند نمونه‌های واقعی تأییدشده است. اگر کمتر از ۱۰ نمونه دارای تصویر برش‌خورده در دیتابیس وجود داشته باشد، API آموزش وضعیت `insufficient` برمی‌گرداند و مدل فعال ساخته نمی‌شود.

---

## منبع: `CHANGELOG_PHASE7_PART11_PRO_BALE_BOT_FORMS.md`

# تغییرات نسخه 1.0.14 — ربات حرفه‌ای بله و فرم‌های ثبت‌نام

## انجام‌شده
- حرفه‌ای‌سازی ربات بله از حالت ارسال پیام ساده به ربات تعاملی.
- افزودن منوی Reply Keyboard برای کاربران بله.
- افزودن پاسخ‌های سفارشی قابل تعریف از پنل مدیریت.
- افزودن فرم‌های ثبت‌نام/درخواست مختلف داخل ربات بله.
- دریافت کد ملی در فرم‌های بله و تکمیل خودکار اطلاعات راننده/کاربر از دیتابیس.
- ذخیره وضعیت مکالمه کاربر برای تکمیل مرحله‌ای فرم‌ها.
- ذخیره ثبت‌نام‌ها و درخواست‌های دریافتی از بله در دیتابیس.
- افزودن بررسی مدیریتی ثبت‌نام‌ها: تأیید، رد، انجام‌شده.
- افزودن لاگ رویدادهای ورودی ربات.
- افزودن APIهای مدیریتی منو، پاسخ سفارشی، فرم، ثبت‌نام و رویداد.
- توسعه پنل تنظیمات بله با تب‌های جدید.
- افزایش نسخه اپ به 1.0.14، نسخه سایت به 102 و versionCode به 10014.

## APIهای جدید
- GET /api/admin/bale/menu-items
- POST /api/admin/bale/menu-items
- PUT /api/admin/bale/menu-items/{id}
- DELETE /api/admin/bale/menu-items/{id}
- GET /api/admin/bale/custom-replies
- POST /api/admin/bale/custom-replies
- PUT /api/admin/bale/custom-replies/{id}
- DELETE /api/admin/bale/custom-replies/{id}
- GET /api/admin/bale/forms
- POST /api/admin/bale/forms
- PUT /api/admin/bale/forms/{id}
- DELETE /api/admin/bale/forms/{id}
- GET /api/admin/bale/form-submissions
- POST /api/admin/bale/form-submissions/{id}/review
- GET /api/admin/bale/events

## جداول جدید
- bale_menu_items
- bale_custom_replies
- bale_forms
- bale_form_fields
- bale_chat_sessions
- bale_form_submissions
- bale_bot_events

---

## منبع: `CHANGELOG_PHASE7_PART12_UNIFIED_MESSENGER_BOTS.md`

# Phase 7 Part 12 — Unified Messenger Bots

Version: 1.0.15  
Site version: 103  
Android versionCode: 10015

## Completed

- Added unified messenger bot engine for Telegram and Eitaa.
- Kept Bale backward-compatible while adding a shared cross-messenger hub.
- Added Telegram webhook endpoint:
  - `POST /api/telegram/webhook/{secret}`
- Added Eitaa webhook endpoint:
  - `POST /api/eitaa/webhook/{secret}`
- Added shared menu, custom replies and form workflow for all three bots.
- Added national-code based account binding for Telegram and Eitaa.
- Added automatic driver/user data prefill in bot forms for Telegram and Eitaa.
- Added per-platform subscriber tables, message logs, chat sessions, form submissions and incoming event logs.
- Added per-platform admin APIs under:
  - `/api/admin/messengers/{platform}/...`
- Added platform tabs in the bot settings panel:
  - Bale
  - Telegram
  - Eitaa
- Added per-platform API base URL, token, webhook secret, API path mode and enabled item settings.
- Extended message mirroring to enabled messenger bots through `MessengerHub`.
- Extended queued delivery retry for Telegram and Eitaa.
- Consolidated database upgrade in:
  - `php/public/upgrade.php`

## New core file

- `php/lib/MessengerBots.php`

## New database tables

- `messenger_subscribers`
- `messenger_message_log`
- `messenger_chat_sessions`
- `messenger_form_submissions`
- `messenger_bot_events`

## Shared configuration model

The menu items, custom replies, forms and form fields are shared across all bots.
Platform-specific data is isolated by `platform`.

Supported platforms:

- `bale`
- `telegram`
- `eitaa`

## Notes

- Telegram default API base: `https://api.telegram.org`
- Eitaa default API base is configurable from the panel because providers may use different bot gateways.
- Real webhook registration must be done on the production domain after token setup.

---

## منبع: `CHANGELOG_PHASE7_PART13_SYSTEM_HEALTH_DASHBOARD.md`

# CHANGELOG — Phase 7 Part 13: Complete System Health Dashboard

Version: 1.0.16  
Site version: 104  
Android versionCode: 10016

## انجام‌شده

- تکمیل داشبورد سلامت سامانه در پنل مدیریت.
- افزودن آیتم «داشبورد سلامت سامانه» در منوی اصلی پنل.
- افزودن API جامع سلامت سامانه:
  - `GET /api/admin/system-health-dashboard`
  - `POST /api/admin/system-health-dashboard/run`
- افزودن Endpoint کرون‌جاب سلامت:
  - `GET /api/cron/system-health-probe?key=CRON_KEY`
  - `POST /api/cron/system-health-probe`
- افزودن پایش بخش‌های اصلی:
  - دیتابیس
  - مسیرهای ذخیره‌سازی و آپلود
  - افزونه‌های PHP
  - فضای دیسک
  - صف ارسال پیام‌ها
  - Dead-letter پیام‌ها
  - همگام‌سازی آفلاین
  - خطاهای اپ موبایل
  - خطاهای GPS و رد حضور
  - OCR و آموزش پلاک
  - ربات‌های بله، تلگرام و ایتا
  - کرون‌جاب‌ها
- افزودن ثبت تاریخچه چک‌ها در `system_health_checks`.
- افزودن جدول رخدادهای سلامت در `system_health_incidents`.
- نمایش خطاهای اخیر سامانه، خطاهای اپ و شکست‌های آفلاین در پنل.
- افزودن اجرای دستی چک سلامت و ثبت نتیجه از داخل پنل.
- افزودن حالت Auto Refresh سی‌ثانیه‌ای در داشبورد سلامت.
- تجمیع بروزرسانی دیتابیس در `php/public/upgrade.php`.

## فایل‌های اصلی تغییرکرده

- `php/lib/routes.php`
- `php/public/panel.html`
- `php/public/upgrade.php`
- `mobile/app.config.js`
- `mobile/package.json`
- `mobile/.env`
- `mobile/.env.example`
- `mobile/src/diagnostics.js`

## بررسی

- Syntax همه فایل‌های PHP بررسی شد.
- خطای نحوی PHP مشاهده نشد.

---

## منبع: `CHANGELOG_PHASE7_PART14_REMOVE_BROWSER_BABEL.md`

# CHANGELOG — Phase 7 Part 14

## نسخه
- App Version: `1.0.17`
- Site Version: `105`
- Android Version Code: `10017`

## هدف
حذف کامل اجرای Babel در مرورگر از پنل سایت و تبدیل پنل React/JSX به خروجی Build شده برای کاهش خطاهای Syntax، کاهش کندی بارگذاری و آماده‌سازی پنل برای محیط تولید.

## تغییرات انجام‌شده
- حذف `vendor/babel/babel.min.js` از پنل اصلی سایت.
- حذف `<script type="text/babel">` از پنل اصلی سایت.
- تبدیل JSX پنل اصلی به JavaScript تولیدی:
  - `php/public/assets/panel.bundle.js`
- استخراج CSS پنل اصلی به فایل مستقل:
  - `php/public/assets/panel.bundle.css`
- سبک‌سازی `php/public/panel.html` و اتصال آن به فایل‌های Build شده.
- حذف دانلود خودکار Babel از:
  - `php/public/setup-libs.php`
  - `php/public/vendor/fetch.php`
  - `php/public/vendor/fetch-vendor.sh`
- حذف Runtime Babel از پنل‌های قدیمی/پشتیبان:
  - `backend/public/index.html`
  - `web/*.html`
- تولید Bundle برای پنل‌های پشتیبان:
  - `backend/public/assets/backend_panel.bundle.js`
  - `backend/public/assets/backend_panel.bundle.css`
  - `web/assets/web_panel.bundle.js`
  - `web/assets/web_panel.bundle.css`
- افزودن Source قابل بازسازی برای پنل:
  - `php/tools/panel_source.jsx`
- افزودن اسکریپت بازسازی Bundle:
  - `php/tools/build_panel_bundle.sh`
- افزودن بررسی وضعیت Bundle پنل در داشبورد سلامت سامانه.
- افزودن وضعیت نسخه جدید:
  - `GET /api/admin/phase7-part14/status`
  - `GET /api/project-version-v7p14`

## بررسی‌ها
- `php -l php/lib/routes.php`
- `php -l php/public/upgrade.php`
- `php -l php/public/setup-libs.php`
- `php -l php/public/vendor/fetch.php`
- `node --check php/public/assets/panel.bundle.js`
- `node --check backend/public/assets/backend_panel.bundle.js`
- `node --check web/assets/web_panel.bundle.js`
- جستجوی نهایی برای `babel.min.js`، `text/babel` و `@babel/standalone` در فایل‌های اجرایی پروژه انجام شد.

## نتیجه
پنل دیگر JSX را داخل مرورگر کامپایل نمی‌کند. Babel از Runtime حذف شد و پنل با فایل‌های Build شده اجرا می‌شود.

---

## منبع: `CHANGELOG_PHASE7_PART15_SQL_IMPORT_PLATE_SETTINGS.md`

# تغییرات نسخه 1.0.18 — اصلاح فایل SQL Import و تنظیمات پلاک‌خوان

- تولید فایل `php/db/upgrade_import_mysql.sql` مناسب Import مستقیم در دیتابیس.
- حذف کامل دستور ناسازگار `CREATE INDEX IF NOT EXISTS` از فایل‌های اجرایی SQL.
- جایگزینی ساخت ایندکس‌ها با روش امن مبتنی بر `information_schema`.
- بررسی وجود جدول و ستون قبل از ساخت ایندکس.
- افزودن تنظیمات پلاک‌خوان به پنل سایت در مسیر `تنظیمات سامانه → پلاک‌خوان`.
- افزودن مقدارهای پیش‌فرض پلاک‌خوان در دیتابیس.
- افزایش نسخه سایت به 106 و نسخه اپ به 1.0.18.

---

## منبع: `CHANGELOG_PHASE7_PART16_SEARCH_PLATE_TRAINING_FIX.md`

# تغییرات نسخه 1.0.19 — رفع کرش جستجو و خطای پلاک‌خوان

- افزایش نسخه سایت به 107 و نسخه اپ به 1.0.19.
- افزایش versionCode اندروید به 10019.
- رفع خطای 500 در صفحه «پلاک‌خوان و آموزش OCR».
- مقاوم‌سازی API نمونه‌های آموزشی پلاک در برابر ستون‌ها/جداول ناقص در دیتابیس‌های ارتقایافته.
- اصلاح جستجوی پلاک تاکسی ۱۲/ت با نرمال‌سازی اعداد فارسی، عربی و انگلیسی.
- اصلاح جستجوی خودرو با پلاک موجود در دیتابیس حتی در صورت تفاوت فاصله، خط تیره یا فرمت ذخیره پلاک.
- مقاوم‌سازی جستجوی کد ملی برای ستون‌های national_id و national_code.
- جلوگیری از کرش اپ هنگام دریافت پاسخ‌های ناقص یا قدیمی از کش.
- اصلاح کش آفلاین جستجوی پلاک؛ جستجو اکنون با کل ساختار پلاک انجام می‌شود، نه فقط دو رقم اول.
- ثبت خطای داخلی پلاک‌خوان در لاگ سرور برای عیب‌یابی بعدی.
- بروزرسانی فایل‌های SQL ارتقا به نسخه 1.0.19.

---

## منبع: `CHANGELOG_PHASE7_PART17_PLATE_TRAINING_500_SEARCH_FIX.md`

# Phase 7 Part 17 — Plate Training 500 & Search Stabilization

نسخه: 1.1.0
SITE_VERSION: 110
versionCode: 10100

## اصلاحات
- رفع خطای 500 در مسیر `/api/admin/plate-training/samples`.
- ترمیم خودکار ستون‌های ناقص جدول `plate_scan_samples` روی دیتابیس‌های قدیمی.
- انتخاب امن ستون‌ها در API نمونه‌های آموزشی پلاک؛ اگر ستون هنوز موجود نباشد مقدار `NULL` برگردانده می‌شود.
- جلوگیری از توقف پنل «پلاک‌خوان و آموزش OCR» هنگام ناقص بودن ساختار دیتابیس.
- تکمیل کش آفلاین جستجو با فهرست خودروها و پلاک نرمال‌شده.
- تقویت جستجوی آفلاین پلاک و کد ملی با اعداد فارسی/عربی/انگلیسی.
- بروزرسانی نسخه سایت به 110 و نسخه اپ به 1.1.0.
- افزودن SQL قابل Import مستقیم برای phpMyAdmin: `php/db/upgrade_import_mysql.sql`.

## اجرای لازم روی سرور
فایل `php/db/upgrade_import_mysql.sql` را در phpMyAdmin ایمپورت کنید یا `php/public/upgrade.php` را از مرورگر اجرا کنید.

---

## منبع: `CHANGELOG_PHASE7_PART18_PLATE_MODEL_RUNS_500_FIX.md`

# Phase 7 Part 18 — رفع ریشه‌ای خطای 500 در «پلاک‌خوان و آموزش OCR»

## علت واقعی خطا (یافته‌شده پس از بررسی کامل کد)
در Part 17 فقط جدول `plate_scan_samples` دارای مکانیزم ترمیم خودکار ستون‌های ناقص
(ALTER TABLE ADD COLUMN برای نصب‌های قدیمی) شده بود. جدول دوم مرتبط،
`plate_model_runs`، همچنان فقط با `CREATE TABLE IF NOT EXISTS` ساخته می‌شد.

روی سرورهایی که این جدول از فازهای قبلی (۷.۱۰ به بعد) با ستون‌های کمتر ساخته شده
بود (مثلاً بدون `manifest_path` / `log_text` / `error_text`)، مسیر
`POST [قابلیت منسوخ و حذف‌شده]` هنگام اجرای INSERT/UPDATE با خطای
«Unknown column» روبه‌رو می‌شد. این خطا داخل try/catch نبود، پس مستقیماً به
handler عمومی index.php می‌رسید و پیام «خطای داخلی سرور» (کد 500) دقیقاً همان
پیامی که مشاهده می‌شد، نمایش داده می‌شد.

## اصلاحات این نسخه
- افزودن حلقهٔ ترمیم خودکار ستون‌های `plate_model_runs` در `_ensure_plate_training()`
  (`php/lib/routes.php`)، دقیقاً مشابه مکانیزمی که قبلاً فقط برای
  `plate_scan_samples` وجود داشت.
- افزودن دستورات معادل `add_col`/`add_idx` برای `plate_model_runs` در
  `php/public/upgrade.php`.
- افزودن دستورات معادل `__taxi_add_col`/`__taxi_add_idx` برای `plate_model_runs`
  در `php/db/upgrade_import_mysql.sql` (برای Import مستقیم در phpMyAdmin).
- پوشاندن کل بدنهٔ route مربوط به `[قابلیت منسوخ و حذف‌شده]` در
  try/catch: اکنون هر خطای پایگاه‌داده یا فایل‌سیستم به‌جای خطای 500 عمومی،
  پیام مشخص و قابل‌فهم برمی‌گرداند و ردیف `plate_model_runs` مربوطه (در صورت
  وجود) با status='failed' به‌روزرسانی می‌شود، و در `app_error_logs` نیز ثبت
  می‌شود.

## اجرای لازم روی سرور
یکی از این دو کافی است:
1. فایل `php/db/upgrade_import_mysql.sql` را در phpMyAdmin ایمپورت کنید (idempotent است، اجرای مجدد مشکلی ندارد).
2. یا صفحهٔ `php/public/upgrade.php` را از مرورگر باز کنید.

نکته: خود کد PHP هم در هر بار فراخوانی مسیرهای پلاک (`_ensure_plate_training`)
این ترمیم را خودکار انجام می‌دهد، پس حتی بدون اجرای دستی مراحل بالا، پس از
آپلود همین نسخه روی سرور، خطا باید برطرف شود؛ اجرای Import صرفاً برای اطمینان
بیشتر و هم‌گام‌سازی سریع‌تر توصیه می‌شود.

---

## به‌روزرسانی: علت اصلی واقعی پیدا شد (خط ۲۹۱ routes.php)

بعد از فعال‌سازی `API_DEBUG` روی سرور، خطای واقعی مشخص شد: مشکل از تابع
`_plate_python_bin()` بود، دقیقاً همان‌جایی که `shell_exec()` صدا زده می‌شد.

**علت دقیق:** روی بسیاری از هاست‌های اشتراکی (به‌ویژه هاست‌های فارسی)، توابع
اجرای دستور سیستم مثل `shell_exec`, `exec`, `system` از طریق `disable_functions`
در `php.ini` غیرفعال هستند. وقتی تابعی به این شکل غیرفعال باشد، صدا زدن آن —
برخلاف تصور رایج — با عملگر `@` سرکوب **نمی‌شود** و یک خطای مرگبار
(`Fatal Error: Call to undefined function shell_exec()`) ایجاد می‌کند.

این خطا در `_plate_python_bin()` داخل هیچ try/catch ای نبود. چون این تابع از
`_plate_model_status_payload()` صدا زده می‌شود (برای فیلد `python_available`)
و این تابع هم در انتهای مسیر `/api/admin/plate-training/samples` برای پیوست
کردن وضعیت مدل فراخوانی می‌شود، خطا مستقیم به handler عمومی `index.php`
می‌رسید و پیام «خطای داخلی سرور» (500) نمایش داده می‌شد — even عندما کوئری
اصلی نمونه‌ها موفق بود.

### اصلاح
`_plate_python_bin()` بازنویسی شد تا:
- ابتدا با `function_exists('shell_exec')` بررسی کند که آیا این تابع
  غیرفعال شده یا نه (PHP برای توابع داخل `disable_functions`،
  `function_exists` را `false` برمی‌گرداند).
- در صورت غیرفعال بودن، بدون هیچ فراخوانی خطرناکی، مقدار `false` (معادل
  «Python در دسترس نیست») برگرداند.
- کل بلوک را در try/catch قرار داد (برای مقاومت در برابر هر رفتار غیرمنتظرهٔ
  دیگر روی هاست‌های مختلف).
- نتیجه را کش (static) کند تا در هر درخواست فقط یک بار بررسی شود.

با این اصلاح، پنل «پلاک‌خوان و آموزش OCR» حتی روی هاستی که Python/shell_exec
اصلاً در دسترس نیست، به‌جای خطای 500، به‌درستی بارگذاری می‌شود و صرفاً
«آموزش مدل واقعی» (که نیاز به Python دارد) غیرفعال نمایش داده خواهد شد؛
بقیهٔ قابلیت‌ها (ثبت نمونه، بازبینی، جستجو) بدون تغییر کار می‌کنند.

---

## منبع: `CHANGELOG_PHASE7_PART19_MOBILE_LOCAL_OCR_REVIEW_422_FIX.md`

# Phase 7 Part 19 — Mobile Local OCR / Review 422 / JSON Guard

- تشخیص پلاک از مسیر اسکن موبایل کاملاً محلی شد و فراخوانی `/plate-ocr/predict` حذف شد.
- سرور فقط نمونه برش‌خورده و اصلاح‌شده را برای دیتاست آموزشی دریافت می‌کند.
- تغییر وضعیت نمونه به pending/rejected بدون پلاک اصلاح‌شده معتبر مجاز شد.
- وضعیت verified همچنان فقط با پلاک معتبر پذیرفته می‌شود.
- دکمه‌های سریع پنل دیگر `corrected_plate` خالی ارسال نمی‌کنند.
- لایه API پنل و موبایل پاسخ‌های HTML/PHP را بدون JSON.parse crash مدیریت می‌کند.
- مسیرهای جستجوی کد ملی و پلاک در اپ در برابر پاسخ نامعتبر و خطای داخلی سرور مقاوم شدند.

---

## منبع: `CHANGELOG_PHASE7_PART1_ATTENDANCE_TIMER_SYNC.md`

# CHANGELOG - Phase 7 Part 1 - Attendance Timer Sync & Configurable Auto Shift

Version: App 1.0.4 / Site 92 / versionCode 10004

## انجام‌شده در این قسمت

### 1) همگام‌سازی تایمر حضور با سرور
- API `/api/my/work-timer` بازنویسی شد تا زمان مرجع سرور، زمان ورود، زمان سپری‌شده، موظفی، سقف اضافه‌کار و مازاد حضور را برگرداند.
- اپ موبایل همچنان تایمر لحظه‌ای را هر ثانیه نمایش می‌دهد، اما هر ۶۰ ثانیه با سرور Sync می‌شود تا اختلاف ساعت گوشی باعث خطا نشود.
- پیام هشدار سقف اضافه‌کار فقط یک‌بار هنگام ورود به مرحله مازاد حضور نمایش داده می‌شود.

### 2) Rule Engine پایه برای شیفت خودکار
- شیفت خودکار از جدول `role_work_rules` خوانده می‌شود.
- مقادیر موظفی، سقف اضافه‌کار، شروع شب‌کاری و پایان شب‌کاری از قانون سمت کاربر گرفته می‌شود.
- در صورت نبود قانون، مقدار پیش‌فرض اعمال می‌شود.

### 3) شب‌کاری قابل تنظیم
- محاسبه شب‌کاری دیگر ثابت و فقط ۲۲ تا ۶ نیست؛ از مقادیر `night_start` و `night_end` قانون سمت استفاده می‌کند.
- حالت عبور از نیمه‌شب در محاسبه شب‌کاری حفظ شد.

### 4) نسخه‌ها
- `SITE_VERSION` به 92 افزایش یافت.
- نسخه اپ به 1.0.4 افزایش یافت.
- `versionCode` به 10004 افزایش یافت.
- تنظیمات نسخه داخل `upgrade.php` نیز به‌روزرسانی شد.

## باقی‌مانده برای قسمت‌های بعدی

1. تکمیل رابط مدیریتی Rule Engine برای ویرایش قوانین از پنل.
2. تکمیل گزارش مدیریتی تبدیل مازاد حضور به اضافه‌کار با UI ساده‌تر.
3. تست عددی کامل گزارش ماهانه با سناریوهای شب‌کاری، جمعه‌کاری و تعطیل‌کاری.
4. همگام‌سازی آفلاین کامل حضور و گزارش با Conflict Resolution.
5. بهینه‌سازی نهایی GPS و ثبت علت دقیق رد حضور در اپ.

---

## منبع: `CHANGELOG_PHASE7_PART20_JSON_PHP_INTERPOLATION_FIX.md`

# Phase 7 Part 20 — JSON/PHP interpolation fix

- Fixed PHP variable interpolation in `php/lib/routes.php` inside `_taxi12_plate_variants()`.
- Replaced ambiguous strings such as `$aت$c-$r` with explicit forms such as `{$a}ت{$c}-{$r}`.
- Prevents PHP warnings (`Undefined variable $aت`) from being printed before API JSON responses.
- Verified PHP syntax for all PHP files in the package.

---

## منبع: `CHANGELOG_PHASE7_PART21_FINAL_MOBILE_STABILITY_FIX.md`

# Phase 7 Part 21

- Plate training defaults to pending; reviewed rows leave current list.
- Fixed React Native fontFamily object crash in DriverScreen and TempDriversScreen.
- GPS no longer blocks before login; security-exempt users bypass location permission/GPS guard.
- Added timeout/cleanup for offline map update banner.
- Shift handover displays a scannable QR image with text fallback.
- Added visible salary-slip PDF upload entry in web panel.

---

## منبع: `CHANGELOG_PHASE7_PART22_MESSENGER_BULK_AND_ACCESS.md`

# Part 22
- ارسال گروهی در بله، تلگرام و ایتا از سایت و اپ
- فهرست مشترکین و دعوت پیامکی شماره‌های متصل‌نشده
- ارسال فیش آبونمان، تذکر و چک‌لیست در ربات‌ها
- آیتم BotMessages در کنترل نمایش اپ و دسترسی سمت‌ها
- حفظ و اعمال معافیت امنیتی از اطلاعات واقعی کاربر

---

## منبع: `CHANGELOG_PHASE7_PART23_SALARY_SLIPS_SIDEBAR_TOKEN_FIX.md`

# Phase 7 Part 23 — Salary Slips Sidebar & Token Fix

- Added a dedicated salary-slip management module to the web panel sidebar.
- Removed the floating salary-slip upload button from panel.html.
- Added professional user search, user selection, Jalali period selection, drag-and-drop PDF upload, file validation, progress state, and registered-slip management.
- Added the `salaryslips` permission key to role-based web panel visibility.
- Fixed `/api/public/salary-slip/{id}/{token}` to be truly public while retaining HMAC signed-link validation.
- Fixed Android PDF viewing that previously returned expired or invalid token errors.
- Rebuilt panel bundle as v113.

---

## منبع: `CHANGELOG_PHASE7_PART24_FORMS_DATES_NAN_FIX.md`

# Part 24 — Forms driver data and Jalali date fixes

- ذخیره پاسخ فرم‌ها با کلید فیلد به‌جای عنوان نمایشی
- سازگاری کامل با پاسخ‌های قدیمی که با عنوان فیلد ذخیره شده‌اند
- بازیابی خودکار driver_id از کد ملی هنگام ثبت فرم
- نمایش اطلاعات راننده در لیست پاسخ‌ها و خروجی CSV
- ترمیم تاریخ‌های NaN/undefined رفاهیات و فعالیت‌های فرهنگی با تاریخ واقعی ثبت
- جلوگیری از نمایش «ناعدد» در کارت تولد و تبدیل امن اعداد

---

## منبع: `CHANGELOG_PHASE7_PART25_SIDEBAR_IMAGE_SLIP_SAVE.md`

# Part 25 — Sidebar modules, image salary slips and mobile save

- حذف لینک‌ها و دکمه‌های شناور «مرکز ارسال ربات‌ها» از پایین پنل.
- افزودن «مرکز ارسال ربات‌ها» به‌عنوان نمای واقعی سایدبار در گروه پیام و سازمان.
- تثبیت «بارگذاری فیش حقوقی» به‌عنوان ماژول واقعی سایدبار منابع انسانی.
- افزودن هر دو مجوز به تنظیمات دسترسی سمت‌های سایت.
- پشتیبانی بارگذاری PDF، JPG، JPEG و PNG تا ۱۰ مگابایت.
- تشخیص امن نوع فایل بر اساس امضای فایل و MIME و ذخیره پسوند صحیح.
- افزودن mime_type به رکوردهای فیش حقوقی با ارتقای idempotent.
- نمایش تصویر فیش در اپ و امکان مشاهده PDF/تصویر.
- افزودن «ذخیره در برنامه» و اشتراک‌گذاری فایل ذخیره‌شده توسط کاربر.
- ارتقای پنل به build v114.

---

## منبع: `CHANGELOG_PHASE7_PART26_SAMEORIGIN_EMBED_FIX.md`

# Part 26 — Same-origin embedded module fix

- Fixed browser refusal when loading `messenger_center.html` inside the admin panel.
- Replaced `X-Frame-Options: DENY` with `SAMEORIGIN`.
- Added `Content-Security-Policy: frame-ancestors 'self'`.
- Embedding remains restricted to the same origin only.

---

## منبع: `CHANGELOG_PHASE7_PART27_NATIVE_MESSENGER_CENTER.md`

# Part 27 — Native Messenger Center

- Removed iframe-based messenger center from the admin panel.
- Added a native React panel page for group bot messaging and SMS invitations.
- Added separate subscriber tabs and statistics for Bale, Telegram, and Eitaa.
- Added subscriber search, active/inactive state, linked user/driver, chat id, connection and last-seen dates.
- Rebuilt panel bundle as v115.

---

## منبع: `CHANGELOG_PHASE7_PART28_FUNCTIONAL_BOT_SETTINGS.md`

# Part 28 — Functional bot settings

- Added platform ownership to bot menus, custom replies, and forms.
- Separated Bale, Telegram, and Eitaa records.
- Fixed CRUD APIs to validate and filter by platform.
- Fixed bot runtime to load only its own menus, forms, and replies.
- Added visible loading, success, retry, and API error states in panel.
- Kept form submissions and logs platform-specific.
- Rebuilt panel bundle v116.

---

## منبع: `CHANGELOG_PHASE7_PART29_ROLE_APP_ITEMS_TEMP_BOT.md`

# Part 29 — تنظیم نمایش رانندگان موقت و پیام ربات‌ها برای هر سمت

- افزودن کلید `TempDrivers` به صفحه «آیتم‌های اپ هر سمت».
- افزودن کلید `BotMessages` به صفحه «آیتم‌های اپ هر سمت».
- تطبیق کلیدها با route واقعی داشبورد اپ اندروید.
- امکان فعال/غیرفعال‌سازی مستقل دو آیتم برای هر سمت.
- بازسازی پنل با نسخه v117.

---

## منبع: `CHANGELOG_PHASE7_PART2_OVERNIGHT_SURPLUS.md`

# CHANGELOG — Phase 7 Part 2

Version: 1.0.5  
Site Version: 93  
Android versionCode: 10005

## انجام‌شده در این قسمت

### حضور و شیفت خودکار
- اصلاح محاسبه ترددهای عبوری از نیمه‌شب در گزارش روزانه و ماهانه.
- رکوردهایی مثل ورود ۲۳:۰۰ و خروج ۰۷:۰۰ اکنون با هم‌پوشانی روزانه محاسبه می‌شوند؛ یعنی سهم بعد از ساعت ۰۰:۰۰ در روز بعد هم دیده می‌شود.
- محاسبه شب‌کاری همچنان بر اساس بازه قابل تنظیم Rule Engine انجام می‌شود.
- API محاسبه قوانین اکنون جمعه‌کاری و تعطیل‌کاری را دقیق‌تر تشخیص می‌دهد.

### گزارش تردد پرسنل
- ستون‌های «حضور کل»، «جمعه/تعطیل»، «مازاد حضور» و «تبدیل‌شده» به جدول گزارش تردد اضافه شد.
- امکان تبدیل مازاد حضور به اضافه‌کار از داخل گزارش تردد اضافه شد.
- امکان لغو تبدیل مازاد حضور به اضافه‌کار اضافه شد.

### API مدیریتی
- افزودن `POST /api/admin/attendance-surplus/reset`
- افزودن `GET /api/admin/attendance-surplus/list`
- بهبود `POST /api/admin/attendance-surplus/convert`

### دیتابیس
- تمام تغییرات دیتابیس همچنان در `php/public/upgrade.php` تجمیع شده است.
- جدول `attendance_ot_adjustments` در upgrade.php لحاظ شد.
- ایندکس کمکی `idx_staff_att_user_in_out` برای گزارش‌های تردد اضافه شد.

## باقی‌مانده برای قسمت‌های بعدی
- تست میدانی GPS، آلارم، Push و QR روی گوشی واقعی.
- Build نهایی پنل بدون Babel داخل مرورگر.
- تست بارگذاری همزمان کاربران روی سرور نهایی.
- گزارش‌ساز سفارشی پیشرفته و زمان‌بندی ارسال گزارش‌ها.

---

## منبع: `CHANGELOG_PHASE7_PART30_OFFLINE_QUEUE_BANNER_FIX.md`

# Phase 7 Part 30 — Offline Queue and Persistent Banner Fix

## اصلاحات
- جلوگیری از بازنویسی `client_uuid` تولیدشده با مقدار `undefined` هنگام افزودن عملیات به صف آفلاین.
- ترمیم خودکار رکوردهای قدیمی صف که فاقد `client_uuid` هستند.
- حذف رکوردهای تأییدشده بر اساس تعداد واقعی حذف از AsyncStorage.
- جلوگیری از اجرای هم‌زمان چند عملیات flush و ارسال تکراری یک صف.
- نمایش تعداد واقعی موارد حذف‌شده در پیام بروزرسانی دستی.
- حذف نوار زرد دائمی در حالت آنلاین؛ نوار فقط هنگام اجرای واقعی همگام‌سازی نمایش داده می‌شود.
- نمایش نوار آفلاین فقط هنگام قطع اینترنت، همراه با تعداد موارد باقی‌مانده در صف.
- پاک‌سازی صف خراب یا JSON نامعتبر به‌جای گیرکردن دائمی اپ.

## فایل‌های تغییرکرده
- `mobile/src/offline.js`
- `mobile/src/OfflineBanner.js`

---

## منبع: `CHANGELOG_PHASE7_PART31_1_PLATE_CLEANUP_BOTS_PAYSLIP.md`

# Phase 7 Part 31.1 — حذف کامل پلاک‌خوان Python، تکمیل ربات‌ها، حذف خودکار فیش حقوقی

## ۱) حذف کامل بخش Python از پلاک‌خوان
- کد Python (`[قابلیت منسوخ و حذف‌شده]`, `[قابلیت منسوخ و حذف‌شده]`) حذف شد.
- مسیرهای `[قابلیت منسوخ و حذف‌شده]`, `/export-manifest`, `[قابلیت منسوخ و حذف‌شده]` و توابع
  کمکی مرتبط (`_plate_python_bin`, `_plate_active_model`, ...) از `routes.php` حذف شدند.
- جدول `plate_model_runs` هنگام اجرای بعدی `upgrade.php` یا Import کردن
  `upgrade_import_mysql.sql` به‌طور خودکار حذف می‌شود (چون توسط فیکس قبلی روی
  سرور شما ساخته شده بود).
- **پنل مدیریت واقعاً rebuild شد** (نه فقط تغییر ظاهری): دکمه‌های «آموزش
  مدل» و «خروجی Manifest» به‌طور کامل از `panel.bundle.js` حذف شدند. بخش
  «بازبینی پلاک‌خوان» (بدون نیاز به Python) دست‌نخورده باقی ماند.
- تنظیمات «پلاک‌خوان» در صفحهٔ تنظیمات هم پاک‌سازی شد (حذف گزینه‌های مربوط
  به «مدل سرور» که دیگر وجود ندارد).

## ۲) بررسی و تکمیل ربات‌های بله/تلگرام/ایتا
بعد از بررسی کامل کد مشخص شد **منو، پاسخ خودکار، و فرم‌های ثبت‌نام با
تکمیل خودکار از دیتابیس راننده از قبل به‌صورت کامل و حرفه‌ای پیاده‌سازی
شده بودند** (هم بک‌اند در `Bale.php`/`MessengerBots.php`، هم پنل مدیریت
کامل CRUD در تب «ربات‌ها» → منو/پاسخ سفارشی/فرم‌ها/ثبت‌نام‌ها/لاگ، برای هر
سه پلتفرم). تنها بخش واقعاً ناقص، **ثبت خودکار Webhook** بود که اضافه شد:
- مسیر جدید `POST /api/admin/messengers/{platform}/register-webhook`:
  با استفاده از توکن ذخیره‌شده، مستقیماً `setWebhook` را نزد سرویس بله/
  تلگرام/ایتا صدا می‌زند (دیگر نیازی به curl دستی نیست).
- دکمهٔ «ثبت خودکار Webhook» کنار آدرس Webhook در تب تنظیمات هر ربات اضافه شد.
- اگر کلید محرمانهٔ Webhook تنظیم نشده باشد، به‌صورت خودکار یک مقدار امن
  تصادفی ساخته و ذخیره می‌شود.

**یادآوری:** برای فعال‌سازی هر ربات، همچنان باید توکن واقعی را از
BotFather (تلگرام)، پنل بله بیزنس، یا ایتایار (ایتا) بگیرید و در تب
«تنظیمات» وارد کنید؛ این مرحله جایگزین‌ناپذیر است و کد نمی‌تواند آن را
خودکار انجام دهد.

## ۳) حذف خودکار فیش‌های حقوقی بعد از مدت تعیین‌شده
- حذف تکی دستی از قبل وجود داشت (دکمهٔ «حذف» کنار هر فیش).
- تابع پاک‌سازی خودکار موجود سامانه (`_run_retention_cleanup`) که قبلاً
  گزارش‌ها/پیام‌ها/تصاویر فرم‌ها/تصاویر صحت‌سنجی حضور را پوشش می‌داد،
  اکنون **فیش‌های حقوقی را هم شامل می‌شود** — هم فایل فیزیکی و هم ردیف
  دیتابیس کامل حذف می‌شوند (نه فقط خالی‌کردن یک ستون).
- فیلد جدید «نگهداری فیش‌های حقوقی (روز)» در تنظیمات → «حذف خودکار تصاویر
  و پیوست‌ها» اضافه شد (۰ = بدون حذف خودکار).
- از همان مسیرهای موجود قابل اجراست: دکمهٔ دستی مدیر، یا Cron روزانه به
  آدرس `/api/cron/cleanup?key=...`.

## فایل‌های تغییرکرده
- `php/lib/routes.php`, `php/lib/Http.php`, `php/public/index.php`
- `php/public/upgrade.php`, `php/db/upgrade_import_mysql.sql`
- `php/public/cron_push_expiry.php`, `php/public/upgrade.php` (از دور قبل)
- `php/tools/panel_source.jsx` → rebuild → `php/public/assets/panel.bundle.js`
- `php/tools/build_panel_bundle.sh` (رفع ناسازگاری با نسخهٔ جدید tsc)
- `php/public/panel.html` (ارتقای نسخهٔ cache-busting به v120)
- `mobile/src/offline.js`, `mobile/src/OfflineBanner.js` (از دور قبل)

## اجرای لازم روی سرور
1. فایل zip را جایگزین کنید.
2. `php/public/upgrade.php` را با `ALLOW_MAINTENANCE=1` یک‌بار اجرا کنید
   (برای پاک‌سازی جدول `plate_model_runs` و اطمینان از ساختار جدول‌ها).
3. اگر می‌خواهید حذف خودکار فیش حقوقی فعال باشد، مقدار «نگهداری فیش‌های
   حقوقی (روز)» را در تنظیمات وارد کنید.

---

## منبع: `CHANGELOG_PHASE7_PART31_SECURITY_HARDENING.md`

# Phase 7 Part 31 — رفع اشکالات امنیتی و پایداری (بر اساس بازبینی کامل کد)

این نسخه نتیجهٔ یک بازبینی کامل امنیتی/ساختاری/طراحی روی نسخهٔ Part 30 است.
همهٔ موارد پیداشده اصلاح شدند.

## ۱) `/api/media` — دسترسی عمومی و بدون احراز هویت (امنیتی، جدی)
این مسیر که تصاویر فیزیکی (از جمله سلفی حضور و «سلفی نامحسوس») را سرو می‌کند،
به‌اشتباه با `public=true` ثبت شده بود — برخلاف کامنت بالای همان خط که می‌گفت
«نیازمند احراز هویت». یعنی هرکسی با دانستن/حدسِ مسیر فایل، بدون ورود به سامانه
می‌توانست تصویر را ببیند.

**اصلاح:**
- `route('GET','/api/media', ...)` اکنون مثل بقیهٔ مسیرها نیاز به احراز هویت دارد.
- برای اینکه این تغییر باعث نشکستن پیش‌نمایش تصاویر در پنل وب نشود (چون تگ
  `<img>` مرورگر نمی‌تواند هدر Authorization بفرستد)، یک مکانیزم مرکزی در
  `Http::json()` اضافه شد: هر رشتهٔ `/api/media?path=...` که در پاسخ JSON
  برگردانده می‌شود، خودکار با توکن معتبر همان درخواست تکمیل می‌شود
  (`&token=...`) — دقیقاً همان فالبک `?token=` که خود `Http::bearer()` از قبل
  برای این مسیر پشتیبانی می‌کرد اما هیچ‌جا استفاده نمی‌شد.
- اپ موبایل بدون تغییر کار می‌کند (از قبل هدر Authorization می‌فرستد).

فایل‌های تغییرکرده: `php/lib/Http.php`، `php/public/index.php`، `php/lib/routes.php`

## ۲) `cron_push_expiry.php` — بدون هیچ احراز هویتی (امنیتی)
برخلاف بقیهٔ اسکریپت‌های cron (`cron_daily.php`, `cron_birthday.php`, ...) که
همه از `cron.php` با چک `cron_key` عبور می‌کنند، این فایل پیاده‌سازی مستقل و
کاملاً بدون کلید داشت. هرکسی می‌توانست بدون هیچ محدودیتی آن را از مرورگر صدا
بزند و برای همهٔ کاربران دارای خط، Push هشدار انقضا ارسال کند (ریسک اسپم).

**اصلاح:** تبدیل به یک delegator محافظت‌شده به `cron.php` (دقیقاً مثل بقیهٔ
اسکریپت‌های هم‌خانواده‌اش)؛ دستور اجرای Cron شما بدون تغییر کار می‌کند.

فایل تغییرکرده: `php/public/cron_push_expiry.php`

## ۳) `upgrade.php` — دور زدن کامل احراز هویت وقتی کلید تنظیم نشده (امنیتی، جدی)
منطق قبلی فقط وقتی کلید را چک می‌کرد که یک `upgrade_key` سفارشی در `config.php`
یا `.env` تنظیم شده باشد. اگر تنظیم نشده بود (مقدار پیش‌فرض
`change-this-upgrade-key` که در سرور شما فعال بود)، **کل چک رد می‌شد و مسیر
برای همه، بدون هیچ کلیدی، در دسترس بود.**

**اصلاح:** افزودن همان محافظ قفل `.installed` + `ALLOW_MAINTENANCE` که در
`fix_attendance_2026.php` / `migrate_images.php` / `setup-libs.php` استفاده
می‌شود. از این پس برای اجرای `upgrade.php` روی سایت نصب‌شده، باید موقتاً
`ALLOW_MAINTENANCE=1` را در `.env` تنظیم کنید، اجرا را انجام دهید، سپس بردارید.
چک کلید سفارشی هم به‌عنوان لایهٔ دوم محافظتی باقی مانده است.

فایل تغییرکرده: `php/public/upgrade.php`

## ۴) نوار آفلاین ممکن بود در برخی خطاهای شبکه گیر بماند (پایداری)
در `offline.js` (تابع `flush`) و `OfflineBanner.js` (تابع `tick`)، فراخوانی
`Network.getNetworkStateAsync()` بدون `try/catch` بود. اگر این تابع در برخی
شرایط (رایج در برخی نسخه‌های Android/expo-network) خطا می‌داد، کل چرخهٔ
به‌روزرسانی وضعیت بی‌صدا متوقف می‌شد و نوار می‌توانست در همان وضعیت قبلی
(مثلاً «در حال همگام‌سازی…») برای همیشه گیر بماند — دقیقاً همان مشکلی که
Part 30 قرار بود برطرف کند.

**اصلاح:** افزودن `try/catch` مناسب در هر دو نقطه؛ در صورت خطا در تشخیص وضعیت
شبکه، فرض امن (آنلاین) در نظر گرفته می‌شود و چرخهٔ بعدی (هر ۸ ثانیه) دوباره
تلاش می‌کند، بدون گیرکردن دائمی نوار.

فایل‌های تغییرکرده: `mobile/src/offline.js`، `mobile/src/OfflineBanner.js`

---

## یادداشت دربارهٔ مورد بررسی‌شده و بدون نیاز به تغییر
هدر `Cross-Origin-Resource-Policy: same-origin` در `index.php` بررسی شد؛ چون
طبق تأیید شما کل برنامه فقط روی `app.yousefipour.ir` سرو می‌شود (بدون
ساب‌دامین جداگانه برای پنل)، این مورد مشکلی ایجاد نمی‌کند و نیازی به تغییر
ندارد.

---

## منبع: `CHANGELOG_PHASE7_PART32_SQL_REACT_WEBHOOK_CLOUD_OCR.md`

# Phase 7 Part 32

- Fixed dangling SQL procedure calls after DROP PROCEDURE.
- Fixed async React useEffect cleanup crash (`c is not a function`).
- Improved webhook URL auto-detection and explicit provider errors.
- Added PHP/cURL cloud OCR service without shell_exec.
- Added Google Cloud Vision and generic JSON provider settings.

---

## منبع: `CHANGELOG_PHASE7_PART33_OCR_MULTIPASS_CLOUD_FALLBACK.md`

# Part 33 — OCR چندمرحله‌ای پلاک و کد ملی

- افزودن OCR چندبرشی برای جبران اختلاف نسبت تصویر CameraView و عکس دوربین
- بزرگ‌نمایی ناحیه پلاک و شماره ملی پیش از OCR محلی
- اجرای OCR محلی روی چند crop و انتخاب نخستین نتیجه معتبر
- fallback اختیاری به OCR ابری PHP/cURL فقط در صورت شکست OCR محلی
- بهبود استخراج کد ملی از ارقام شکسته، فاصله‌دار و چندتوکنی
- جلوگیری از چسباندن اشتباه تاریخ تولد و شماره سریال به کد ملی
- حذف placeholder گمراه‌کننده 72/575 و جایگزینی با «۲ رقم» و «۳ رقم»
- افزودن قوانین R8/Proguard برای جلوگیری از حذف کلاس‌های [قابلیت منسوخ و حذف‌شده] در Release
- افزایش نسخه اپ به 1.2.1 و versionCode به 10201

---

## منبع: `CHANGELOG_PHASE7_PART34_MYKET_FIXES_OCR_ALTERNATIVES.md`

# Phase 7 Part 34 — رفع ایرادات نسخهٔ کاندید انتشار مایکت + جایگزین‌های Google Vision

## ایرادات پیداشده و رفع‌شده

### ۱) نسخهٔ خروجی APK با نسخهٔ واقعی اپ هم‌خوانی نداشت (اسکریپت انتشار)
- `mobile/build-myket-release.ps1` نام فایل خروجی را به‌صورت ثابت
  `taxi-lines-management-1.2.0-myket.apk` می‌نوشت، درحالی‌که اپ الان نسخهٔ
  ۱.۲.۱ است. اصلاح شد تا نام فایل از مقدار واقعی `ANDROID_VERSION_NAME` در
  `.env` ساخته شود (`...-1.2.1-myket.apk`) و بیلدهای بعدی هم خودکار درست
  نام‌گذاری شوند.
- `mobile/.env.example` هم مقادیر قدیمی (`ANDROID_VERSION_CODE=10200`,
  `ANDROID_VERSION_NAME=1.2.0`) داشت — به ۱۰۲۰۱/۱.۲.۱ به‌روزرسانی شد. این
  مهم بود چون اگر کسی طبق راهنما `.env` را از این فایل می‌ساخت، عملاً یک
  نسخهٔ قدیمی‌تر می‌ساخت که برای آپلود در مایکت روی نسخهٔ موجود، رد می‌شد.
- `release/myket/MYKET_RELEASE_CHECKLIST.md` و `BUILD_AND_PUBLISH.md` هم به
  همین نسخه به‌روزرسانی شدند.

### ۲) افزونهٔ امضای انتشار (`withReleaseSigning`) شکنندگی داشت
الگوی متنی‌ای که برای پیدا کردن بلاک `release {` در `build.gradle` استفاده
می‌شد، به فاصله/CRLF حساس بود و در صورت عدم تطابق (مثلاً به‌خاطر تفاوت جزئی
در قالب تولیدی نسخه‌ای از Expo)، **بدون هیچ خطایی** امضای Myket اعمال
نمی‌شد و خروجی نهایی با کلید Debug امضا می‌شد — که برای انتشار در فروشگاه
قابل قبول نیست و ممکن بود این مشکل تا لحظهٔ رد شدن توسط مایکت متوجه نشود.
اصلاح شد: الگو نسبت به فاصله/CRLF مقاوم شد و اگر ساختار مورد انتظار پیدا
نشود، خطای صریح در همان مرحلهٔ prebuild پرتاب می‌شود.

### ۳) تنظیمات «OCR ابری» دوباره از پنل ادمین حذف شده بود (رگرسیون)
در نسخهٔ آپلودشده، بک‌اند (`CloudOcr.php`) و اتصال اپ موبایل به OCR ابری
(fallback) کامل بودند، اما بخش تنظیمات آن در پنل مدیریت (فعال‌سازی/انتخاب
سرویس/کلید API) دوباره وجود نداشت — احتمالاً چون این نسخه بر پایهٔ یک
نسخهٔ قدیمی‌تر از فایل تنظیمات ساخته شده. دوباره اضافه شد؛ این‌بار همراه با
هشدار دربارهٔ Google Vision (پایین را ببینید).

## پاسخ به سؤال دوم: جایگزین Google Cloud Vision

**بله، و برای این پروژه هم توصیه می‌شود.** طبق بررسی جدید، زیرساخت Google
Cloud (که Vision API هم رویش اجرا می‌شود) بر خلاف AWS/Azure، دسترسی از IP
ایران را در سطح شبکه مسدود می‌کند و ثبت‌نام/صورتحساب حساب‌های ایرانی هم به
دلیل تحریم عملاً ممکن نیست. یعنی حتی با کلید API معتبر، درخواست‌ها از سرور
شما (که ظاهراً در ایران است) به‌احتمال زیاد اصلاً به مقصد نمی‌رسند.

کد از قبل یک گزینهٔ «سرویس عمومی (JSON دلخواه)» دارد که با هر API ای که
JSON با فیلد `text` برگرداند کار می‌کند؛ این را الان **پیش‌فرض** کردم
(به‌جای Google Vision) و در تنظیمات پنل، سه مسیر جایگزین توضیح داده شده:

۱. **سرویس‌های OCR فارسی داخلی** (چند نمونه برای بررسی: api.ir، iranocr.ir،
   farsiocr.ir) — قبل از اتصال، مستندات API خودشان را برای فرمت دقیق
   درخواست/پاسخ بررسی کنید؛ ممکن است نیاز به یک تبدیل کوچک فرمت پاسخ به
   `{"text": "..."}` داشته باشید (چون دقیقاً مستندات زندهٔ آن‌ها را
   نداشتم که در کد سخت‌کد کنم).
۲. **Tesseract OCR خودتان روی یک VPS کوچک** با یک API ساده (Flask/FastAPI)
   — کد Python که قبلاً برای آموزش/تشخیص پلاک در پروژه بود، مبنای خوبی
   برای این سرویس است؛ چون خودتان میزبانی می‌کنید، مشکل تحریم و شکل پاسخ
   کاملاً در کنترل شماست.
۳. اگر به یک سرویس بین‌المللی شناخته‌شده نیاز دارید، **Azure/AWS** برخلاف
   Google در سطح شبکه IP ایران را مسدود نمی‌کنند، اما همچنان به یک روش
   پرداخت بین‌المللی معتبر نیاز دارید.

## فایل‌های تغییرکرده
- `mobile/build-myket-release.ps1`, `mobile/.env.example`
- `mobile/plugins/withReleaseSigning.js`
- `php/lib/CloudOcr.php` (تغییر provider پیش‌فرض)
- `php/tools/panel_source.jsx` → rebuild → `php/public/assets/panel.bundle.js`
- `php/public/panel.html` (نسخهٔ cache-busting به v123)
- `release/myket/MYKET_RELEASE_CHECKLIST.md`, `BUILD_AND_PUBLISH.md`

---

## منبع: `CHANGELOG_PHASE7_PART35_BOT_MIRROR_FIELD_ALERTS.md`

# Phase 7 Part 35 — آینه‌سازی پیام‌ها در ربات + هشدارهای میدانی جدید (VPN/GPS)

## خلاصهٔ یافته‌های مهم قبل از توضیح تغییرات
با بررسی کد مشخص شد **بخش بزرگی از خواستهٔ شما از قبل به‌صورت خودکار کار
می‌کند** و نیازی به تغییر نداشت:

- تابع مرکزی ارسال Push (`Push::send()`/`Push::notify()`) از قبل **هر پیامی
  که از این طریق ارسال شود را خودکار به ربات‌های متصل کاربر (بله/تلگرام/
  ایتا) هم آینه (mirror) می‌کند** — به شرط اینکه آن دسته‌بندی پیام
  (پیام‌ها/تولد/حضور و غیاب/...) در تنظیمات همان ربات فعال باشد.
- اعلان «گزارش جدید برای بررسی» و «پاسخ به گزارش» از قبل از همین مسیر
  ارسال می‌شوند → یعنی **پیام‌های گزارش از قبل در ربات هم می‌رسند** (اگر
  دسته «پیام‌ها» برای آن پلتفرم فعال باشد).
- اعلان «صحت‌سنجی حضور» (درخواست ارسال سلفی/عکس) هم از قبل از همین مسیر
  ارسال می‌شود → یعنی **این هم از قبل در ربات می‌رسد** (اگر دسته «حضور و
  غیاب» فعال باشد).

**چیزی که واقعاً ناقص بود:** اعلان «خروج از محدودهٔ خط» به‌جای این مسیر
مرکزی، مستقیم در جدول اعلان‌ها درج می‌شد و از Push/ربات کاملاً صرف‌نظر
می‌کرد؛ و هیچ اعلانی برای «روشن‌شدن VPN» و «خاموش‌شدن GPS» اصلاً وجود
نداشت (فقط یک نشانگر بصری روی نقشهٔ زنده بود؛ همان «هشدار در برنامه» که
اشاره کردید).

## تغییرات این نسخه

### ۱) اعلان خروج از محدودهٔ خط، حالا در ربات هم می‌رسد
`_notify_station_exit()` بازنویسی شد تا به‌جای درج مستقیم در دیتابیس، از
`Push::notify()` استفاده کند — یعنی حالا هم اعلان داخل‌برنامه‌ای، هم Push،
و هم پیام در ربات (طبق تنظیم «هشدارها») ارسال می‌شود.

### ۲) دو هشدار جدید: روشن‌شدن VPN و خاموش‌شدن GPS
- به محض روشن‌شدن فیلترشکن (تشخیص از گزارش اپ)، اعلان «روشن‌شدن فیلترشکن
  (VPN)» برای مسئول(ان) پیکربندی‌شده ارسال می‌شود.
- به محض خاموش‌شدن GPS روی گوشی کاربر، اعلان «خاموش‌شدن GPS» ارسال می‌شود.
- هر دو، مثل خروج از خط، هم داخل برنامه، هم Push، و هم در ربات (دستهٔ
  «هشدارها») ارسال می‌شوند.
- این دو رویداد فقط لحظهٔ *شروع* حالت (روشن‌شدن VPN / خاموش‌شدن GPS)
  اعلان می‌فرستند، نه هر بار که وضعیت گزارش می‌شود — تا اسپم نشود.

### ۳) امکان انتخاب «یک یا چند مسئول» برای هرکدام از این سه هشدار
تنظیمات قبلی «خروج از خط» فقط یک نفر را پشتیبانی می‌کرد. حالا هر سه هشدار
(خروج از خط / VPN / GPS) به‌طور مستقل قابل‌تنظیم‌اند و در حالت «ارسال برای
مشخص»، می‌توانید از بین سمت‌های مختلف، چند نفر را هم‌زمان به‌عنوان مسئول
دریافت انتخاب کنید (کامپوننت جدید انتخاب چندگانه در پنل).

این بخش را در پنل مدیریت → تنظیمات → «هشدارهای میدانی» پیدا می‌کنید.

## نکتهٔ مهم برای فعال‌سازی واقعی
مثل قبل، برای اینکه پیام‌ها واقعاً در بله/تلگرام/ایتا برسند:
۱) آن شخص باید قبلاً با ارسال شماره همراهش به ربات، حساب خود را متصل
   (bind) کرده باشد،
۲) در تنظیمات همان ربات، دستهٔ «هشدارها» (برای این سه مورد) و «پیام‌ها»/
   «حضور و غیاب» (برای گزارش/حضور) فعال باشد.

## فایل‌های تغییرکرده
- `php/lib/routes.php` (تعمیم توابع اعلان + قلاب‌های VPN/GPS)
- `php/lib/Push.php` (نگاشت نوع پیام‌های جدید به دستهٔ «هشدارها»)
- `php/tools/panel_source.jsx` → rebuild → `php/public/assets/panel.bundle.js`
- `php/public/panel.html` (نسخهٔ cache-busting به v124)

---

## منبع: `CHANGELOG_PHASE7_PART36_FIELD_ALERTS_ITEM.md`

# Phase 7 Part 36 — رفع کامل مسیرهای بایپس‌شدهٔ ربات + آیتم «هشدارها»

## علت واقعی «چرا پیام در ربات نمی‌آید»
با بررسی دقیق‌تر، **سه محل دیگر در کد** پیدا شد که هنوز مستقیم در جدول
`notifications` می‌نوشتند و کامل از تابع مرکزی `Push::notify()` (که مسئول
ارسال Push و آینه‌سازی خودکار به ربات‌هاست) عبور می‌کردند — دقیقاً همان
الگوی باگی که در نسخهٔ قبل برای «خروج از خط» پیدا و رفع کردم، اما این‌بار
جای دیگری در کد:

1. **ثبت حضور/بازدید مسئولین** (`OfficialPresenceScreen` → مسیر
   `official_visits`) — این همان چیزی بود که در پیام شما «ثبت حضور
   مسئولین» نامیده شد. رفع شد.
2. اعلان «معرفی به‌عنوان جایگزین» (substitute) — رفع شد.
3. اعلان «شیفت جایگزینی» بعد از تأیید مرخصی — رفع شد.

با این سه مورد، **دیگر هیچ نقطه‌ای در کد باقی نمانده** که پیام را از مسیر
مرکزی (و در نتیجه از آینه‌سازی به ربات) دور بزند.

## آیتم جدید «هشدارها» در اپ
- یک آیکون کوچک ⚠️ کنار آیکون 💬 (پیام‌ها) در هدر اپ اضافه شد.
- **فقط برای کاربرانی که واقعاً به‌عنوان مسئول این هشدارها پیکربندی
  شده‌اند** نمایش داده می‌شود (یعنی اگر حداقل یک هشدار خروج از خط/VPN/GPS
  دریافت کرده باشند) — دقیقاً همان چیزی که خواستید: «تنها برای کاربرانی که
  دسترسی گرفتن این اعلان‌ها را دارند».
- با ضربه روی آن، صفحهٔ جدید «هشدارها» باز می‌شود: فهرست جداگانه از
  هشدارهای خروج از خط/VPN/GPS (جدا از صفحهٔ عمومی اعلان‌ها)، با شمارندهٔ
  خوانده‌نشده مستقل.
- endpoint های جدید بک‌اند: `GET /api/my/field-alerts` و
  `POST /api/my/field-alerts/read`.

## یادآوری تنظیمات لازم (بدون تغییر نسبت به قبل)
برای اینکه پیام واقعاً در بله/تلگرام/ایتا برسد:
۱) شخص مسئول باید قبلاً شمارهٔ همراهش را به ربات فرستاده و حساب را وصل
   (bind) کرده باشد.
۲) در تنظیمات → «هشدارهای میدانی»، هرکدام از سه مورد را فعال و مسئول(ان)
   را انتخاب کنید.
۳) در تنظیمات ربات مربوطه (بله/تلگرام/ایتا)، تیک «تذکرها و اخطارها» فعال
   باشد (این دستهٔ همان چیزی است که این سه هشدار در آن قرار می‌گیرند).

## فایل‌های تغییرکرده
- `php/lib/routes.php` (رفع سه بایپس + endpoint های جدید + شمارش هشدارها در dashboard)
- `mobile/App.js` (ثبت صفحهٔ جدید)
- `mobile/src/screens/FieldAlertsScreen.js` (جدید)
- `mobile/src/screens/DashboardScreen.js` (آیکون جدید در هدر)

---

## منبع: `CHANGELOG_PHASE7_PART37_REPORT_FLOW_ACTIVITY_BIRTHDATE.md`

# Part 37
- نمایش گزارش‌های درگیرشده (ارسالی، دریافتی و ارجاع‌شده) برای پیگیری در پنل
- تب «ارجاع‌شده توسط من» در اپ
- امکان انتخاب مقام بالادست یا شخص خاص هنگام ارسال گزارش در اپ
- اصلاح تبدیل تاریخ شمسی در گزارش فعالیت همه کاربران و خروجی ریز
- افزودن تاریخ تولد به خروجی کامل کاربران

---

## منبع: `CHANGELOG_PHASE7_PART38_FIELD_ALERTS_BALE_DELIVERY_FIX.md`

# Phase 7 Part 38 — Field alerts to Bale delivery fix

## Fixed
- Field alerts are now mirrored to messenger bots even when the configured recipient has no Expo push token.
- Bale delivery to configured users first resolves an active subscriber by `user_id`, then falls back to normalized phone number for legacy subscriptions.
- Selected users without a phone number can still receive Bale alerts when their Bale subscription is linked to their user account.
- Messenger delivery failures are written to the PHP error log instead of being silently swallowed at the Push bridge.

## Affected alerts
- Station/line geofence exit
- VPN enabled
- GPS disabled

## Files
- `php/lib/Push.php`
- `php/lib/Bale.php`

---

## منبع: `CHANGELOG_PHASE7_PART39_ADVANCED_FIELD_ALERTS.md`

# Phase 7 Part 39 — Advanced field alerts

- جلوگیری از ارسال اعلان‌های انقضای مدارک به ربات شخصی کاربر
- افزودن تاریخ و ساعت شمسی به هشدارهای خروج/ورود محدوده، VPN و GPS
- افزودن هشدار ورود به محدوده خط
- افزودن هشدار ثبت ورود و ثبت خروج از «حضور من»
- افزودن فیلتر کلی افراد تحت پایش: همه، سمت‌های خاص، افراد خاص
- افزودن دامنه اختصاصی تحت نظارت برای هر مسئول دریافت‌کننده
- پشتیبانی از تنظیمات قدیمی user_id/user_ids
- نمایش انواع جدید هشدار در اپ و شمارنده داشبورد
- اصلاح مجدد ترتیب Procedure در upgrade.sql

---

## منبع: `CHANGELOG_PHASE7_PART3_RULES_DIAGNOSTICS.md`

# فاز ۷ - قسمت سوم - نسخه 1.0.6

## تغییرات انجام‌شده
- افزایش نسخه سایت به SITE_VERSION = 94
- افزایش نسخه اپ اندروید به 1.0.6
- افزایش versionCode به 10006
- تکمیل تنظیمات Rule Engine برای شیفت خودکار بر اساس سمت
- افزودن کنترل فعال/غیرفعال بودن شیفت خودکار برای هر سمت
- افزودن کنترل بازه مجاز ورود در شیفت خودکار
- افزودن تنظیم ساعات شب‌کاری، جمعه‌کاری و تعطیل‌کاری برای هر سمت
- افزودن تنظیم هشدار قبل از سقف اضافه‌کار
- افزودن لاگ مدیریتی رد شدن ثبت حضور
- افزودن نمایش علت رد حضور، خط، روش ثبت، مختصات و دقت GPS در پنل
- حفظ تجمیع بروزرسانی دیتابیس در php/public/upgrade.php

## مسیرهای جدید API
- GET /api/admin/attendance-reject-logs
- GET /api/admin/phase7-part3/status
- GET /api/project-version-v7p3

## فایل ارتقا
- php/public/upgrade.php

---

## منبع: `CHANGELOG_PHASE7_PART40_ATTENDANCE_STATION_VPN_IP_IRAN_BUILD.md`

# Part 40 — هشدار حضور، ایستگاه تخصیص‌یافته، جزئیات خط و VPN

- ارسال هشدار ثبت ورود/خروج «حضور من» برای درخواست‌های آنلاین و همگام‌سازی آفلاین.
- ثبت خطاهای ارسال هشدار حضور در error log به‌جای سکوت کامل.
- محاسبه ورود و خروج ایستگاه فقط روی خطوط تخصیص‌یافته به کاربر.
- حذف محدوده‌های عمومی line_id=NULL از محاسبه حضور و پایش کاربران دارای خطوط مشخص.
- بی‌اعتبارکردن state قدیمی ایستگاهی که دیگر به خطوط کاربر تعلق ندارد.
- درج شماره خط، مبدأ و مقصد و نام ایستگاه در پیام‌های ورود/خروج محدوده و حضور من.
- درج IP، کشور فعلی و کشور قبلی در هشدار روشن‌شدن VPN.
- استفاده اجباری از myket.init.gradle در Build Release و کپی آن در Gradle init.d.
- استفاده از npm ci در صورت وجود package-lock برای بیلد قابل تکرار.

---

## منبع: `CHANGELOG_PHASE7_PART41_LIVE_STAFF_DASHBOARD_SUBSTITUTE_SHIFT.md`

# Part 41 — داشبورد لحظه‌ای پرسنل و شیفت خودکار جایگزین

- افزودن آمار کل پرسنل فعال، حاضر در شیفت، غایب و مرخصی/ماموریت به داشبورد سایت.
- افزودن فهرست حاضرین امروز با ساعت ورود، محل ثبت، خط، مبدا و مقصد، روش ثبت و وضعیت باز/بسته بودن حضور.
- افزودن تب‌های غایبین و افراد دارای مرخصی/ماموریت تاییدشده.
- بروزرسانی خودکار داده‌های حضور هر ۶۰ ثانیه و دکمه بروزرسانی دستی.
- اعمال خودکار شیفت فرد مرخصی‌گیرنده برای نیروی جایگزین در تمام روزهای بازه تاییدشده.
- اولویت شیفت جایگزینی فقط در تاریخ‌های درخواست تاییدشده و بازگشت خودکار به شیفت اصلی بعد از پایان بازه.
- پشتیبانی از شیفت‌های صریح و شیفت‌های خودکار نقش‌محور فرد مرخصی‌گیرنده.
- نسخه پنل: v126.

---

## منبع: `CHANGELOG_PHASE7_PART42_KHATYAR_BRANDING_LOADING_REPORT_AUDIO.md`

# Phase 7 - Part 42

- تغییر نام اپ اندروید به «خطیار»
- جایگزینی آیکون سایت، PWA، Splash و اپ اندروید با تصویر ارسالی
- افزودن لودینگ تپشی مشترک مبتنی بر تصویر خطیار و جایگزینی ActivityIndicatorهای اپ
- استفاده از لودینگ برندشده در شروع برنامه و دریافت اطلاعات کاربر
- جلوگیری از ارسال Push «گزارش جدید» به فرستنده و پخش صدای دریافت هنگام ارسال گزارش
- حفظ صدای مستقل موفقیت ارسال گزارش در دستگاه فرستنده
- به‌روزرسانی عنوان و Manifest سایت

---

## منبع: `CHANGELOG_PHASE7_PART43_LIVE_STAFF_FATIME_FIX.md`

# Phase 7 — Part 43: Live Staff faTime Fix

- تعریف تابع ایمن `faTime` برای نمایش ساعت ثبت حضور در داشبورد لحظه‌ای پرسنل.
- پشتیبانی از ورودی‌های `YYYY-MM-DD HH:mm:ss`، ISO و زمان خالص.
- جلوگیری از کرش کامل React در صورت مقدار خالی یا تاریخ نامعتبر.
- افزایش نسخه پنل به v128.

---

## منبع: `CHANGELOG_PHASE7_PART44_EXPO57_IRAN_BUILD.md`

# Part 44 — Expo SDK 57 / React Native 0.86 / Myket Iran Build

- حذف android:debuggable ثابت برای رفع lintVitalRelease.
- ارتقا به Expo SDK 57، React Native 0.86 و React 19.2.3.
- هم‌ترازسازی خودکار Camera، Location، Notifications، FileSystem، Reanimated، AsyncStorage و WebView با expo install.
- ارتقای React Navigation پایدار و [قابلیت منسوخ و حذف‌شده] OCR.
- سازگاری FileSystem قدیمی با expo-file-system/legacy.
- اصلاح API دسترسی دوربین در PermissionGuard.
- اسکریپت prepare-iran-build.ps1 برای انتخاب مخزن npm بدون تحریم و مخزن Maven مایکت.
- ساخت هم‌زمان APK و AAB امضاشده.
- اجرای expo-doctor قبل از ساخت و توقف در صورت ناسازگاری.
- نیازمندی Node.js 22.13 یا جدیدتر مطابق Expo SDK 57.

---

## منبع: `CHANGELOG_PHASE7_PART45_RESILIENT_IRAN_BUILD_WATCHDOG.md`

# Phase 7 Part 45 — Resilient Iran Build Watchdog

- Added per-stage idle and hard timeouts.
- Added process-tree termination for frozen npm, npx, Expo and Gradle commands.
- Added retries and automatic npm registry fallback tests.
- Build continues after preparation failures to collect a complete diagnostic report.
- Critical failures are not hidden: final exit is successful only when a signed APK exists.
- Added TXT, JSON and HTML reports under `release/myket/reports`.
- Added automatic removal of hardcoded `android:debuggable` after Expo prebuild.
- Added non-interactive Expo prebuild and fixed NODE_ENV/CI environment handling.
- Added Gradle network timeout options and 120-minute hard build limit.

---

## منبع: `CHANGELOG_PHASE7_PART47_EXPO_PREBUILD_BUILD_SCRIPT_FIX.md`

# Phase 7 Part 47 - Expo Prebuild and resilient build script fixes

- Rewrote the PowerShell build helpers as ASCII-only PowerShell 5.1 scripts.
- Fixed the invalid variable interpolation in prepare-iran-build.ps1.
- Fixed report object creation and missing startedAt/endedAt/elapsedSeconds fields.
- Fixed generic list conversion that caused "Argument types do not match".
- Added a mandatory local Expo CLI check and disabled fallback to legacy global expo-cli.
- Added npm registry probing, timeout, retry, and dependency installation before prebuild.
- Added explicit module resolution checks for Expo Camera, Location, Notifications, FileSystem, Build Properties, AsyncStorage, [قابلیت منسوخ و حذف‌شده] OCR, Reanimated, and WebView.
- Made dependency preparation fail fast before Expo prebuild if required modules are unavailable.
- Added Node.js 22.13+ validation for Expo SDK 57.
- Kept Myket Gradle mirror installation and release build watchdog support.
- Kept automatic removal of hardcoded android:debuggable after prebuild.
- Build reports remain available as TXT, JSON, and HTML.

---

## منبع: `CHANGELOG_PHASE7_PART48_BUILD_ENGINE_REWRITE.md`

# Phase 7 Part 48 - Build engine rewrite

- Replaced Start-Process watchdog with a managed System.Diagnostics.Process runner.
- Uses cmd.exe explicitly for .cmd/.bat compatibility on Windows PowerShell 5.1.
- Detects process completion immediately and does not wait after npm exit code 0.
- Adds hard timeout, idle timeout, taskkill process-tree termination, retries, and structured logs.
- npm installation now tries each registry independently; a successful ping is not treated as proof that package installation will work.
- Registry fallback order: Runflare, npmmirror, official npm.
- Failure classification includes DNS, network timeout, dependency conflict, access denied, Java, Android SDK, and disk-space failures.
- All PowerShell source files are ASCII-only.
- Build reports remain available as TXT, JSON, and HTML.

---

## منبع: `CHANGELOG_PHASE7_PART49_NPM_OFFICIAL_REGISTRY_IPV4_WATCHDOG_FIX.md`

# Phase 7 Part 49

- Removed unreachable Runflare and deprecated Taobao npm registries.
- Official npm registry is now the first choice; npmmirror is fallback.
- Replaced unreliable `npm ping` with a real `npm view expo-camera version` metadata request.
- A failed probe no longer aborts the build; official npm installation is still attempted.
- Added IPv4-first Node DNS behavior to reduce intermittent IPv6 resets.
- Removed stale npm proxy/cafile settings before dependency installation.
- Added deterministic npm timeout/retry settings.
- Added CPU activity as a watchdog progress signal so long installs are not killed while working.
- Added Node version policy reporting; Node 22 LTS is recommended.
- Converted release validation messages to ASCII-only.
- Persisted only the registry that actually completed package installation.

---

## منبع: `CHANGELOG_PHASE7_PART4_OFFLINE_SYNC.md`

# CHANGELOG_PHASE7_PART4_OFFLINE_SYNC

نسخه: 1.0.7
نسخه سایت: 95
versionCode: 10007

## انجام‌شده
- پردازش واقعی صف آفلاین موبایل در سرور.
- ثبت و پردازش ورود/خروج آفلاین کاربر.
- ثبت موقعیت‌های آفلاین در جدول location_pings.
- افزودن وضعیت processed/failed/server_result به offline_sync_logs.
- افزودن API مدیریتی مشاهده لاگ همگام‌سازی آفلاین.
- اصلاح کلاینت موبایل برای ارسال صحیح batch آفلاین.
- تبدیل ثبت ورود/خروج من به postOrQueue برای جلوگیری از ازبین‌رفتن عملیات هنگام قطع اینترنت.
- تجمیع دیتابیس در php/public/upgrade.php.

## باقی‌مانده
- ساخت نسخه production بدون Babel داخل مرورگر.
- تست میدانی GPS/QR/Push روی دستگاه واقعی.
- تست خودکار API.
- رابط پیشرفته حل تعارض عملیات آفلاین.

---

## منبع: `CHANGELOG_PHASE7_PART50_ONE_CLICK_RESUMABLE_MYKET_BUILD.md`

# Phase 7 Part 50 - One-click resumable Myket build

- Added `mobile/BUILD-ONE-CLICK.ps1` as the main build orchestrator.
- Added `mobile/BUILD-ONE-CLICK.bat` for double-click execution.
- Added stage checkpoints in `.build-state.json` and automatic resume.
- Added `-Fresh`, `-SkipCleanup`, `-SkipDoctor`, and `-FromStage` options.
- Added official npm registry first with npmmirror fallback.
- Added dependency validation before Expo prebuild.
- Split Gradle clean, APK, and AAB into independent stages.
- Preserved APK success even when optional AAB generation fails.
- Added signing, Node, Java, project file, and environment preflight checks.
- Existing `build-myket-release.ps1` now delegates to the new engine.
- All new PowerShell and batch files are ASCII-only.

---

## منبع: `CHANGELOG_PHASE7_PART51_IRAN_IP_COUNTRY_STATION_DISTANCE.md`

# Phase 7 Part 51 — Iran IP verification and station-exit distance

## Changes

- Added server-side verification for Iranian IPv4 addresses before composing VPN alerts.
- Added daily cached download of official Iran IPv4 prefixes from RIPEstat using PHP cURL only.
- Added safe local seed prefixes for known Iranian mobile/provider ranges, including the reported examples:
  - 5.112.0.0/12
  - 83.120.0.0/14
- Corrected stored country values for historical records in those ranges through `upgrade.php`.
- VPN alerts no longer claim a country change when the observed IP belongs to an Iranian range.
- Added exact distance from the user's last location to the boundary of the assigned line/station geofence.
- Station-exit alerts now include:
  - line number
  - origin and destination
  - station name
  - event date/time
  - distance to the line boundary in metres
- Circle and polygon geofences are both supported.
- PHP syntax checks passed for `routes.php` and `upgrade.php`.

## Deployment

Replace:

- `php/lib/routes.php`
- `php/public/upgrade.php`

Then temporarily enable maintenance mode and run `upgrade.php` once to correct historical country values.

---

## منبع: `CHANGELOG_PHASE7_PART52_COMPANY_REQUESTS_PHASE1.md`

# Phase 7 Part 52 — Company Requests Phase 1

- Added idempotent database schema for request types, requests, files, payments, logs and settings.
- Added four default service types and configurable tariffs.
- Added authenticated mobile/user APIs for settings, create, upload, payment submission, list and detail.
- Added admin APIs for list, detail, status workflow and settings/tariffs.
- Added standalone administrative viewer page `php/public/company_requests_admin.html`.
- Added card-to-card and Bale wallet configuration fields.
- Added audit logs for request creation, uploads, payments and status changes.

---

## منبع: `CHANGELOG_PHASE7_PART53_COMPANY_REQUESTS_MOBILE_PHASE2.md`

# Phase 7 Part 53 - Company Requests Mobile Phase 2

- Added the native Android screen "Send to Company".
- Added four request workflows for technical inspection, insurance, taxi license renewal, and operation license renewal.
- Added server-driven tariffs and required document lists.
- Added sample-document previews from the supplied archive.
- Added camera/gallery capture and adjustable edge crop before upload.
- Added front-camera portrait capture with face guide.
- Added sequential authenticated document upload and progress tracking.
- Added Bale wallet and card-to-card payment selection.
- Added card details, receipt image and tracking-number submission.
- Added request history and status tracking.

---

## منبع: `CHANGELOG_PHASE7_PART54_COMPANY_PAYMENT_WORKFLOW.md`

# Phase 7 Part 54 — Company Requests Payment and Workflow

- Bale wallet invoice via sendInvoice
- PreCheckoutQuery validation and response
- SuccessfulPayment persistence and automatic request submission
- Card-to-card receipt review endpoints
- Required document validation before payment
- SHA-256 duplicate file protection and image thumbnails
- Correction workflow notifications
- User-facing invoice confirmation in Android app

---

## منبع: `CHANGELOG_PHASE7_PART55_COMPANY_REQUESTS_ADMIN_REVIEW.md`

# Phase 7 Part 55 — Company Requests Admin Review Panel

- Native company documents module added to the main React admin panel
- Sidebar item and role-permission key: companyrequests
- Request list with search and status filters
- Full request detail modal with form data, files, payments, and audit history
- Image thumbnails and direct full-size document links
- Card-to-card payment approval/rejection workflow
- Correction request, approve, reject, and complete actions
- Service tariff editor
- Bale wallet and card-to-card payment settings editor
- Panel bundle build v129

---

## منبع: `CHANGELOG_PHASE7_PART56_COMPANY_DOCUMENT_QUALITY_OCR.md`

# Phase 7 Part 56 - Company document quality and OCR

- Added server-side image quality scoring for company request documents.
- Added brightness, contrast, sharpness, resolution and aspect-ratio checks.
- Added configurable minimum score and optional hard rejection.
- Added optional Cloud OCR for technical inspection and insurance documents.
- Stored quality/OCR metadata with each uploaded document.
- Added pre-upload validation and warnings in the Android app.
- Added quality score and OCR text to the admin review panel.
- Added idempotent database upgrade columns/settings.

---

## منبع: `CHANGELOG_PHASE7_PART57_COMPANY_BALE_PAYMENT_HARDENING.md`

# Phase 7 Part 57 - Company Bale Payment Hardening

Completed stage 3 of the company document request workflow.

- Added retry logic for Bale `sendInvoice`.
- Added invoice message and send-time persistence.
- Added strict IRR, amount and payload validation for `PreCheckoutQuery`.
- Added idempotent `SuccessfulPayment` handling.
- Added transaction inquiry through `inquireTransaction`.
- Added user payment status endpoint and invoice resend endpoint.
- Added admin reconciliation endpoint for pending Bale payments.
- Added payment error, inquiry count and last inquiry tracking.
- Added mobile payment status screen with refresh and resend controls.
- Added database upgrade columns and feature flags.

---

## منبع: `CHANGELOG_PHASE7_PART58_COMPANY_CARD_PAYMENT_WORKFLOW.md`

# Phase 7 Part 58 - Company card-to-card payment workflow

- Dedicated card payment records and duplicate tracking protection
- Amount/date/bank/device/IP validation
- Receipt ownership verification
- Resubmit rejected receipts
- Operator review queue and synchronized status
- Mobile card copy, amount, paid time, bank and receipt crop fields
- Configurable card payment rules

---

## منبع: `CHANGELOG_PHASE7_PART59_COMPANY_ADMIN_WORKFLOW_NOTIFICATIONS.md`

# Phase 7 Part 59 — Company requests admin workflow and notifications

- Added management dashboard statistics for company document requests.
- Added overdue/SLA calculation and filtering.
- Added request due dates based on each service deadline.
- Added automated SLA warning scan and notifications to configured administrators.
- Added administrator notifications for submitted requests and card-to-card receipts.
- Added in-app push and Bale notifications for request status changes.
- Added mandatory rejection/cancellation reason.
- Added lifecycle timestamps, completion timestamp and latest admin note.
- Added payment-status and overdue filters in the React administration panel.
- Added configurable SLA warning hours and administrator recipient IDs.
- Panel build: v132.

---

## منبع: `CHANGELOG_PHASE7_PART5_OFFLINE_CONFLICTS.md`

# CHANGELOG_PHASE7_PART5_OFFLINE_CONFLICTS

نسخه: 1.0.8  
نسخه سایت: SITE_VERSION = 96  
versionCode اندروید: 10008

## انجام‌شده در قسمت پنجم

- افزایش نسخه اپ از 1.0.7 به 1.0.8.
- افزایش نسخه سایت از 95 به 96.
- افزایش versionCode از 10007 به 10008.
- افزودن مدیریت تعارض‌های همگام‌سازی آفلاین در بخش شیفت و کارکرد.
- افزودن تب «تعارض آفلاین» در صفحه شیفت کاری و کارکرد.
- افزودن API مشاهده رکوردهای آفلاین ناموفق، تعارضی و پردازش‌نشده.
- افزودن امکان پردازش مجدد رکورد آفلاین توسط مدیر.
- افزودن امکان نادیده گرفتن رکوردهای آفلاین نامعتبر یا تکراری.
- افزودن امکان علامت‌گذاری رکورد به‌عنوان تعارض مدیریتی.
- افزودن Audit Trail برای تمام اقدامات مدیر روی رکوردهای آفلاین.
- افزودن ستون‌های resolved_by، resolved_at، resolution_note و conflict_reason به offline_sync_logs.
- افزودن جدول offline_sync_audit.
- تجمیع بروزرسانی دیتابیس در php/public/upgrade.php.

## APIهای اضافه‌شده

- GET /api/admin/offline-sync-conflicts
- POST /api/admin/offline-sync-conflicts/{id}/retry
- POST /api/admin/offline-sync-conflicts/{id}/ignore
- POST /api/admin/offline-sync-conflicts/{id}/mark-conflict
- GET /api/admin/offline-sync-conflicts/{id}/audit
- GET /api/admin/phase7-part5/status
- GET /api/project-version-v7p5

## باقی‌مانده بعد از این قسمت

- حذف کامل Babel مرورگر و ساخت پنل به‌صورت Production Bundle.
- تست میدانی GPS، Push، آلارم و Offline Sync روی چند گوشی واقعی.
- تست فشار روی سرور نهایی.
- تست خودکار APIها و سناریوهای حضور/شیفت.

---

## منبع: `CHANGELOG_PHASE7_PART60_COMPANY_DOCUMENT_QUALITY_NO_OCR.md`

# Phase 7 Part 60 — Company document quality controls without OCR

## Implemented
- OCR is disabled for the company-document workflow.
- Server-side image quality scoring for resolution, brightness, contrast, sharpness, glare, deep shadows, aspect ratio and crop coverage.
- Heuristic screenshot detection for gallery images and common screen resolutions.
- Cross-request SHA-256 duplicate protection for each user.
- Original and processed copies are stored separately; the processed JPEG strips metadata and is normalized for review.
- Configurable minimum resolution, acceptance score, good score, processed width and JPEG quality.
- Optional hard rejection of low-quality documents.
- New analyze, validate, duplicate-check and finalize endpoints.
- Mobile app sends capture source and crop metadata, shows quality warnings and a per-document quality summary.
- Admin panel shows detailed quality metrics, warnings, source and links to original/processed files.

## APIs
- POST /api/company-document/analyze
- POST /api/company-document/validate
- POST /api/company-document/duplicate-check
- POST /api/company-document/finalize

## Database additions
- company_request_files.original_path
- company_request_files.processed_path
- company_request_files.processed_size
- company_request_files.source_type

---

## منبع: `CHANGELOG_PHASE7_PART6_AUTO_LINE_CHECKIN.md`

# CHANGELOG - Phase 7 Part 6 - Auto Line Check-in

Version: 1.0.9
SITE_VERSION: 97
versionCode: 10009

## Changes
- Removed manual line selection from the mobile "ثبت حضور من" screen.
- Mobile app no longer sends `line_id` for normal check-in.
- Server now decides the final line by checking all lines assigned to the user.
- If the user is inside any assigned line geofence, attendance is registered for that line automatically.
- Old clients that still send `line_id` are sanitized; unassigned line ids are ignored.
- Method restrictions are rechecked after automatic line detection.
- Offline check-in conflict handling now supports automatic line detection and line identifier matching.
- Database updates remain consolidated in `php/public/upgrade.php`; no separate SQL file was added.

---

## منبع: `CHANGELOG_PHASE7_PART7_SHIFT_FINALIZATION.md`

# CHANGELOG — Phase 7 Part 7: Shift Scheduling Finalization

Version: 1.0.10  
Site Version: 98  
Android versionCode: 10010

## انجام‌شده

- تکمیل مرحله‌های باقی‌مانده شیفت‌بندی و شیفت خودکار.
- افزودن قانون اختصاصی شیفت خودکار برای هر کاربر مستقل از سمت.
- افزودن جدول `user_work_rule_overrides` برای تنظیم موظفی، اضافه‌کار، مازاد، شب‌کاری، جمعه‌کاری، تعطیل‌کاری و بازه مجاز ورود برای هر فرد.
- افزودن جلوگیری از تخصیص شیفت‌های هم‌پوشان برای یک کاربر.
- افزودن ثبت تاریخچه تغییرات تخصیص شیفت در `shift_assignment_audit`.
- اعمال تنظیمات پیشرفته روزانه `day_config` در محاسبه کارکرد شیفت‌های تقویمی.
- اصلاح محاسبه شیفت خودکار برای جمعه و تعطیل رسمی:
  - امکان محاسبه جمعه‌کاری و تعطیل‌کاری مستقل از موظفی.
  - امکان تنظیم اینکه جمعه/تعطیل داخل موظفی حساب شود یا جدا از موظفی.
- اعمال کلیدهای `night_calc`، `friday_calc` و `holiday_calc` در شیفت خودکار.
- افزودن کنترل حداکثر زمان باز بودن جلسه حضور.
- افزودن قابلیت خروج خودکار جلسات باز طبق قانون سمت/کاربر.
- افزودن API محاسبه مجدد کارکرد در بازه زمانی.
- افزودن API عیب‌یابی شیفت‌بندی:
  - تعارض تخصیص شیفت.
  - شیفت چرخشی بدون تاریخ شروع.
  - جلسات باز طولانی.
  - کاربرانی که قانون دقیق سمت ندارند.
- افزودن تب‌های مدیریتی به بخش «شیفت و کارکرد»:
  - قانون اختصاصی افراد.
  - عملیات و عیب‌یابی شیفت.
- تجمیع به‌روزرسانی دیتابیس داخل `php/public/upgrade.php`.

## APIهای جدید

- `GET /api/admin/rule-engine/user-overrides`
- `PUT /api/admin/rule-engine/user-overrides/{user_id}`
- `DELETE /api/admin/rule-engine/user-overrides/{user_id}`
- `GET /api/admin/shift-planning/diagnostics`
- `POST /api/admin/attendance/recalculate`
- `POST /api/admin/attendance/auto-close-open-sessions`
- `GET /api/admin/shift-assignment-audit`
- `GET /api/admin/phase7-part7/status`

## نکته نصب

پس از جایگزینی فایل‌ها، فقط `php/public/upgrade.php` اجرا شود. فایل SQL جداگانه لازم نیست.

---

## منبع: `CHANGELOG_PHASE7_PART8_CLIENT_TIME_WELFARE_FIX.md`

# CHANGELOG — Phase 7 Part 8 — Client Time & Welfare Date Fix

نسخه: 1.0.11
نسخه سایت: 99
versionCode: 10011

## تغییرات اصلی

- ثبت زمان واقعی عملیات از سمت کلاینت با `client_time` برای درخواست‌های آنلاین و آفلاین.
- جلوگیری از ثبت زمان لحظه ارسال صف آفلاین به‌جای زمان واقعی انجام عملیات.
- اصلاح ثبت حضور مسئولین تا در حالت آفلاین `created_at` بر اساس زمان واقعی ثبت در گوشی ذخیره شود.
- اصلاح ثبت گزارش‌ها تا زمان ارسال واقعی کاربر ملاک باشد.
- اصلاح ثبت حضور/خروج کاربران در `staff_attendance` بر اساس زمان ثبت کلاینت.
- اصلاح ثبت حضور و خروج تاکسیران‌ها در `attendances` بر اساس زمان ثبت کلاینت.
- اصلاح تاریخ رفاهیات در اپ: مقدار اولیه تاریخ دیگر رشته خام نیست و به‌صورت شیء جلالی معتبر ذخیره می‌شود.
- اصلاح ثبت رفاهیات در سرور با نرمال‌سازی تاریخ شمسی و جلوگیری از ذخیره `undefined/NaN`.
- اصلاح ثبت فعالیت‌های فرهنگی مشابه رفاهیات.
- تبدیل ثبت رفاهیات و فعالیت فرهنگی در اپ به `postOrQueue` برای حفظ عملیات در قطع اینترنت.
- اصلاح صف آفلاین موبایل: فقط آیتم‌هایی از صف حذف می‌شوند که سرور واقعاً `synced_ids` آن‌ها را تأیید کرده باشد.
- افزودن پردازش آفلاین برای:
  - حضور مسئولین
  - رفاهیات
  - فعالیت‌های فرهنگی
  - گزارش‌ها
- درخواست‌های آفلاین ناشناخته دیگر به‌اشتباه processed حساب نمی‌شوند.

## دیتابیس

تمام بروزرسانی‌ها در فایل زیر تجمیع شد:

`php/public/upgrade.php`

## فایل‌های مهم تغییر یافته

- `mobile/src/api.js`
- `mobile/src/offline.js`
- `mobile/src/screens/WelfareScreen.js`
- `mobile/src/screens/CulturalScreen.js`
- `php/lib/routes.php`
- `php/public/upgrade.php`
- `mobile/app.config.js`
- `mobile/package.json`

---

## منبع: `CHANGELOG_PHASE7_PART9_PLATE_OCR_LEARNING.md`

# CHANGELOG — Phase 7 Part 9 — Plate OCR Learning

Version: 1.0.12  
Site version: 100  
Android versionCode: 10012

## اعمال‌شده

- تغییر منطق اسکن پلاک به مدل اختصاصی تاکسی مشهد:
  - کد منطقه همیشه `12`
  - حرف پلاک همیشه `ت`
  - OCR فقط دو رقم اول و سه رقم آخر را استخراج می‌کند
- افزودن OCR مقاوم‌تر برای حالت‌های زیر:
  - `72ت575-12`
  - `72 ت 575 - 12`
  - `۱۲ ت ۵۷۵ - ۱۲`
  - خروجی معکوس OCR مانند `12 575 ت 72`
  - خروجی فقط عددی مانند `72575`
- افزودن کادر مخصوص پلاک در صفحه اسکن.
- افزودن برش خودکار قسمت پلاک از داخل کادر دوربین.
- افزودن صفحه تأیید انسانی بعد از اسکن پلاک.
- افزودن امکان اصلاح دستی دو رقم اول و سه رقم آخر قبل از جستجو.
- افزودن ذخیره نمونه آموزشی پلاک برای بهبود مدل در آینده.
- افزودن API ثبت نمونه آموزشی:
  - `POST /api/plate-scan-samples`
- افزودن API مدیریتی مشاهده نمونه‌ها:
  - `GET /api/admin/plate-scan-samples`
- افزودن جدول دیتابیس:
  - `plate_scan_samples`
- اصلاح جستجوی پلاک در سرور با نرمال‌سازی پلاک تاکسی ۱۲/ت.
- اصلاح اسکن و جستجوی کد ملی با اعداد فارسی و عربی.
- اصلاح جستجوی آفلاین برای اعداد فارسی/عربی.
- بروزرسانی تجمیعی دیتابیس در:
  - `php/public/upgrade.php`

## رفتار جدید اپ

در جستجوی پلاک:

1. کاربر گزینه اسکن پلاک را می‌زند.
2. پلاک را داخل کادر زرد قرار می‌دهد.
3. اپ تصویر کادر را برش می‌دهد.
4. OCR فقط ۵ رقم مهم را می‌خواند.
5. کاربر نتیجه را تأیید یا اصلاح می‌کند.
6. نمونه اصلاح‌شده برای آموزش آینده ذخیره می‌شود.
7. جستجو با پلاک نهایی انجام می‌شود.

ساختار نهایی پلاک:

```text
[دو رقم] ت [سه رقم] - 12
```

---

## منبع: `CHANGELOG_PRESENCE_ALARM_STOP_FIX.md`

# اصلاح قطع آلارم صحت‌سنجی حضور

## مشکل
در برخی گوشی‌ها پس از باز شدن صفحه صحت‌سنجی و شروع فرآیند، صدای آلارم همچنان پخش می‌شد.

## اصلاحات انجام‌شده
- قطع فوری آلارم هنگام زدن دکمه «شروع — گرفتن سلفی».
- قطع آلارم هنگام ورود به مراحل سلفی، عکس خودروها، ارسال و ثبت موفق.
- جلوگیری از اجرای چندباره و همزمان چند نمونه آلارم.
- حفظ قطع آلارم در پایان، انقضا یا بستن فرآیند صحت‌سنجی.

## فایل‌های اصلاح‌شده
- `mobile/src/presenceAlarm.js`
- `mobile/src/PresenceCheckModal.js`
- `mobile/src/PresenceGate.js`

---

## منبع: `CHANGELOG_PRESENCE_VALIDATION_ALERTS.md`

# تغییرات صحت‌سنجی حضور و هشدار موبایل

## اصلاحات موبایل
- کانال اختصاصی Android با نام `presence_alarm` برای هشدار صحت‌سنجی حضور اضافه شد.
- اهمیت اعلان صحت‌سنجی روی سطح حداکثری تنظیم شد تا با صفحه خاموش نیز صدا/لرزش اجرا شود.
- اعلان‌های دریافتی با `data.type = presence_check` مستقیماً پنجره صحت‌سنجی را در اپ باز می‌کنند.
- هشدار داخلی اپ همچنان هنگام فعال بودن پنجره صحت‌سنجی، صدای آلارم و لرزش تکرارشونده پخش می‌کند.
- کنترل هوش مصنوعی/تشخیص چهره در سلفی صحت‌سنجی حذف شد؛ کافی است کاربر داخل کادر مشخص‌شده باشد و عکس را تأیید کند.

## اصلاحات سرور و پنل
- تنظیم جدید در بخش «صحت‌سنجی حضور» اضافه شد:
  - ارسال برای همه کاربران مشمول صحت‌سنجی
  - ارسال فقط برای کاربران مشمولی که در ساعت شیفت کاری هستند
- محاسبه تخلف «عدم ارسال صحت‌سنجی» با همین تنظیم هماهنگ شد.
- Endpoint کرون جدید اضافه شد:
  - `/api/cron/presence-alert?key=CRON_KEY`
- Push سمت سرور برای صحت‌سنجی از کانال `presence_alarm` استفاده می‌کند.

## نکته نصب
برای اینکه کاربران حتی وقتی اپ باز نیست یا صفحه خاموش است هشدار دریافت کنند، روی هاست کرون هر دقیقه فعال شود:

```bash
* * * * * wget -qO- "https://YOUR_DOMAIN/api/cron/presence-alert?key=CRON_KEY" >/dev/null 2>&1
```

`CRON_KEY` همان کلید کرون تعریف‌شده در تنظیمات برنامه است.

---

## منبع: `CHANGELOG_SHIFT_ATTENDANCE_FIXES.md`

# اصلاحات حضور، شیفت‌بندی و محاسبه کارکرد

## موارد اصلاح‌شده

1. **محاسبه صحیح شیفت شب و بازه‌های عبوری از نیمه‌شب**
   - بازه‌هایی مثل `23:00 تا 07:00` اکنون ۸ ساعت محاسبه می‌شوند.
   - خروج روز بعد برای شیفت شب در محاسبه کارکرد، کسری و شب‌کاری لحاظ می‌شود.

2. **محاسبه دقیق شب‌کاری**
   - الگوریتم قبلی گام ۵ دقیقه‌ای داشت و می‌توانست اختلاف ایجاد کند.
   - الگوریتم جدید بر اساس هم‌پوشانی دقیقه‌ای با بازه `22:00 تا 06:00` محاسبه می‌کند.

3. **پشتیبانی از جلسه باز حضور**
   - اگر کاربر ورود زده و هنوز خروج نزده باشد، در همان روز جاری کارکرد تا لحظه فعلی محاسبه می‌شود.
   - برای روزهای گذشته، رکورد باز بدون خروج به‌صورت خودکار وارد محاسبه نمی‌شود تا گزارش غیرواقعی تولید نشود.

4. **محاسبه اسفند ۳۰ روزه در سال کبیسه**
   - تعداد روزهای ماه شمسی از تابع مرکزی `ShiftCalc::jMonthDays` محاسبه می‌شود.
   - گزارش ماهانه و دریافت تعطیلات از این منطق استفاده می‌کنند.

5. **اعمال بازه شروع و پایان تخصیص شیفت**
   - فیلدهای `from_jdate` و `to_jdate` در گزارش ماهانه رعایت می‌شوند.
   - ورود کاربر نیز بر اساس شیفت فعال همان روز کنترل می‌شود.

6. **محاسبه تأخیر ورود و تعجیل خروج**
   - خروجی‌های `late_in` و `early_out` به گزارش ماهانه اضافه شد.
   - خروجی اکسل نیز این ستون‌ها را دارد.

7. **تفکیک حضور کل و حضور در بازه شیفت**
   - `worked`: کل مدت حضور ثبت‌شده.
   - `in_shift`: مدت حضور داخل بازه موظفی شیفت.

8. **بهبود گزارش تردد پرسنل**
   - گزارش روزانه هر کاربر اکنون شیفت فعال همان روز را تشخیص می‌دهد.
   - شیفت‌های پیشرفته تقویمی در گزارش تردد روزانه نیز لحاظ می‌شوند.
   - فیلدهای موظفی، حضور کل، تأخیر و تعجیل خروج به داده گزارش اضافه شد.

9. **پشتیبانی از شیفت چرخشی ۷/۷/۷**
   - نوع شیفت `rotating` اضافه شد.
   - الگوی پیش‌فرض: ۷ روز صبح `07-15`، ۷ روز عصر `15-23`، ۷ روز شب `23-07`.
   - تاریخ شروع چرخه از تنظیمات شیفت دریافت می‌شود.

10. **کنترل بهتر آستانه ورود/خروج**
    - آستانه‌ها برای شیفت‌های عبوری از نیمه‌شب درست‌تر بررسی می‌شوند.
    - در صورت نبود آستانه، بازه پیش‌فرض یک ساعت قبل/بعد شروع یا پایان شیفت اعمال می‌شود.

## فایل‌های اصلی تغییرکرده

- `php/lib/ShiftCalc.php`
- `php/lib/routes.php`
- `php/public/panel.html`

## نکته اجرایی

برای شیفت چرخشی، در پنل مدیریت از مسیر:

`شیفت و کارکرد → تعریف شیفت‌ها → + شیفت چرخشی ۷/۷/۷`

تاریخ شروع چرخه را وارد کنید. تاریخ شروع باید اولین روز دوره صبح باشد.

---

## منبع: `CHANGELOG_STARTUP_REFRESH_MAINTENANCE.md`

# صفحه آغازین، تازه‌سازی داده‌ها و کنترل حالت تعمیر

- تصویر ارسالی کاربر در `mobile/assets/khatyar-startup.png` قرار گرفت.
- در هر اجرای کامل برنامه، صفحه آغازین با نشانگر بارگذاری و متن مرحله جاری نمایش داده می‌شود.
- پیش از ورود، مسیر عمومی `/api/system/maintenance-status` بدون کش بررسی می‌شود.
- اگر حالت تعمیر فعال باشد، هیچ‌یک از صفحات ورود یا داشبورد نمایش داده نمی‌شوند و فقط پیام تعمیر و دکمه بررسی مجدد دیده می‌شود.
- آخرین وضعیت معتبر تعمیر ذخیره می‌شود تا هنگام اختلال شبکه نیز حالت تعمیر فعال دور زده نشود.
- کش داده‌های دریافتی از API و پیکربندی برنامه در هر اجرای کامل پاک و از سرور تازه‌سازی می‌شود.
- صف ارسال‌های آفلاین در شروع برنامه ارسال می‌شود؛ فقط موارد تأییدشده توسط سرور از صف حذف می‌شوند و در صورت شکست ارتباط، داده‌ها حفظ می‌شوند.
- نسخه SDK، Expo، React Native، Gradle، پلاگین‌ها و وابستگی‌ها تغییر نکرده‌اند.

---

## منبع: `CHANGELOG_STATION_EXIT_BIRTHDAY_FIXES.md`

# اصلاحات اعلان خروج از محدوده و تبریک تولد

## 1) اعلان خروج کاربر از محدوده خط
- در تنظیمات سایت، حالت «ارسال برای یک شخص مشخص» دیگر نیاز به ورود دستی شناسه کاربر ندارد.
- ابتدا سمت کاربر دریافت‌کننده انتخاب می‌شود و سپس لیست کاربران همان سمت نمایش داده می‌شود.
- مقدار نهایی همچنان به‌صورت `user_id` ذخیره می‌شود تا با منطق اعلان قبلی سازگار باشد.
- متن راهنما و فرم تنظیمات اصلاح شد.

## 2) اصلاح اعلان تبریک تاریخ تولد
- مشکل تطبیق تولد فقط با فرمت محدود `MM-DD` برطرف شد.
- تاریخ تولد اکنون با فرمت‌های زیر پشتیبانی می‌شود:
  - `1370-04-15`
  - `1370/04/15`
  - `1370.04.15`
  - ماه و روز یک‌رقمی مانند `1370/4/5`
  - تاریخ میلادی ذخیره‌شده با سال 1900 به بالا، با تبدیل به شمسی
- Cron مستقل `cron.php birthday` و API کرون `/api/cron/birthday` با همین منطق اصلاح شدند.
- ارسال پیام براساس تنظیمات سایت شامل نوتیفیکیشن، پیامک یا هر دو انجام می‌شود.

## 3) داشبورد متولدین ماه جاری
- API `GET /api/admin/birthdays-month` اصلاح شد.
- لیست متولدین ماه جاری براساس ماه شمسی فعلی نمایش داده می‌شود.
- مرتب‌سازی براساس روز تولد انجام می‌شود.
- فیلدهای `days_left`, `is_today`, `passed` برای نمایش دقیق‌تر در داشبورد برگردانده می‌شوند.

## فایل‌های تغییر یافته
- `php/public/panel.html`
- `php/lib/routes.php`
- `php/public/cron.php`

---

## منبع: `CHANGELOG_TEMP_DRIVER_VEHICLE_NATIONALCODE_FIX.md`

# اصلاح رانندگان موقت و کد ملی

- رفع کرش جستجوی رانندگان موقت با کد ملی در اپ موبایل.
- مقاوم‌سازی پاسخ API در برابر اطلاعات ناقص راننده/خط/خودرو.
- افزودن جستجوی خودرو در مدیریت رانندگان موقت خطوط.
- امکان انتخاب همزمان بهره‌بردار و راننده کمکی برای ثبت موقت در خط.
- پشتیبانی API از ثبت چند راننده در یک درخواست.
- حذف نمایش جداکننده سه‌رقمی از کد ملی در پنل و ورودی‌های مرتبط.

---

## منبع: `CHANGELOG_UI_AUTO_LINE_SALARY_SLIPS.md`

# تغییرات بازطراحی، ثبت حضور هوشمند و فیش حقوقی

- بازطراحی رنگ‌بندی و اجزای ظاهری اپ برای تجربه کاربری بهتر.
- انتخاب خودکار خط در «ثبت حضور من» بر اساس نزدیک‌ترین محدوده GPS تعریف‌شده برای کاربر.
- امکان تغییر دستی خط در صورت نیاز حفظ شد.
- نمایش اعداد کارکرد و تایمر حضور به صورت فارسی.
- تکمیل آیتم‌های کارکرد: موظفی، اضافه‌کار، کسری، شب‌کاری، جمعه‌کاری، تعطیل‌کاری، مرخصی، ماموریت، تأخیر، تعجیل خروج و تردد دستی.
- افزودن ماژول فیش حقوقی PDF:
  - بارگذاری فایل PDF برای هر کاربر با انتخاب سال و ماه شمسی.
  - مشاهده فیش‌های پیوست‌شده در اپ از منوی «فیش حقوقی».
  - مشاهده/دانلود فایل PDF توسط کاربر.
- فایل دیتابیس جدید: php/db/upgrade_salary_slips.sql

---

## منبع: `CHANGELOG_USER_BIRTHDATE_FIELD_FIX.md`

# اصلاح فیلد تاریخ تولد کاربران

## مشکل
در بخش مدیریت کاربران، تاریخ تولد پس از ذخیره مجدد در فرم ویرایش خالی نمایش داده می‌شد. همچنین انتخابگر تاریخ امکان انتخاب مستقیم سال نداشت.

## اصلاحات اعمال‌شده
- افزودن انتخاب سال به تقویم شمسی فرم‌ها.
- اصلاح مقداردهی اولیه تقویم برای تاریخ‌های شمسی ذخیره‌شده با جداکننده‌های `-`، `/` و `.`.
- ارسال `birth_date` در درخواست ویرایش کاربر به API.
- ذخیره `birth_date` هنگام افزودن کاربر جدید.
- اطمینان خودکار از وجود ستون `users.birth_date` در مسیرهای API.
- افزودن اسکریپت کمکی `php/db/upgrade_birthdate_fix.sql` برای دیتابیس‌های قدیمی.

## نحوه تست
1. از مدیریت کاربران یک کاربر را ویرایش کنید.
2. تاریخ تولد را با انتخاب سال، ماه و روز ثبت کنید.
3. ذخیره کنید.
4. دوباره وارد ویرایش همان کاربر شوید؛ تاریخ تولد باید نمایش داده شود.

---

## منبع: `CHANGELOG_V134_SCANNER_COMPANY_DOCUMENTS_CALENDAR.md`

# گزارش تغییرات نسخه 1.3.13 / سایت 111 / پنل v134

تاریخ: 2026-07-24

## موبایل
- اصلاح نسبت و محل کادر اسکن پلاک مطابق فرم افقی پلاک تاکسی.
- اصلاح نسبت کادر اسکن کارت ملی مطابق نسبت واقعی کارت و افزودن گوشه‌های راهنما.
- محاسبه واکنش‌گرای کادرها بر اساس ابعاد واقعی نمایشگر.
- اصلاح کادر قرارگیری چهره در صحت‌سنجی حضور و جلوگیری از کشیدگی روی نمایشگرهای مختلف.
- تبدیل صفحه تأیید سلفی به صفحه اسکرول‌پذیر و واکنش‌گرا؛ دکمه‌های «گرفتن دوباره» و «تأیید عکس» دیگر ناقص یا خارج از صفحه نیستند.
- اصلاح مقدار پیش‌فرض تمام انتخابگرهای تاریخ عمومی به تاریخ شمسی روز جاری.
- حذف سال ثابت 1370 از پروفایل و سال ثابت 1380 از فرم درخواست شرکت.
- افزایش نسخه نمایشی اپ از 1.3.12 به 1.3.13 و versionCode از 10312 به 10313.

## سایت و بک‌اند
- تعریف امن تابع `faDate` و رفع خطای `ReferenceError: faDate is not defined` در «مدارک ارسالی شرکت».
- افزایش شناسه ساخت پنل از v133 به v134.
- افزایش SITE_VERSION از 110 به 111 و APP_VERSION سمت سرور به 1.3.13.
- افزودن ترمیم خودکار ستون‌های تکمیلی جدول `company_request_files` برای نصب‌های قدیمی.
- مقاوم‌سازی تشخیص MIME؛ در نبود افزونه PHP Fileinfo، نوع JPEG/PNG/WebP/PDF از امضای فایل شناسایی می‌شود.
- اجرای ترمیم ساختار پیش از بارگذاری مدرک و پیش از نمایش جزئیات مدارک در پنل.

## محدودیت‌های رعایت‌شده
- نسخه SDK، Expo، React Native، Gradle و پلاگین‌ها تغییر نکرد.
- فایل‌های `package.json` و `package-lock.json` تغییر نکردند.

---

## منبع: `CHANGELOG_V135_FIELD_ALERT_FILTERS_STATUS.md`

# تغییرات نسخه v135 / اپ 1.3.14

- افزودن تنظیمات شخصی دریافت هشدارهای میدانی در اپ برای کاربران مجاز.
- امکان فعال/غیرفعال‌کردن کل هشدارها و انتخاب جداگانه خروج/ورود محدوده، VPN، GPS و ورود/خروج حضور من.
- امکان دریافت هشدار تمام کاربران، سمت‌های منتخب یا کاربران منتخب.
- اعمال فیلتر شخصی گیرنده در بک‌اند پیش از ایجاد اعلان و Push.
- افزودن دوره مستقل بررسی GPS، VPN و خروج از محدوده در تنظیمات سایت.
- افزودن نمایش مدت روشن‌بودن VPN، خاموش‌بودن GPS و خارج‌بودن از محدوده در داشبورد اپ همراه تذکر.
- رفع متغیر تعریف‌نشده در اعلان gps_off و استفاده از زمان واقعی رویداد.
- بدون تغییر SDK، Expo، React Native، Gradle، پلاگین‌ها، package.json و package-lock.json.

---

## منبع: `CHANGELOG_V136_SETTINGS_COMPANY_REQUEST_500_FIX.md`

# تغییرات نسخه پنل v136

تاریخ: ۱۴۰۵/۰۵/۰۲

## تنظیمات پایش
- تب مستقل «پایش و هشدارها» در صفحه تنظیمات پنل افزوده شد.
- دوره بررسی خاموش بودن GPS، روشن بودن VPN و خروج از محدوده با عنوان و واحد روشن نمایش داده می‌شود.
- محدوده معتبر هر مقدار ۱۵ تا ۳۶۰۰ ثانیه است.
- تنظیمات GSM نیز در همین تب قرار گرفت.

## رفع خطای 500 درخواست‌های شرکت
- ترمیم خودکار ستون‌های تکمیلی جدول company_requests پیش از تغییر وضعیت و SLA اجرا می‌شود.
- ستون‌های due_at، last_status_at، completed_at، admin_note، priority، last_sla_notified_at، reviewed_by، reviewed_at و assigned_to در نصب‌های قدیمی ساخته می‌شوند.
- ستون meta جدول company_request_logs نیز در صورت نبودن ایجاد می‌شود.
- پارامتر INTERVAL در SLA به مقدار عددی امن تبدیل شد تا با تنظیمات سخت‌گیرانه PDO/MySQL خطای SQL ایجاد نکند.
- شکست ارسال اعلان یا ثبت لاگ، دیگر عملیات اصلی تغییر وضعیت یا SLA را با خطای 500 متوقف نمی‌کند.

## نسخه‌ها
- پنل: v136
- سایت: 112
- نسخه اپ و وابستگی‌های موبایل تغییر نکرده‌اند.

---

## منبع: `CHANGELOG_V138_COMPANY_STATUS_APK_FIX.md`

# نسخه v138 — اصلاح قطعی تغییر وضعیت درخواست شرکت و بیلد APK

- حذف اجرای schema migration از مسیرهای runtime تغییر وضعیت و SLA.
- جداسازی UPDATE اصلی از ثبت لاگ، اعلان بله و Push؛ شکست اعلان دیگر پاسخ 500 ایجاد نمی‌کند.
- ثبت خطاهای واقعی هر مرحله در PHP error log.
- اصلاح JSX فایل `mobile/src/PersonalPhotoCapture.js`؛ تگ `<View>` اکنون با `</View>` بسته می‌شود.
- رفع خطای `Expected corresponding JSX closing tag for <View>` در `createBundleReleaseJsAndAssets`.
- نسخه اپ: 1.3.16 / versionCode 10316.
- کش پنل: v138.

---

## منبع: `CHANGELOG_V139_DB_EXEC_COMPANY_STATUS_MONITORING_FIX.md`

# v139 — Db::exec، تغییر وضعیت درخواست شرکت و تب پایش

- افزودن متد واقعی `Db::exec()` به کلاس دیتابیس.
- رفع Fatal Error مسیر `POST /api/admin/company-requests/{id}/status`.
- رفع خطای SLA و سایر مسیرهایی که از `Db::exec()` استفاده می‌کنند.
- بازگردانی selector تب `monitoring` در CSS پنل.
- افزایش cache marker پنل به v139.
- حفظ اصلاح JSX فایل PersonalPhotoCapture از v138.

---

## منبع: `CHANGELOG_V140_COMPANY_STATUS_MONITORING_UPLOAD_PREVIEW_FIX.md`

# v140 — رفع قطعی تغییر وضعیت شرکت، پایش و پیش‌نمایش مدارک

- هسته تغییر وضعیت درخواست شرکت به یک UPDATE حداقلی و بدون وابستگی به کلید خارجی تبدیل شد.
- ثبت reviewed_by، ارجاع، لاگ، اعلان و Push مستقل و غیرمسدودکننده شدند.
- برای خطاهای اصلی کد پیگیری مرحله‌ای CRS-READ و CRS-UPDATE در پاسخ و error_log ثبت می‌شود.
- نسخه کش CSS و JS پنل هر دو به 140 افزایش یافت.
- selector تب پایش و هشدارها حفظ و مجدداً کنترل شد.
- نمایش امن فایل‌های JPG/PNG/WEBP/GIF/PDF در uploads/company_requests فعال و اجرای هر نوع اسکریپت مسدود شد.
- SITE_VERSION به 114 افزایش یافت.

---

## منبع: `CHANGELOG_V141_COMPANY_REQUEST_WORKFLOW_PAYMENT_CROP.md`

# v141

- نمایش فایل‌های درخواست شرکت از مسیر امن `/api/media` و fallback از thumbnail مفقود به فایل اصلی.
- انتقال نمای پنل پس از تغییر وضعیت به فهرست همان وضعیت.
- تشخیص خودکار provider token نامعتبر بله، غیرفعال‌سازی موقت پرداخت بله و پیام روشن برای ثبت توکن پذیرنده معتبر.
- انتخاب تاریخ شمسی و ساعت پرداخت کارت‌به‌کارت با منطقه زمانی تهران.
- کراپ چهارگوشه‌ای قابل درگ به جای دکمه‌های مثبت و منفی.
- حذف کنترل و نمایش کیفیت تصاویر از اپ، API و تنظیمات پنل.

---

## منبع: `CHANGELOG_V143_FIELD_STATUS_DASHBOARD.md`

# نسخه v143 — کادر زنده وضعیت دستگاه در داشبورد اپ

- کادر «وضعیت ایمنی و موقعیت دستگاه» به‌صورت دائمی در بالای داشبورد نمایش داده می‌شود.
- وضعیت VPN، GPS و محدوده ایستگاه حتی در حالت عادی نیز قابل مشاهده است.
- در حالت غیرعادی، مدت‌زمان به‌صورت زنده و ثانیه‌ای نمایش داده می‌شود.
- برای هر وضعیت، راهنمای واضح رفع مشکل نمایش داده می‌شود.
- دکمه «بررسی مجدد» برای دریافت فوری وضعیت از سرور اضافه شد.
- خطای شبکه دیگر باعث ناپدیدشدن کامل کادر نمی‌شود و پیام مشخص نمایش داده می‌شود.
- API وضعیت در برابر نبودن موقت جداول یا رکوردها مقاوم شد.
- زمان شروع VPN از آخرین رویداد واقعی VPN و زمان خروج از آخرین رویداد خروج ایستگاه خوانده می‌شود تا با هر پینگ بازنشانی نشود.
- نسخه اپ به 1.3.18 و versionCode به 10318 افزایش یافت.

---

## منبع: `CHANGELOG_V143_LIVE_FIELD_STATUS_DASHBOARD.md`

# نسخه v143 — کادر زنده وضعیت میدانی در داشبورد اپ

- کادر وضعیت VPN، GPS و محدوده ایستگاه اکنون همیشه در بالای داشبورد نمایش داده می‌شود.
- در حالت سالم، هر سه وضعیت با نشان سبز نمایش داده می‌شوند.
- در حالت هشدار، مدت روشن‌بودن VPN، مدت خاموش‌بودن GPS و مدت خارج‌بودن از محدوده تا ثانیه نمایش داده می‌شود.
- زمان شروع هر وضعیت در حافظه محلی ذخیره می‌شود و با رندر یا تازه‌سازی صفحه از بین نمی‌رود.
- VPN و GPS علاوه بر پاسخ سرور مستقیماً از دستگاه نیز بررسی می‌شوند.
- پاسخ وضعیت سرور بدون کش خوانده می‌شود و هر ۱۵ ثانیه تازه می‌گردد.
- برای هر مشکل، راهنمای واضح رفع وضعیت نمایش داده می‌شود.
- دکمه تازه‌سازی دستی اضافه شد.
- نسخه اپ به 1.3.18، versionCode به 10318 و SITE_VERSION به 116 افزایش یافت.

---

## منبع: `CHANGELOG_V144_CAMERA_CROP_DASHBOARD_STARTUP_FIX.md`

# V144
- رفع نمایش سیاه پیش‌نمایش دوربین در اسکن پلاک و کارت ملی
- تثبیت کادر اسکن روی تصویر دوربین
- انتقال وضعیت میدانی بالای پرکارترین/کم‌کارترین
- مینیمایز در حالت عادی و بازشدن خودکار هنگام هشدار
- اصلاح حرکت چهار نقطه کراپ بر اساس مختصات واقعی تصویر contain
- رفع خطای finalizedDocuments/finalizeDocuments
- حداقل زمان لودینگ آغاز برنامه: ۵ ثانیه
- بدون تغییر SDK/Expo/React Native/Gradle/plugins/package.json/package-lock.json

---

## منبع: `CHANGELOG_V145_COMPANY_PAYMENT_SELFIE_DOCS_UPLOAD_CHECKIN_FIX.md`

# v145 — Company payment, selfie preview, documents and check-in fixes

- Fixed Bale payment readiness response: provider token was removed before `bale_ready` was calculated.
- Kept Bale invoice flow based on sendInvoice, pre_checkout_query and successful_payment.
- Fixed front camera preview layout without changing Expo/SDK dependencies.
- Added camera ready and mount error states.
- Added canonical aliases for company document types, including residence_document.
- Added blocking upload/progress overlay during image, portrait, finalization and payment operations.
- Added one fresh high-accuracy GPS retry for intermittent check-in geofence rejection.
- Increased server-side practical GPS tolerance while preserving geofence enforcement.
- package.json and package-lock.json unchanged.

---

## منبع: `CHANGELOG_V146_SECURITY_EXEMPT_LOGIN_FIX.md`

# v146 — Security exemption login fix

- Login now authenticates the exact user record before applying device security policy.
- `security_exempt=1` bypasses VPN, Developer Options, mock location and GPS login blocks.
- Runtime SecurityGuard, GpsGuard and PermissionGuard continue to bypass restrictions for exempt users.
- User update endpoint ensures and normalizes the `security_exempt` column and returns the stored value.
- Password is removed from security-block activity logs.
- No SDK, Expo, React Native, Gradle, plugin, package.json or package-lock.json changes.

---

## منبع: `CHANGELOG_V147_BALE_PAYMENT_REAR_PORTRAIT_FIX.md`

# V147

- پرداخت کیف پول بله از کلید عمومی اعلان‌های بله مستقل شد؛ وجود توکن بازو برای sendInvoice کافی است.
- وب‌هوک پرداخت حتی در صورت خاموش بودن اعلان‌های عمومی، pre_checkout_query و successful_payment را پردازش می‌کند.
- وضعیت آماده‌بودن پرداخت از توکن بازو و Provider واقعی محاسبه می‌شود و پرچم خطای قدیمی باعث مخفی‌شدن روش پرداخت نمی‌شود.
- ورود کاربر به بازو با لینک start و ارسال صورتحساب پس از شروع بازو پشتیبانی می‌شود.
- عکس پرسنلی درخواست شرکت با دوربین پشت گرفته می‌شود و کادر چهره حذف شد.
- صحت‌سنجی حضور همچنان از دوربین جلو و راهنمای چهره استفاده می‌کند.
- SDK، Expo، React Native، Gradle، پلاگین‌ها، package.json و package-lock.json تغییر نکردند.

---

## منبع: `CHANGELOG_V147_BALE_WALLET_BOT_PAYMENT_FIX.md`

# v147 — Bale wallet and bot payment flow

- Payment no longer requires a previously linked Bale chat.
- A secure one-time start token is generated and appended to the Bale bot link.
- Opening the bot links the authenticated app user to the chat and sends the invoice.
- The app always shows the Bale wallet method with a precise readiness reason when disabled.
- Payment is finalized only after pre_checkout_query approval and successful_payment webhook.
- No SDK, Expo, React Native, Gradle, plugin, package.json, or package-lock.json changes.

---

## منبع: `CHANGELOG_V148_BALE_PAYMENT_LINK_FACE_GUIDE_FIX.md`

# v148
- اصلاح کادر بیضی عکس پرسنلی و صحت‌سنجی روی پیش‌نمایش واقعی دوربین
- استفاده از دوربین جلو برای افزودن/تغییر تصویر پرسنلی
- ایجاد لینک اختصاصی پرداخت بله برای هر درخواست
- ارسال لینک پرداخت با پیامک و ربات بله
- باقی ماندن مسیر پرداخت حتی در صورت خطای مستقیم sendInvoice
- بدون تغییر package.json، package-lock.json، SDK، Expo، React Native و Gradle

---

## منبع: `CHANGELOG_V149_PROFILE_PHOTO_FORMDATA_FIX.md`

# V149 — Profile photo upload fix

- Fixed `Unsupported FormDataPart implementation` while changing personnel photo.
- Replaced multipart FormData upload for this flow with the existing JSON/base64 backend path.
- Added the shared front-camera personnel capture screen with centered oval face guide.
- App version: 1.3.23 / versionCode 10323.
- Site version: 120.
- SDK, Expo, React Native, Gradle, plugins, package.json and package-lock.json were not changed.

---

## منبع: `CHANGELOG_V150_BALE_FAST_LINK_SECURE_MEDIA.md`

# V150

- جلوگیری از timeout هنگام ایجاد پرداخت کیف پول بله؛ مسیر ایجاد پرداخت فقط رکورد و لینک اختصاصی را می‌سازد و ارسال صورتحساب پس از بازشدن لینک در ربات انجام می‌شود.
- اصلاح نمایش پیوست‌های درخواست شرکت از مسیر امن `/api/media` به‌جای URL مستقیم و ناموجود uploads.
- ذخیره refresh token در پنل وب و تمدید خودکار access token در پاسخ 401.
- افزودن توکن جاری به URL تصاویر پنل برای تگ img.
- افزایش نسخه سایت به 121 و اپ به 1.3.24 / versionCode 10324.
- بدون تغییر SDK، Expo، React Native، Gradle، پلاگین‌ها، package.json و package-lock.json.

---

## منبع: `CHANGELOG_V151_FACE_PAYMENT_SUBSCRIPTION_FIX.md`

# v151
- اصلاح قطعی کادر بیضی با لایه دارای zIndex/elevation و اندازه‌گیری واقعی صحنه دوربین
- تعریف تابع sendBalePaymentLink و جلوگیری از کرش ReferenceError
- همگام‌سازی idempotent وضعیت پرداخت موفق بله با درخواست شرکت و ترمیم خودکار در payment-status
- پشتیبانی شناسه‌های تراکنش بله
- افزودن اشتراک معمولی/گروهی/انفرادی، پرداخت ۳۰ روزه با ربات بله، وضعیت و روزهای باقی‌مانده در اپ
- افزودن تنظیمات اشتراک در پنل وب
- بدون تغییر package.json/package-lock.json و نسخه‌های SDK/Expo/RN/Gradle/plugins

---

## منبع: `CHANGELOG_V152_SUBSCRIPTION_COMPLETE.md`

# v152 — اشتراک گروهی و انفرادی

- تنظیم اشتراک از مسیر پنل مدیریت ← تنظیمات ← زبانه «اشتراک».
- سه حالت: استفاده معمولی، اشتراک گروهی، اشتراک انفرادی.
- مبلغ مستقل برای اشتراک گروهی و مبلغ هر فرد برای اشتراک انفرادی.
- ایجاد رکورد پرداخت و لینک ورود به ربات بله برای دریافت صورتحساب.
- فعال‌سازی ۳۰ روزه پس از دریافت successful_payment معتبر از وب‌هوک بله.
- نمایش نوع اشتراک، وضعیت، تاریخ انقضا و روزهای باقی‌مانده در اپ.
- محدودکردن دسترسی اپ پس از انقضا و نمایش مستقیم صفحه تمدید.
- بررسی دوره‌ای وضعیت پرداخت و فعال‌شدن خودکار دسترسی پس از پرداخت.
- جداول دیتابیس در upgrade.php و فایل مستقل php/db/upgrade_v152_subscription.sql.
- نسخه اپ 1.3.26 و versionCode 10326؛ نسخه پنل v152.
- SDK، Expo، React Native، Gradle، پلاگین‌ها، package.json و package-lock.json تغییر نکرده‌اند.

---

## منبع: `CHANGELOG_V153_SUBSCRIPTION_SETTINGS_VISIBLE_FIX.md`

# v153 - رفع خالی بودن تب اشتراک در تنظیمات

- علت: تب اشتراک به فهرست زبانه‌ها و محتوای JSX اضافه شده بود، اما selector مربوط به `tab-subscription` در CSS وجود نداشت؛ بنابراین همه پنل‌های تب پنهان می‌ماندند.
- اصلاح: selector زیر به CSS افزوده شد: `.settings-tabs.tab-subscription .t-subscription`
- cache-busting فایل‌های پنل به نسخه 153 افزایش یافت.
- هیچ تغییری در SDK، Expo، React Native، Gradle، پلاگین‌ها، package.json یا package-lock.json انجام نشد.

---

## منبع: `CHANGELOG_V155_COMPANY_REQUEST_SECURE_MEDIA_FIX.md`

# V155 — Company request secure media fix

- Company-request thumbnails and files no longer use direct `/uploads/...` URLs.
- All preview and full-size links now use authenticated `/api/media?path=...` URLs.
- The current access token is appended for `<img>` preview requests.
- Thumbnail fallback order: thumbnail -> processed file -> original file path.
- No SDK, Expo, React Native, Gradle, plugin, package.json, or package-lock.json changes.

---

## منبع: `CHANGELOG_V156_SUBSCRIPTION_SELFIE_AUDIT_FIX.md`

# V156 — اصلاح قطعی اشتراک و دوربین سلفی

- یکسان‌سازی و سازگاری تنظیمات اشتراک با کلیدهای نسخه‌های قبلی.
- فعال تلقی‌شدن حالت گروهی/انفرادی حتی در صورت ناسازگاری checkbox قدیمی.
- نمایش نوع و روزهای باقی‌مانده اشتراک روی کارت داشبورد.
- بازنویسی کامل دوربین سلفی بدون کادر، ماسک و برش اجباری.
- پیش‌نمایش کامل تصویر با resizeMode=contain و ثبت تک‌مرحله‌ای Base64.
- مدیریت خطای وضعیت اشتراک بدون تبدیل اشتباه خطای شبکه به «استفاده معمولی».
- بررسی نحوی PHP و JavaScript و حفظ package.json و package-lock.json.

---

## منبع: `CHANGELOG_V157_SUBSCRIPTION_NOTICE_VPN_FIX.md`

# V157
- ثبت تذکر تصویری از multipart به JSON/Base64 منتقل شد تا خطای Unsupported FormDataPart implementation حذف شود.
- مقدار send_bot به‌درستی به API ارسال می‌شود.
- تشخیص VPN فقط بر اساس سیگنال قطعی سیستم‌عامل انجام می‌شود؛ کشور IP دیگر به‌تنهایی هشدار VPN ایجاد نمی‌کند.
- ذخیرهٔ تنظیمات اشتراک جزئی دیگر حالت گروهی/انفرادی را ناخواسته به استفاده معمولی برنمی‌گرداند.
- پاسخ وضعیت اشتراک با حالت مؤثر سرور برگردانده می‌شود.
- SDK، Expo، React Native، Gradle، پلاگین‌ها، package.json و package-lock.json بدون تغییر باقی مانده‌اند.

---

## منبع: `CHANGELOG_V158_JPEG_PIPELINE_CLEANUP.md`

# V158 — یکپارچه‌سازی JPG و پاک‌سازی رسانه

- همه تصاویر ارسالی موبایل و وب پیش از ارسال به JPEG تبدیل می‌شوند.
- کیفیت و حداکثر عرض از تنظیمات `image_quality` و `image_max_width` خوانده می‌شود.
- ذخیره سمت سرور نیز تنظیمات پنل را اعمال می‌کند و خروجی تصویر را با پسوند `.jpg` نگه می‌دارد.
- مدارک شرکت، رسید پرداخت، عکس پروفایل، حضور، تذکر، گزارش، چک‌لیست، بازدید، سلفی و اسکرین‌شات تحت همین سیاست قرار گرفتند.
- فایل PDF همچنان PDF باقی می‌ماند و مشمول تبدیل تصویر نیست.
- پاک‌سازی دستی و Cron فایل فیزیکی و ارجاع دیتابیس را هم‌زمان حذف می‌کند.
- گزارش پاک‌سازی شامل تعداد فایل‌های حذف‌شده، رکوردهای پاک‌شده و خطاهای هر بخش است.
- نگهداری جداگانه تصاویر مدارک شرکت با کلید `company_request_retention_days` افزوده شد.
- SDK، Expo، React Native، Gradle، پلاگین‌ها، package.json و package-lock.json تغییر نکرده‌اند.

---

## منبع: `CHANGELOG_V159_AUDIT_SETTINGS_NOTICE_SYNC.md`

# V159
- الزام عکس تذکر در اپ و سرور
- تازه‌سازی اجباری تنظیمات هنگام ورود به ثبت تذکر
- حذف چشمک‌زدن نوار همگام‌سازی و کاهش تلاش‌های تکراری
- نمایش تک‌خطی نام آیتم‌های راننده
- ذخیره فقط تنظیمات تغییرکرده در پنل با مدیریت timeout
- رفع کش نسخه پنل
- نسخه اپ 1.3.30 و سایت 124

---

## منبع: `CHANGELOG_V160_COMPANY_REQUEST_STATUS_DELETE_FIX.md`

# V160 - Company request status and delete fix

- Added explicit validation for rejection/cancellation notes in the admin panel.
- Normalized legacy and localized status values in the API.
- Added a visible admin delete button with confirmation.
- Delete removes request, files, logs and payment records through the existing secure endpoint.
- Updated panel build/cache marker to v160.
- No SDK, Expo, React Native, Gradle, plugin, package.json or package-lock.json changes.



## v162 — حذف OCR ناموفق، نقشه آفلاین واقعی و یکپارچه‌سازی پایش
- آیتم‌های اسکن پلاک و کارت ملی از اپ حذف شدند؛ جستجوی دستی کد ملی و پلاک باقی است.
- وابستگی و پلاگین ML Kit OCR و فایل‌های Scanner/OCR حذف شدند.
- تنظیمات پلاک‌خوان از پنل تنظیمات حذف شد.
- نقشه آفلاین پس از دانلود، تایل‌ها را از حافظه داخلی خوانده و در صفحه حضور نمایش می‌دهد.
- آزمون سلامت نقشه آفلاین به تنظیمات نقشه افزوده شد.
- دوره بررسی GPS و VPN مستقیماً از تنظیمات سایت دریافت می‌شود.
- دوره‌های GPS/VPN/GSM در API تنظیمات اپ منتشر شدند.
- منوی اپ در تب‌های میدانی، عملیات، پیام‌ها، شخصی و سایر دسته‌بندی شد.
- BUILD-ONE-CLICK-README.txt حذف شد.


## v163
- جستجوی کد ملی و پلاک در یک صفحه و با دو کادر مستقل.
- افزوده‌شدن گزارش ماهانه عملکرد تاکسیران و ریز حضورهای هر ماه.
- افزوده‌شدن مشاهده، خروجی و حذف فیزیکی تذکرات تاکسیران.
- تفکیک کامل مجوزهای منوی سایت از آیتم‌های اپ.
- بازآرایی گروه‌بندی منوهای سایت و تب‌های اپ.
- استفاده از آیکون‌های Icons8 3D Fluency با fallback داخلی.

## v191 - موتور مأموریت و الگوهای سمت
- افزودن Mission Engine درصدی برای ناظر خط، گشت موتوری، بازرس مقیم، بازرس گشت خودرویی و سربازرس.
- افزودن الگوهای روزانه/هفتگی/ماهانه و Override اختصاصی کاربر.
- افزودن پنل مدیریت مأموریت‌ها و صفحه موبایل «مأموریت روزانه من».
- افزودن Migration و APIهای مدیریت مأموریت.

## v192.1 — اصلاح سازگاری Import دیتابیس
- اضافه شدن Migrationهای مستقل و idempotent برای MySQL/MariaDB و phpMyAdmin.
- حذف syntaxهای اختصاصی PostgreSQL از نسخه MySQL: JSONB، ::jsonb، TIMESTAMPTZ، BIGSERIAL، DO $$ و ON CONFLICT.
- اضافه شدن فایل تجمیعی `db/mysql/upgrade_v191_v192_all_mysql.sql`.
- حفظ Migrationهای PostgreSQL برای جلوگیری از شکستن نصب‌های موجود.
- اضافه شدن راهنمای فارسی تشخیص و اجرای صحیح Migration بر اساس موتور دیتابیس.
