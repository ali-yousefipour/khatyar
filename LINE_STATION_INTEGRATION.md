# یکپارچه‌سازی ثبت موقعیت و مشخصات ایستگاه

آخرین بروزرسانی: 1405/06/05 — 2026-08-26

## جریان جدید Android

- «ثبت مشخصات ایستگاه» به‌عنوان آیتم مستقل داشبورد و تحت کنترل «آیتم‌های اپ» هر سمت ارائه می‌شود؛ دیگر بخشی از «وضعیت ایمنی و موقعیت دستگاه» نیست.
- «ایستگاه‌های ثبت‌شده من» نیز آیتم مستقل داشبورد است و کاربر از همان مسیر می‌تواند سوابق خود را مشاهده و ویرایش کند.
- مرحله ۱: دریافت چند نمونه GPS با بالاترین دقت. اگر accuracy بیشتر از ۲۰ متر باشد، ثبت دستی فعال می‌شود؛ کاربر می‌تواند نشانگر را Drag کند یا با ضربه روی نقشه نقطه را تعیین کند.
- در حالت دستی پس از جابه‌جایی نقشه، پنج خط نزدیک به مختصات جدید دوباره محاسبه می‌شوند.
- مرحله ۲: پنج خط نزدیک ابتدا نمایش داده می‌شوند؛ سایر خطوط با جستجو قابل انتخاب‌اند. شناسه خط در رابط کاربر نمایش یا دریافت نمی‌شود.
- گزینه «خط در فهرست وجود ندارد یا غیرفعال است» فقط عنوان خط را دریافت می‌کند و شناسه داخلی برای کاربر نمایش داده نمی‌شود.
- مرحله ۳: افزودن تابلو با دکمه +، انتخاب نوع تابلو، باز شدن دوربین، پیش‌نمایش و تأیید عکس و سپس سؤال برای افزودن تابلوی بعدی انجام می‌شود.
- مرحله ۴: تصویر محل ایستگاه با دوربین گرفته، پیش‌نمایش و تأیید می‌شود.
- مرحله ۵: فقط آدرس کامل محل ایستگاه دریافت می‌شود و فیلد جداگانه خیابان وجود ندارد.
- مرحله ۶: خلاصه کامل خط، مختصات، آدرس، تابلوها و تصاویر نمایش داده و ثبت نهایی فقط پس از تکمیل همه مراحل ممکن است.

## Backend

- `station-wizard-v3.php` و API سازگار با MySQL/MariaDB هستند و از `JSONB`، `::jsonb` یا `TIMESTAMPTZ` استفاده نمی‌کنند.
- زمان ذخیره‌سازی با timezone `Asia/Tehran` انجام می‌شود و نمایش تاریخ به تقویم شمسی تبدیل می‌شود.
- شناسه خط و شناسه ایستگاه برای کاربر و خروجی Excel جزو اطلاعات نمایشی نیستند؛ ارتباط داخلی DB همچنان با `line_id` انجام می‌شود.
- تصاویر در `/uploads/line-locations/` ذخیره و URL عمومی آن‌ها با `app.yousefipour.ir` سازگار می‌شود.

## Admin / Web

- نقشه خطوط تاکسیرانی همه خطوط دارای مختصات را نمایش می‌دهد؛ خطوط دارای ایستگاه ثبت‌شده سبز و خطوط فاقد ثبت ایستگاه قرمز هستند.
- با انتخاب نشانگر ایستگاه، اطلاعات کامل خط، آدرس، مختصات، accuracy، زمان شمسی تهران، تصویر محل و همه تصاویر تابلوها نمایش داده می‌شود.
- مسیر تصاویر از URL واقعی `app.yousefipour.ir` ساخته می‌شود تا خطای 404 ناشی از مسیر نسبی برطرف شود.
- خروجی Excel به‌صورت XLSX تولید می‌شود و تصویر محل و تصاویر تابلوها داخل فایل قرار می‌گیرند؛ مسیر فایل تصویر در ستون خروجی قرار نمی‌گیرد.

## کنترل دسترسی

- نمایش آیتم‌های ثبت و «ایستگاه‌های ثبت‌شده من» از تنظیمات «آیتم‌های اپ» و نقش کاربر کنترل می‌شود.
- محدودسازی خطوط بر اساس `user_lines` برای کاربران غیرمدیر حفظ شده است.
- مدیران مجاز همچنان به همه خطوط دسترسی دارند.

## موارد باقی‌مانده برای تست Production

- Build واقعی Android و نصب نسخه جدید.
- تست Android 9 و نسخه‌های جدید Android.
- تست GPS دقیق، حالت دستی، Drag و Tap روی نقشه.
- تست کامل دوربین و تأیید تصاویر تابلو/ایستگاه.
- تست ثبت و ویرایش چند ایستگاه.
- تست API و DB واقعی MySQL/MariaDB.
- تست نمایش همه تصاویر در پنل و Excel واقعی.
- تست نقشه همه خطوط در Production و بررسی خطوط فاقد مختصات.
- تست نقش‌ها و تنظیمات آیتم‌های اپ.
