# یکپارچه‌سازی ثبت موقعیت و تصویر خطوط

آخرین بروزرسانی: 1405/06/05 — 2026-08-26

## انجام‌شده

- آیتم مستقل «ثبت موقعیت و تصویر خطوط» از فهرست امکانات داشبورد وب حذف شد و قابلیت داخل «خطوط تاکسیرانی» قرار گرفت.
- صفحه مستقل برای نمایش قبل از ورود استفاده نمی‌شود؛ دسترسی از Token و مجوزهای `can_view` / `can_capture` کنترل می‌شود.
- در بخش «خطوط تاکسیرانی» امکان انتخاب خط، مشاهده آخرین مختصات، دقت GPS، نام ایستگاه و دو تصویر محل/تابلو اضافه شد.
- ثبت موقعیت جدید مستقیماً از بخش خطوط انجام می‌شود.
- GPS با `enableHighAccuracy=true` و `maximumAge=0` درخواست می‌شود.
- Backend ثبت موقعیت را فقط در صورت دریافت accuracy و با حداکثر خطای ۲۰ متر قبول می‌کند.
- مختصات با ۷ رقم اعشار ذخیره می‌شود.
- هر دو تصویر اجباری هستند و قبل از ارسال در مرورگر به JPEG فشرده می‌شوند؛ Backend نیز خروجی نهایی را JPEG ذخیره می‌کند و تنظیمات `LineImageCompressor` را رعایت می‌کند.
- اطلاعات آخرین موقعیت در رکورد همان خط بروزرسانی می‌شود.
- تاریخچه موقعیت‌های همان خط در دسترس کاربران دارای مجوز مشاهده قرار گرفت.
- محدودسازی خطوط بر اساس `user_lines` برای کاربران غیرمدیر حفظ شده است.
- مدیران مجاز همچنان به همه خطوط دسترسی دارند.
- API تاریخچه با درخواست داخلی Web App سازگار شد.

## کنترل دسترسی

- `can_view`: مشاهده اطلاعات و تاریخچه.
- `can_capture`: ثبت یا بروزرسانی موقعیت و تصاویر.
- `can_manage`: مدیریت مجوزهای سمت‌ها.
- مدیریت مجوزها باید از تنظیمات مدیریتی انجام شود و صفحه عمومی مستقلی برای آن نمایش داده نمی‌شود.

## محدودیت دقت GPS

- `accuracy <= 20m`: ثبت مجاز.
- `accuracy > 20m`: ثبت رد می‌شود و کاربر باید موقعیت دقیق‌تری دریافت کند.
- `accuracy` نامشخص: ثبت رد می‌شود.

این کنترل هم در Web App و هم در Backend اعمال شده و صرفاً به اعتبارسنجی سمت مرورگر متکی نیست.

## موارد باقی‌مانده برای تست Production

- تست واقعی روی چند گوشی Android و مرورگرهای Chrome/Samsung Browser.
- تست GPS در فضای باز و بررسی accuracy واقعی.
- تست دوربین و تبدیل JPEG روی تصاویر حجیم.
- تست ثبت در DB واقعی و بررسی تاریخچه.
- تست نقش‌های مختلف و `user_lines`.
- تست مسیرهای Token منقضی/نامعتبر.
- بررسی نمایش نقشه واقعی داخل کامپوننت اصلی خطوط در محیط Production.
- پس از تست موفق، Build و انتشار نسخه نهایی Android.
