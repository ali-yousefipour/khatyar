# راهنمای آپدیت برنامهٔ اندروید (سازگار با ایران)

از آنجا که سرویس‌های اکسپو (EAS Update) و حتی نصب پکیج `expo-updates`
از داخل ایران در دسترس نیستند، از روش **آپدیت با APK روی سرور خودی** استفاده می‌کنیم.
این روش به هیچ سرویس خارجی وابسته نیست.

---

## نحوهٔ کار (خودکار)

برنامه هنگام هر بار باز شدن، نسخهٔ فعلی خود را با نسخهٔ ثبت‌شده روی سرور شما
مقایسه می‌کند. اگر نسخهٔ جدیدتری موجود باشد، به کاربر پیام می‌دهد و لینک
دانلود APK جدید را نشان می‌دهد. هیچ چیزی روی سرورهای خارجی (که در ایران بلاک‌اند)
بررسی نمی‌شود.

---

## مراحل انتشار یک نسخهٔ جدید

### ۱) ساخت APK جدید (روی کامپیوتر ویندوز)

```
cd mobile
npx expo prebuild --clean
cd android
gradlew assembleRelease
```

فایل خروجی:
`mobile/android/app/build/outputs/apk/release/app-release.apk`

> نکته: نسخهٔ `ANDROID_VERSION_NAME` و `ANDROID_VERSION_CODE` را در فایل
> `mobile/.env` قبل از ساخت افزایش دهید.

### ۲) آپلود APK روی هاست

فایل APK را در پوشه‌ای روی هاست خود آپلود کنید، مثلاً:
```
https://app.yousefipour.ir/downloads/taxi-v0.5.20.apk
```

### ۳) ثبت نسخهٔ جدید در پنل

وارد پنل مدیریت شوید → **تنظیمات سامانه** → بخش «نسخهٔ برنامهٔ اندروید»:

- **آخرین نسخه (latest_version):** `0.5.20`
- **حداقل نسخهٔ مجاز (min_version):** اگر می‌خواهید نصب نسخهٔ جدید اجباری باشد،
  این را هم برابر `0.5.20` بگذارید؛ وگرنه کمتر بگذارید تا اختیاری باشد.
- **لینک دانلود APK (apk_url):** آدرس کامل فایل APK که در مرحلهٔ ۲ آپلود کردید.
- **توضیحات (notes):** تغییرات این نسخه (اختیاری).

ذخیره کنید.

### ۴) تمام!

از این پس هر کاربری که برنامه را باز کند:
- اگر نسخه‌اش قدیمی‌تر از `latest_version` باشد → پیام آپدیت + دکمهٔ دانلود
- اگر نسخه‌اش از `min_version` کمتر باشد → آپدیت **اجباری** (تا نصب نکند نمی‌تواند ادامه دهد)

---

## دکمهٔ بررسی دستی

کاربران می‌توانند از مسیر **حساب کاربری → 🔄 بررسی آپدیت** هم به‌صورت دستی
نسخهٔ جدید را بررسی کنند.

---

## چرا OTA اکسپو استفاده نشد؟

روش OTA اکسپو (`expo-updates` + EAS) نیازمند اتصال به سرورهای `expo.dev` و
`u.expo.dev` است که از ایران فیلتر هستند. حتی نصب خود پکیج هم با خطای شبکه
مواجه می‌شود. روش APK خودی هیچ وابستگی خارجی ندارد و کاملاً پایدار است.
