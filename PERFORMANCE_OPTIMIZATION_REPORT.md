# گزارش بهینه‌سازی سرعت بارگذاری سایت و نرم‌افزار

## تغییرات اعمال‌شده

### 1) فشرده‌سازی و کش فایل‌های ثابت
- فعال‌سازی Gzip در Apache/PHP با `.htaccess` و `ob_gzhandler` برای هاست‌هایی که فشرده‌سازی را خودکار فعال نکرده‌اند.
- فعال‌سازی فشرده‌سازی در بک‌اند Node با `compression`.
- افزودن `Cache-Control`, `ETag`, `Last-Modified` برای فایل‌های ثابت مانند JS/CSS/تصاویر/فونت‌ها.
- HTML با `no-cache` سرو می‌شود تا بعد از آپدیت، نسخه جدید پنل دیده شود.

### 2) کاهش درخواست‌های تکراری در پنل وب
- اضافه‌شدن کش کوتاه‌مدت سمت مرورگر برای داده‌های کم‌تغییر مانند تنظیمات عمومی، خطوط، نقش‌ها، شیفت‌ها، تعطیلات و قالب چک‌لیست.
- پاک‌سازی خودکار کش بعد از عملیات‌های POST/PUT/DELETE تا اطلاعات قدیمی نمایش داده نشود.

### 3) بهینه‌سازی اپ موبایل
- تقویت کش حافظه‌ای و AsyncStorage برای نمایش سریع صفحات تکراری.
- استفاده از الگوی نمایش سریع داده کش‌شده و سپس تازه‌سازی شبکه‌ای در بخش‌هایی که قبلاً پشتیبانی می‌کردند.
- جلوگیری از قفل‌شدن برنامه در اتصال کند با timeout موجود حفظ شد.

### 4) بهینه‌سازی دیتابیس
- اضافه‌شدن ایندکس‌های مهم برای گزارش حضور و غیاب، کارکرد ماهانه، شیفت، حضور خودکار کاربران، اعلان‌ها، لاگ‌ها و موقعیت زنده.
- این ایندکس‌ها مخصوصاً روی گزارش‌های ماهانه و صفحات مدیریتی پرتکرار اثر مستقیم دارند.

### 5) تنظیمات اتصال دیتابیس
- فعال‌سازی persistent connection در PDO برای کاهش هزینه اتصال‌های مکرر روی هاست‌های پرترافیک.
- تنظیم اولیه charset و timezone در لحظه اتصال.

## نکات نصب
- بعد از آپلود نسخه جدید، فایل `php/db/upgrade.sql` را یک‌بار روی دیتابیس اجرا کنید تا ایندکس‌های جدید ساخته شوند.
- برای بک‌اند Node، بعد از جایگزینی فایل‌ها دستور `npm install` را اجرا کنید تا پکیج `compression` نصب شود.
- در هاست Apache مطمئن شوید ماژول‌های `mod_deflate`, `mod_expires`, `mod_headers` فعال باشند.

## نتیجه مورد انتظار
- کاهش حجم انتقال صفحات و APIهای متنی.
- سرعت بیشتر در باز شدن پنل و اپ پس از ورود اول.
- سرعت بهتر گزارش‌های کارکرد، حضور و غیاب، اعلان‌ها و موقعیت زنده.
