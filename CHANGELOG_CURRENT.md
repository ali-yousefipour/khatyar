# تغییرات جاری — 2026-08-26

## نسخه 1.3.73 — اصلاح نهایی تصاویر، تاریخ، نقشه، Excel و دسترسی ثبت ایستگاه
- مسیر جدید `station-image-v4.php` برای تحویل احراز هویت‌شده تصاویر ایستگاه و تابلوها اضافه شد.
- برای سازگاری با خروجی‌های قبلی پنل، `station-image-url-fix.js` مسیر تصاویر قدیمی را به endpoint جدید تبدیل می‌کند.
- تاریخ تاریخچه موقعیت‌ها با `Asia/Tehran` و تقویم فارسی/Jalali به‌صورت اجباری اصلاح شد.
- نقشه نهایی خطوط از `station-capture-report.php?op=map` استفاده می‌کند و همه خطوط دارای مختصات را نمایش می‌دهد.
- برای خطوطی که ایستگاه جدید ندارند، آخرین مختصات قدیمی خط با نشانگر قرمز نمایش داده می‌شود.
- خطوط دارای ایستگاه ثبت‌شده با نشانگر سبز نمایش داده می‌شوند.
- Popup نقشه RTL و با فونت Vazirmatn است و مشخصات کامل ایستگاه، تابلوها، تصاویر و زمان ثبت را نمایش می‌دهد.
- تصاویر Popup نقشه و تاریخچه از endpoint احراز هویت‌شده خوانده می‌شوند.
- خروجی Excel جدید `line-location-export-v4.php` ایجاد شد.
- تصاویر محل ایستگاه و تمام تصاویر تابلوها داخل XLSX Embed می‌شوند، نه به‌صورت URL.
- اندازه ستون‌های تصاویر و ارتفاع ردیف‌ها متناسب با ابعاد تصاویر تنظیم می‌شود.
- ستون «نمایش ایستگاه روی نقشه» به Excel اضافه شد و برای مختصات معتبر، لینک Google Maps به‌صورت Hyperlink واقعی درج می‌شود.
- Cache-Busting پنل برای Assetهای اصلاحی افزایش داده شد.

## دسترسی مستقل «📍 ثبت ایستگاه‌ها»
- تنظیم دسترسی ثبت ایستگاه از تنظیمات قدیمی «مجوز ثبت موقعیت و تصویر خطوط» جدا شد.
- API مستقل `api/station-permissions.php` برای کنترل این قابلیت بر اساس `role_id` ایجاد/تکمیل شد.
- در پنل مدیریت، کارت «📍 ثبت ایستگاه‌ها» زیر بخش آیتم‌های اپ نمایش داده می‌شود و برای تمام سمت‌ها Checkbox مستقل دارد.
- ذخیره تنظیمات جدید هم Android و هم Web App را برای همان سمت کنترل می‌کند.
- برای سازگاری با نسخه‌های نصب‌شده Android که هنوز `/my/app-items` را مصرف می‌کنند، تنظیم جدید پس از ذخیره به‌صورت داخلی در داده legacy نیز Mirror می‌شود؛ منبع تنظیمات مدیر همچنان `station_app_permissions` است و تنظیم قدیمی UI استفاده نمی‌شود.
- آیتم‌های «ثبت موقعیت و تصویر خطوط» و «ایستگاه‌های ثبت‌شده من» با همین مجوز مستقل کنترل می‌شوند.
- مجوز ثبت ایستگاه هنگام ثبت نهایی نیز از همین مسیر مستقل قابل اعمال است.

## فرآیند ثبت مشخصات ایستگاه
- ویزارد ثبت ایستگاه شش‌مرحله‌ای است: موقعیت، انتخاب خط، تابلوها، تصویر محل، آدرس و تأیید نهایی.
- دریافت GPS با چند نمونه و بالاترین دقت انجام می‌شود؛ در accuracy بیشتر از ۲۰ متر، ثبت دستی با Drag/Click روی نقشه فعال است.
- پنج خط نزدیک در مرحله انتخاب خط نمایش داده می‌شوند و سایر خطوط با جستجو قابل انتخاب هستند.
- شناسه خط و شناسه ایستگاه در رابط کاربر و خروجی Excel نمایش داده نمی‌شوند.
- برای خط خارج از فهرست/غیرفعال فقط عنوان خط دریافت می‌شود.
- افزودن تابلو به‌صورت مرحله‌ای با انتخاب نوع، دوربین، پیش‌نمایش و تأیید تصویر و سؤال برای تابلوی بعدی انجام می‌شود.
- تصویر محل ایستگاه نیز قبل از ادامه مرحله تأیید می‌شود.
- مرحله آدرس فقط یک فیلد آدرس کامل دارد.
- ثبت نهایی فقط پس از تکمیل همه اطلاعات و تصاویر امکان‌پذیر است.

## نسخه
- نسخه سایت و Android روی **1.3.73** یکسان نگه داشته شده‌اند.
- Version Code Android برابر **10373** است.

## وضعیت تست و استقرار
- اصلاحات کد و Commitها در GitHub ثبت شده‌اند.
- Build واقعی Android، تست Android 9، تست Production API/DB، تست دوربین/GPS واقعی و تست واقعی XLSX هنوز باید روی محیط اجرایی انجام شوند.
- Repository Workflow اختصاصی Deploy مستقیم به `app.yousefipour.ir` ندارد؛ بنابراین فایل‌های جدید باید در Document Root مربوط به `app.yousefipour.ir` یعنی `php/app/` مستقر شوند.
- خطای `security-lock.png: 404` در جستجوی کد پروژه هیچ ارجاعی ندارد و به همین دلیل به‌عنوان Asset پروژه قابل ردیابی نیست؛ باید در صورت باقی‌ماندن پس از Deployment از Network Initiator مرورگر بررسی شود.
