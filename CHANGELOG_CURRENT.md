# تغییرات جاری — 2026-08-26

## نسخه 1.3.73 — اصلاح تصاویر و تاریخ ایستگاه
- نمایش تصاویر ایستگاه و تابلو در Admin از مسیر مستقیم `/uploads` خارج شد و از endpoint احراز هویت‌شده استفاده می‌کند.
- دسترسی مستقیم به `uploads/line-locations` در `.htaccess` مسدود شد تا تصاویر بدون مجوز عمومی نشوند.
- endpoint جدید `api/station-image.php` برای تصویر محل ایستگاه و تصاویر تابلو ایجاد شد.
- endpoint جدید `api/station-details.php` جزئیات ایستگاه و تمام تابلوهای آن را برای پنل برمی‌گرداند.
- مشکل 403 تصاویر در تاریخچه موقعیت و Popup ایستگاه اصلاح شد؛ درخواست‌های `<img>` با JWT در query نیز پشتیبانی می‌شوند.
- `station-image-v3.php` و نقشه ایستگاه نیز برای نمایش امن تصاویر در Popup اصلاح شدند.
- تاریخ و ساعت تاریخچه موقعیت‌ها اکنون با timezone `Asia/Tehran` و تقویم فارسی/Jalali نمایش داده می‌شود.
- تبدیل تاریخ محلی دیتابیس به Jalali اصلاح شد تا ساعت تهران یک مرحله جابه‌جا نشود.
- Cache-Busting فایل‌های پنل برای دریافت نسخه جدید Bridge و Station Map فعال شد.

## فرآیند ثبت مشخصات ایستگاه
- «ثبت مشخصات ایستگاه» در داشبورد Android به‌صورت آیتم مستقل و تحت کنترل «آیتم‌های اپ» هر سمت قرار گرفت.
- «ایستگاه‌های ثبت‌شده من» به‌صورت آیتم مستقل داشبورد اضافه/همسان شد و امکان ویرایش سوابق کاربر فراهم است.
- ویزارد ثبت ایستگاه شش‌مرحله‌ای است: موقعیت، انتخاب خط، تابلوها، تصویر محل، آدرس و تأیید نهایی.
- دریافت GPS با چند نمونه و بالاترین دقت انجام می‌شود؛ در accuracy بیشتر از ۲۰ متر، ثبت دستی با Drag/Click روی نقشه فعال است.
- پس از تعیین دستی موقعیت، پنج خط نزدیک دوباره محاسبه می‌شوند.
- پنج خط نزدیک در مرحله انتخاب خط نمایش داده می‌شوند و سایر خطوط با جستجو قابل انتخاب هستند.
- شناسه خط و شناسه ایستگاه در رابط کاربر و خروجی Excel نمایش داده نمی‌شوند.
- برای خط خارج از فهرست/غیرفعال فقط عنوان خط دریافت می‌شود.
- افزودن تابلو به‌صورت مرحله‌ای با انتخاب نوع، دوربین، پیش‌نمایش و تأیید تصویر و سؤال برای تابلوی بعدی انجام می‌شود.
- تصویر محل ایستگاه نیز قبل از ادامه مرحله تأیید می‌شود.
- مرحله آدرس فقط یک فیلد آدرس کامل دارد و فیلد خیابان جداگانه حذف شده است.
- ثبت نهایی فقط پس از تکمیل همه اطلاعات و تصاویر امکان‌پذیر است.

## Admin / Web
- نقشه خطوط تاکسیرانی همه خطوط را نمایش می‌دهد؛ خطوط دارای ثبت ایستگاه سبز و خطوط فاقد ثبت ایستگاه قرمز هستند.
- Popup نقشه اطلاعات کامل ایستگاه، آدرس، مختصات، دقت GPS، زمان شمسی تهران و تصاویر محل/تابلوها را نمایش می‌دهد.
- تاریخچه موقعیت‌های هر خط زمان ثبت را به شمسی و تهران نمایش می‌دهد.
- نمایش تصاویر در تاریخچه و جزئیات ایستگاه از مسیر امن و احراز هویت‌شده انجام می‌شود.
- Cache-Busting برای Assetهای اصلاح‌شده پنل اضافه شد تا نسخه قدیمی با `PANEL BUILD v213` از Cache مرورگر استفاده نشود.

## Backend
- `api/station-image.php` برای تحویل امن تصاویر ایجاد شد.
- `api/station-details.php` برای دریافت مشخصات کامل ایستگاه و تابلوها ایجاد شد.
- ساختار DB با MySQL/MariaDB سازگار نگه داشته شد.
- دسترسی خطوط بر اساس نقش و `user_lines` کنترل می‌شود.

## نسخه
- نسخه سایت و Android روی **1.3.73** یکسان نگه داشته شده‌اند.
- Version Code Android برابر **10373** است.

## وضعیت تست
- اصلاحات کد و Commitها در GitHub ثبت شده‌اند.
- Build واقعی Android، تست Android 9، تست Production API/DB، تست دوربین/GPS واقعی، تست Excel واقعی و تست کامل پنل باید در محیط اجرایی انجام شوند.
- Repository در حال حاضر Workflow اختصاصی Deploy به `app.yousefipour.ir` ندارد؛ بنابراین جایگزینی فایل‌های اصلاح‌شده روی سرور Production باید توسط فرآیند استقرار سرور انجام شود.
