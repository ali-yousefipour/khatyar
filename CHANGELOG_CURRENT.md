# تغییرات جاری — 2026-08-26

## نسخه 1.3.71 — همسان‌سازی نسخه و آماده‌سازی استقرار یکپارچه
- نسخه مرجع Android، Web App، Site/Admin و Backend به `1.3.71` منتقل شد.
- `panel.bundle.js`، CSS و assetهای پنل از طریق cache-busting با همان نسخه بارگذاری می‌شوند.
- Badge نسخه پنل نیز از همان مقدار نسخه Android استفاده می‌کند.
- workflow همسان‌سازی نسخه طوری تنظیم شد که پس از تغییرات پروژه، نسخه مرجع را بین Android، Backend و پنل هماهنگ و قبل از commit اعتبارسنجی کند.
- `versionCode` پیش‌فرض Android برای نسخه `1.3.71` برابر `10371` تنظیم شد.
- استقرار PHP همچنان از `php/` انجام می‌شود و تغییرات Backend پس از commit توسط workflow استقرار به هاست ارسال می‌شوند.

## اصلاحات اخیر ثبت ایستگاه‌ها
- ثبت ایستگاه به‌عنوان آیتم مستقل اپ (`LineLocation`) باقی می‌ماند و کنترل نمایش آن از تنظیمات «آیتم‌های اپ» انجام می‌شود، نه مجوز قدیمی ثبت موقعیت.
- منطق ۲۰ متر دقت GPS و شعاع ۱۰۰ متری geofence حضور در Backend حفظ شده و باید در تست Production End-to-End تأیید شود.
- نوع تابلوها و خطوط باید از داده‌های موجود سامانه و APIهای فعلی خوانده شوند و ایجاد خط غیرفعال فقط در حالت «خط وجود ندارد/غیرفعال است» انجام شود.

## وضعیت تست
- ساختار کد و همسانی نسخه در مخزن بررسی و اصلاح شد.
- Build واقعی Android، اجرای Migration روی MySQL/MariaDB Production، تست Chrome/Android واقعی و ارسال واقعی فایل به ربات‌ها هنوز نیازمند اجرای محیطی هستند.
- موفقیت Deploy واقعی هاست فقط پس از مشاهده اجرای workflow و بررسی Production قابل تأیید است.
