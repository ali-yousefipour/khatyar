# تغییرات جاری — 2026-08-27

## نسخه 1.3.75 — بی‌سیم نهایی، مدیریت کانال و امنیت عضویت
- معماری بی‌سیم نهایی شد: PTT فقط در Android و مدیریت کانال‌ها در Admin Web.
- چهار نوع کانال در Backend پشتیبانی می‌شود: منطقه‌ای، اعضای انتخابی، سمت‌محور و ترکیبی/سفارشی.
- منطق عضویت `OR` و `AND` اضافه شد؛ عضویت در هر درخواست سمت سرور مجدداً ارزیابی می‌شود.
- PTT تک‌گوینده با lease اتمیک، حداکثر زمان صحبت قابل تنظیم و آزادسازی پس از پایان پیام پیاده‌سازی شد.
- Chirp شروع و پایان پیام صوتی در Android فعال است.
- وقتی بی‌سیم روشن باشد، Provider سراسری Android در همه Screenها پیام‌های کانال مجاز را Poll و پخش می‌کند؛ با خاموش شدن، دریافت صوتی متوقف می‌شود.
- Presence، کاربران آنلاین، گوینده فعلی و Log ارتباطات اضافه شد.
- فایل صوتی فقط از endpoint احراز هویت‌شده و پس از بررسی عضویت قابل دریافت است.
- صفحه `php/app/radio-admin.html` برای ایجاد، ویرایش، حذف، فعال/غیرفعال‌سازی و تنظیم قوانین کانال ایجاد شد.
- انتخاب منطقه، کاربر، سمت، AND/OR، حداکثر زمان صحبت و اولویت در Admin فراهم شد.
- مسیرهای `/api/radio-api-v2.php` و `/api/radio-admin-api.php` به `.htaccess` اضافه شدند.
- Migration جدید `2026_08_27_radio_v2.sql` با سازگاری MySQL/MariaDB و اجرای مجدد مقاوم اضافه شد.
- نسخه Android، Web و Admin به **1.3.75** افزایش یافت.
- Android Version Code به **10375** افزایش یافت.

## نسخه 1.3.73 — اصلاح نهایی منوی خطوط، نقشه و Excel
- اصلاحات منوی خطوط، نقشه، ایستگاه و خروجی Excel طبق نسخه قبلی حفظ شده‌اند.

## وضعیت استقرار
- تغییرات روی GitHub شاخه `main` ثبت شده‌اند.
- Migration `php/migrations/2026_08_27_radio_v2.sql` باید روی DB Production اجرا شود.
- برای استفاده Admin، فایل‌های `radio-admin.html` و `radio-admin.js` و `radio-admin-api.php` همراه با `.htaccess` مستقر شوند.
- برای Android باید Build جدید `1.3.75 (10375)` تولید و روی حداقل دو دستگاه برای تست همزمان PTT نصب شود.
