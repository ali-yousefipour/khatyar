# تغییرات جاری — 2026-08-26

## اصلاح و تکمیل فرآیند ثبت مشخصات ایستگاه
- «ثبت مشخصات ایستگاه» در داشبورد Android به‌صورت آیتم مستقل و تحت کنترل «آیتم‌های اپ» هر سمت قرار گرفت.
- «ایستگاه‌های ثبت‌شده من» به‌صورت آیتم مستقل داشبورد اضافه/همسان شد و امکان ویرایش سوابق کاربر فراهم است.
- ویزارد ثبت ایستگاه به جریان شش‌مرحله‌ای تبدیل شد: موقعیت، انتخاب خط، تابلوها، تصویر محل، آدرس و تأیید نهایی.
- دریافت GPS با چند نمونه و بالاترین دقت انجام می‌شود؛ در accuracy بیشتر از ۲۰ متر، ثبت دستی با Drag/Click روی نقشه فعال است.
- پس از تعیین دستی موقعیت، پنج خط نزدیک دوباره محاسبه می‌شوند.
- پنج خط نزدیک در مرحله انتخاب خط نمایش داده می‌شوند و سایر خطوط با جستجو قابل انتخاب هستند.
- شناسه خط و شناسه ایستگاه در رابط کاربر و خروجی Excel نمایش داده نمی‌شوند.
- برای خط خارج از فهرست/غیرفعال فقط عنوان خط دریافت می‌شود.
- افزودن تابلو به‌صورت مرحله‌ای با انتخاب نوع، دوربین، پیش‌نمایش و تأیید تصویر و سؤال برای تابلوی بعدی انجام می‌شود.
- تصویر محل ایستگاه نیز قبل از ادامه مرحله تأیید می‌شود.
- مرحله آدرس فقط یک فیلد آدرس کامل دارد و فیلد خیابان جداگانه حذف شده است.
- ثبت نهایی فقط پس از تکمیل همه اطلاعات و تصاویر امکان‌پذیر است.
- زمان ثبت با timezone تهران ذخیره و تاریخ‌ها به شمسی نمایش داده می‌شوند.

## Admin / Web
- نقشه خطوط تاکسیرانی همه خطوط دارای مختصات را نمایش می‌دهد؛ خطوط دارای ایستگاه سبز و خطوط فاقد ثبت ایستگاه قرمز هستند.
- Popup نقشه اطلاعات کامل ایستگاه، آدرس، مختصات، دقت GPS، زمان شمسی تهران و تصاویر محل/تابلوها را نمایش می‌دهد.
- مسیر نمایش تصاویر به URL صحیح `app.yousefipour.ir` اصلاح شد تا خطای 404 رفع شود.
- خروجی Excel خطوط و ایستگاه‌ها تصاویر محل و تابلوها را داخل فایل XLSX قرار می‌دهد و مسیر فایل تصویر را در خروجی درج نمی‌کند.

## Backend
- `station-wizard-v3.php` و APIهای گزارش/خروجی با timezone `Asia/Tehran` همسان شدند.
- دسترسی آیتم ثبت ایستگاه با تنظیمات نقش و `role_app_items` کنترل می‌شود.
- ساختار DB با MySQL/MariaDB سازگار نگه داشته شد.

## وضعیت تست
- کد و Commitهای اصلاحات در GitHub ثبت شده‌اند.
- Build واقعی Android، تست Android 9، تست Production API/DB، تست دوربین/GPS واقعی، تست Excel واقعی و تست کامل پنل هنوز باید در محیط اجرایی انجام شوند.
- وضعیت CI فعلی برای آخرین Commit در لحظه بررسی هنوز نتیجه نهایی ارائه نکرده است.

## نسخه پایه
- سایر بخش‌های نسخه 1.3.72 و هماهنگی نسخه/استقرار طبق مستندات قبلی حفظ شده‌اند.
