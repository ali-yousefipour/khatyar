# تغییرات جاری — 2026-08-27

## نسخه 1.3.73 — اصلاح نهایی منوی خطوط، نقشه و Excel
- تب‌های «نقشه خطوط تاکسیرانی»، «موقعیت خطوط و ایستگاه‌ها»، «تنظیمات نوع تابلوها» و «خروجی Excel» فقط زیر آیتم «خطوط تاکسیرانی» در بخش «تاکسی و تاکسیران» سایدبار قرار گرفتند و دکمه‌های تب تکراری از محتوای اصلی حذف شدند.
- ترتیب سایدبار اصلاح شد تا «خطوط تاکسیرانی» در بالا و چهار آیتم جدید بلافاصله زیر آن قرار بگیرند؛ MutationObserver نیز در صورت بازسازی DOM، ترتیب را دوباره اصلاح می‌کند.
- خطای `400` تایل‌های OpenStreetMap رفع شد؛ نقشه دیگر در Zoom 20 درخواست تایل نمی‌کند و `maxNativeZoom/maxZoom` روی 19 تنظیم شده است و `fitBounds` نیز بیش از Zoom 18 نمی‌رود.
- Popup نقشه اطلاعات ایستگاه، آدرس، دقت GPS، زمان ثبت، تعداد تابلوها، نوع هر تابلو و تصاویر احراز هویت‌شده را نمایش می‌دهد.
- جدول موقعیت خطوط، تعداد و نوع تابلوها و تصویر ایستگاه را نیز نمایش می‌دهد.
- خروجی Excel از endpoint احراز هویت‌شده `api/line-location-export-v4.php` دریافت می‌شود و شامل اطلاعات خطوط، موقعیت ایستگاه، تعداد/نوع تابلوها، تصاویر Embed‌شده ایستگاه و تابلوها و لینک واقعی Google Maps برای موقعیت معتبر است.
- Cache-Busting پنل به `r17` افزایش یافت تا نسخه قدیمی JavaScript/HTML از Cache مرورگر استفاده نکند.

## نسخه
- نسخه سایت و Android روی **1.3.73** یکسان نگه داشته شده‌اند.
- Version Code Android برابر **10373** است.

## وضعیت استقرار
- اصلاحات در GitHub روی شاخه `main` ثبت شده‌اند.
- برای مشاهده اصلاحات روی Production، فایل‌های `php/app/assets/station-admin-tabs-v8.js` و `php/app/panel.html` باید روی Document Root مربوط به `app.yousefipour.ir` مستقر شوند.
- پس از استقرار، یک Hard Refresh مرورگر انجام شود تا Asset با Query String جدید `r17` دریافت شود.
