# تغییرات جاری — 2026-08-26

## نسخه 1.3.72 — همسان‌سازی نسخه و استقرار Backend
- نسخه Android و assetهای پنل به `1.3.72` منتقل شدند.
- `versionCode` پیش‌فرض Android برای نسخه `1.3.72` برابر `10372` است.
- `panel.html` برای جلوگیری از cache قدیمی، bundle و assetهای مرتبط را با `?v=1.3.72` بارگذاری می‌کند.
- Badge نسخه پنل و endpoint مستقل `app/api/version.php` نسخه `1.3.72` را اعلام می‌کنند.
- workflow همسان‌سازی نسخه طوری تنظیم شده که در تغییرات پروژه، نسخه patch را افزایش داده و نسخه Android، Backend، Web/Admin و assetهای پنل را هماهنگ کند.
- workflow استقرار PHP به‌صورت تغییرمحور تنظیم شده است؛ هر تغییر زیر `php/` باعث اجرای Deploy به هاست اشتراکی می‌شود و فهرست فایل‌های تغییرکرده قبل از Upload ثبت می‌شود.
- endpoint خروجی Excel ایستگاه‌های خطوط در `php/app/api/line-location-export.php` در مخزن موجود و برای اجرای مستقیم روی هاست آماده است.
- endpoint مستقل نسخه برای بررسی سریع اختلاف نسخه بین سایت، پنل و اپ اضافه شده است.

## ثبت ایستگاه‌ها
- ثبت ایستگاه به‌عنوان آیتم مستقل اپ (`LineLocation`) باقی می‌ماند و کنترل نمایش آن از تنظیمات «آیتم‌های اپ» انجام می‌شود.
- منطق دقت GPS کمتر از ۲۰ متر و شعاع ۱۰۰ متری geofence حضور باید در تست Production End-to-End تأیید شود.
- نوع تابلوها و خطوط از داده‌های موجود سامانه و APIهای فعلی استفاده می‌کنند.

## وضعیت تست
- تغییرات در مخزن GitHub ثبت شده‌اند.
- Build واقعی Android، اجرای Deploy واقعی FTP، تست Production API و تست Chrome/Android واقعی هنوز باید توسط GitHub Actions/محیط Production اجرا و نتیجه آن بررسی شود؛ صرف وجود workflow به‌تنهایی به معنی موفقیت اجرای آن نیست.
