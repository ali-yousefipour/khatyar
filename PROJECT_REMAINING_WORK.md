# خطیار — کارهای باقی‌مانده و در دست اقدام

> مرجع زنده برنامه توسعه پروژه. این فایل باید با هر تغییر، رفع خطا، قابلیت جدید یا تصمیم فنی به‌روزرسانی شود.
>
> آخرین به‌روزرسانی: 1405/06/05 — 2026-08-26

## وضعیت‌های مورد استفاده

- 🔴 باقی‌مانده / نیازمند اقدام
- 🟡 در دست اقدام / نیازمند تست
- 🟢 انجام‌شده و تأییدشده
- ⚪ وابسته به محیط استقرار یا تست خارجی

## 1. مستندسازی و کنترل تغییرات

- 🟢 ایجاد `PROJECT_DOCUMENTATION.md` به عنوان مرجع واحد پروژه.
- 🟢 یکسان‌سازی README اصلی و `mobile/README.md`.
- 🟢 ایجاد این فایل برای پیگیری کارهای باقی‌مانده.
- 🟡 حذف/آرشیو تدریجی مستندات قدیمی و متناقض پس از تطبیق کامل با مرجع واحد.
- 🟢 ثبت تغییر قابلیت «ثبت موقعیت و تصویر خطوط» در مستندات.
- 🟢 ثبت هر تغییر آینده باید همزمان در مستندات و وضعیت این فایل انجام شود.
- 🟢 ثبت یکپارچه‌سازی «موقعیت و تصاویر خطوط» با بخش خطوط پنل مدیریت.

## 2. Android / Mobile

- 🟡 تست واقعی اجرای برنامه روی Android 9 و رفع هر Crash باقی‌مانده در Startup.
- 🟡 تست Image Picker/Camera و Location روی Android 9، 10 و نسخه‌های جدید.
- 🟡 تست Background Location و Foreground Service در Build واقعی.
- 🟡 تست Permission Guard، GPS Guard، VPN/Developer Options و Battery Guard.
- 🟡 تست کامل ورود، Refresh Token، Device Binding و Logout.
- 🟡 تست صف آفلاین و همگام‌سازی فقط برای عملیات مجاز.
- 🟢 افزودن صفحه Native «ثبت موقعیت و تصویر خطوط» با GPS، دوربین، انتخاب خط، نمایش نقشه، تاریخچه و مدیریت مجوز.
- 🟢 ثبت Route با نام `LineLocation` در Navigation.
- 🟢 افزودن «ثبت موقعیت و تصویر خطوط» به دسترسی سریع داشبورد Android.
- ⚪ Build نهایی APK و AAB و تأیید نصب روی دستگاه واقعی.
- ⚪ تأیید Build و انتشار Myket پس از موفقیت تست نهایی.

## 3. Web App (`php/app/app.html`)

- 🟡 تکمیل همسان‌سازی امکانات Android و Web App.
- 🟡 تکمیل همسان‌سازی UI/UX با Android و حفظ RTL/Mobile-first.
- 🟡 بررسی تمام آیتم‌های Sidebar در برابر Navigation Android.
- 🟡 تست GPS و Camera در مرورگرهای واقعی.
- 🟡 حذف کامل Mock/Placeholderهای باقی‌مانده در محیط عملیاتی.
- 🟡 بررسی رفتار Offline/Online و پیام خطای API.
- 🟢 صفحه `line-location.html` برای ثبت موقعیت، دو تصویر، انتخاب خط و نمایش نقشه موجود است.
- 🟢 Web parity شامل آیتم «ثبت موقعیت و تصویر خطوط» است.

## 4. ثبت موقعیت و تصویر خطوط

- 🟢 API و ساختار ثبت موقعیت/تصویر خطوط ایجاد و تکمیل شد.
- 🟢 مجوزهای `can_capture`، `can_view` و `can_manage` تعریف شدند.
- 🟢 ذخیره latitude/longitude/accuracy و تصاویر انجام می‌شود.
- 🟢 نام ایستگاه نیز در جدول تاریخچه و رکورد جاری خط ذخیره می‌شود.
- 🟢 برای Android یک ورودی سازگار با API پایه `/api` ایجاد شد تا با `apiBase` فعلی برنامه تداخل نداشته باشد.
- 🟢 دسترسی مدیران مجاز به تمام خطوط بدون محدودیت `user_lines` اصلاح شد.
- 🟢 تاریخچه برای خطوط تحت دسترسی کاربر محدود شده است.
- 🟢 کنترل MIME و اعتبارسنجی محتوای تصویر Base64 اضافه شد.
- 🟢 پنل مدیریت به داده‌های آخرین موقعیت خط، دقت GPS، نام ایستگاه و مسیر تصاویر متصل شد.
- 🟢 در پنل، فهرست قابل جستجوی خطوط، نقشه آخرین موقعیت، دو تصویر محل/تابلو و تاریخچه همان خط اضافه شد.
- 🟢 قابلیت جدید به عنوان زیرآیتم بخش «خطوط تاکسیرانی» در منوی پنل قابل دسترسی است.
- 🟡 تست End-to-End ثبت موقعیت از Android تا DB.
- 🟡 تست ثبت دو تصویر محل خط و تابلو با دوربین واقعی.
- 🟡 تست بروزرسانی اطلاعات خط و تاریخچه روی DB واقعی.
- 🟡 تست مدیریت Permission بر اساس role در Site/Admin.
- 🟡 بررسی محدودسازی ثبت به خطوط مجاز کاربر در محیط واقعی.

## 5. بازدید و پوشش خطوط

- 🟢 موتور برنامه بازدید و پوشش خط مستند شده است.
- 🟢 APIهای اصلی و جداول مربوطه مستند شده‌اند.
- 🟡 تست شروع/پایان بازدید با GPS و accuracy واقعی.
- 🟡 تست محدوده خط/ایستگاه.
- 🟡 تست ثبت Timeline و محاسبه پوشش.
- 🟡 تست اتصال حضور، چک‌لیست و تذکر به بازدید صحیح.
- 🟡 تست گزارش مدیریتی روزانه و بازه‌ای.

## 6. Backend / Database

- 🟡 اجرای Migrationهای نهایی روی یک DB تستی MySQL/MariaDB.
- 🟡 بررسی Idempotent بودن تمام Migrationهای جدید.
- 🟡 بررسی تمام endpointها برای پاسخ JSON معتبر و یکنواخت.
- 🟡 بررسی خطاهای HTTP 500 باقی‌مانده.
- 🟡 بررسی CORS، Cache و Headerهای امنیتی در محیط واقعی.
- 🟡 بررسی حجم و فشرده‌سازی تصاویر عملیاتی.
- 🟢 API ثبت ایستگاه با MySQL/MariaDB و `CREATE TABLE IF NOT EXISTS` سازگار شده است.
- 🔴 ممیزی نهایی نام مسیرها پس از تغییر `public` به `app` و حذف ارجاعات قدیمی.

## 7. استقرار و Production

- 🟡 تست `https://app.yousefipour.ir/`، `/app` و APIها پس از آخرین تغییرات.
- 🟡 تست ورود واقعی با کاربر دارای نقش مختلف.
- 🟡 تست مسیرهای حساس با Token منقضی/نامعتبر.
- ⚪ تست SSL/HTTPS و Permissionهای مرورگر روی دامنه Production.
- ⚪ تهیه Backup قبل از Migrationهای Production.

## 8. فرآیند ثبت تغییرات از این پس

برای هر قابلیت یا اصلاح جدید باید حداقل این موارد همزمان بررسی و ثبت شوند:

1. کد Android در صورت ارتباط.
2. کد Web App در صورت ارتباط.
3. کد Site/Admin در صورت ارتباط.
4. Backend/API.
5. Migration/Database در صورت نیاز.
6. `PROJECT_DOCUMENTATION.md` — وضعیت و معماری فعلی.
7. `PROJECT_REMAINING_WORK.md` — وضعیت کار و موارد باقی‌مانده.
8. `CHANGELOG.md` — خلاصه تغییر نسخه/Commit.

هر موردی که در کد انجام نشده نباید در مستندات به عنوان «انجام‌شده» علامت‌گذاری شود؛ ابتدا باید با کد یا تست قابل تأیید باشد.
