/* Shared version badge. */
(function(){'use strict';const V='1.3.83';function mount(){const host=document.getElementById('kh-version-badge-host');if(host)host.textContent='نسخه '+V;}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',mount,{once:true});else mount();})();
