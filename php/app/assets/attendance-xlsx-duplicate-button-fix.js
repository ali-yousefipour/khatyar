/* خطیار — فقط دکمهٔ تکراری «خروجی XLSX گزارش تردد» را حذف می‌کند؛ خروجی اصلی سامانه حفظ می‌شود. */
(function(){'use strict';function norm(v){return String(v||'').replace(/[\u200c\u200e\u200f]/g,'').replace(/\s+/g,' ').trim();}function run(){document.querySelectorAll('button').forEach(function(b){if(norm(b.textContent)==='▣ خروجی XLSX گزارش تردد'||norm(b.textContent)==='خروجی XLSX گزارش تردد')b.remove();});}if(document.body){new MutationObserver(run).observe(document.body,{childList:true,subtree:true});run();}else document.addEventListener('DOMContentLoaded',run,{once:true});})();
