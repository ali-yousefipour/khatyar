(function(){
  'use strict';
  if(window.__KH_STATION_ADMIN_TABS_V6__) return;
  window.__KH_STATION_ADMIN_TABS_V6__=true;
  const LABELS={map:'نقشه خطوط تاکسیرانی',locations:'موقعیت خطوط و ایستگاه‌ها',types:'تنظیمات نوع تابلوها',excel:'خروجی Excel'};
  const ICONS={map:'city-map',locations:'route-line',types:'settings-gears',excel:'excel-upload'};
  const token=()=>localStorage.getItem('token')||localStorage.getItem('access')||localStorage.getItem('access_token')||'';
  const esc=v=>String(v==null?'':v).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));
  const fa=n=>String(n==null?'':n).replace(/\d/g,d=>'۰۱۲۳۴۵۶۷۸۹'[d]);
  const authImage=url=>{if(!url)return '';const t=token();if(!t)return url;try{const u=new URL(url,location.origin);if(u.origin===location.origin&&!u.searchParams.has('access_token'))u.searchParams.set('access_token',t);return u.href;}catch(e){return url;}};
  async function api(op,opt){opt=opt||{};const headers=Object.assign({'Authorization':'Bearer '+token(),'Accept':'application/json'},opt.headers||{});const r=await fetch('/station-admin-v4.php?op='+encodeURIComponent(op),Object.assign({},opt,{headers,cache:'no-store'}));const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.error||'خطای سرور');return d;}
  function css(){if(document.getElementById('kh-line-admin-v6-css'))return;const s=document.createElement('style');s.id='kh-line-admin-v6-css';s.textContent=`
    .kh-line-nav-enhanced.open .navsec-body{max-height:760px!important;opacity:1!important;overflow-y:auto!important}
    .kh-line-subnav{display:flex;flex-direction:column;gap:4px;margin:2px 0 7px;padding:4px 0;border-right:2px solid rgba(247,197,49,.38)}
    .kh-line-subitem{display:flex;align-items:center;gap:9px;width:100%;min-height:46px;border:0;border-radius:10px;background:transparent;color:#aebbd0;padding:0 12px 0 8px;font:inherit;font-size:11.5px;cursor:pointer;text-align:right;transition:.18s}
    .kh-line-subitem:hover{background:rgba(255,255,255,.08);color:#fff;transform:translateX(-2px)}
    .kh-line-subitem.on{background:rgba(14,138,106,.28);color:#fff;font-weight:800}
    .kh-line-subitem .kh-line-ic{width:22px;height:22px;display:grid;place-items:center;flex:0 0 22px}.kh-line-subitem .kh-line-ic img{width:22px;height:22px;object-fit:contain}
    #kh-line-admin-main-host{margin:0 0 20px;padding:0;direction:rtl}
    #kh-line-admin-main-host .kh-line-admin-panel{background:#fff;border:1px solid #e4e9f2;border-radius:16px;padding:16px;box-shadow:0 2px 8px rgba(15,29,51,.08)}
    #kh-line-admin-main-host .kh-line-head{display:flex;align-items:center;justify-content:space-between;gap:10px;flex-wrap:wrap;margin-bottom:12px}
    #kh-line-admin-main-host .kh-line-head h3{margin:0;font-size:16px}.kh-line-actions{display:flex;gap:7px;flex-wrap:wrap}
    #kh-line-admin-main-host .kh-line-btn{border:1px solid #e4e9f2;background:#fff;color:#667085;border-radius:10px;padding:8px 12px;font:inherit;font-size:12px;font-weight:700;cursor:pointer}
    #kh-line-admin-main-host .kh-line-btn.on{background:#0e8a6a;color:#fff;border-color:#0e8a6a}
    #kh-line-admin-main-host .kh-line-map{height:470px;border-radius:13px;overflow:hidden;border:1px solid #e4e9f2}
    #kh-line-admin-main-host table{width:100%;border-collapse:collapse;font-size:12px}#kh-line-admin-main-host th,#kh-line-admin-main-host td{padding:9px;border-bottom:1px solid #e9edf3;text-align:right}#kh-line-admin-main-host th{background:#f7f9fc;color:#667085;font-weight:800}
    #kh-line-admin-main-host .kh-line-muted{color:#667085;font-size:12px}#kh-line-admin-main-host .kh-line-kpis{display:flex;gap:10px;flex-wrap:wrap;margin-bottom:10px}#kh-line-admin-main-host .kh-line-kpi{background:#f7f9fc;border:1px solid #e9edf3;border-radius:11px;padding:9px 14px;font-size:12px}
    #kh-line-admin-main-host .kh-line-error{background:#fff0f2;color:#b42318;border:1px solid #f5c2c7;border-radius:10px;padding:10px;font-size:12px}
    @media(max-width:850px){#kh-line-admin-main-host .kh-line-map{height:360px}}
  `;document.head.appendChild(s);}
  function logged(){return !!token();}
  function sidebar(){
    const sec=[...document.querySelectorAll('.navsec')].find(x=>String(x.querySelector('.navsec-head')?.innerText||'').includes('تاکسی و تاکسیران'));if(!sec)return null;sec.classList.add('kh-line-nav-enhanced');const body=sec.querySelector('.navsec-body');if(!body)return null;if(body.querySelector('.kh-line-subnav'))return body.querySelector('.kh-line-subnav');
    const native=[...body.querySelectorAll('.navitem')].find(x=>String(x.innerText||'').trim()==='خطوط تاکسیرانی');const wrap=document.createElement('div');wrap.className='kh-line-subnav';
    Object.entries(LABELS).forEach(([key,label])=>{const b=document.createElement('button');b.type='button';b.className='kh-line-subitem';b.dataset.khLineTab=key;b.innerHTML='<span class="kh-line-ic"><img src="/assets/icons3d/'+ICONS[key]+'.png" alt=""></span><span>'+label+'</span>';b.onclick=()=>openTab(key);wrap.appendChild(b);});
    if(native)native.insertAdjacentElement('afterend',wrap);else body.appendChild(wrap);return wrap;
  }
  function main(){let host=document.getElementById('kh-line-admin-main-host');if(host)return host;const m=document.querySelector('.main');if(!m)return null;host=document.createElement('div');host.id='kh-line-admin-main-host';m.appendChild(host);return host;}
  let data=null,active='map',map=null;async function load(){if(!data)data=await api('map');return data;}
  function activateTab(key){active=key;document.querySelectorAll('[data-kh-line-tab]').forEach(b=>b.classList.toggle('on',b.dataset.khLineTab===key));}
  function base(title,body){const h=main();if(!h)return null;if(map){try{map.remove();}catch(e){}map=null;}h.innerHTML='<section class="kh-line-admin-panel"><div class="kh-line-head"><h3>'+title+'</h3><div class="kh-line-actions">'+Object.entries(LABELS).map(([k,l])=>'<button class="kh-line-btn '+(k===active?'on':'')+'" data-main-tab="'+k+'">'+l+'</button>').join('')+'</div></div><div id="kh-line-admin-body">'+body+'</div></section>';h.querySelectorAll('[data-main-tab]').forEach(b=>b.onclick=()=>openTab(b.dataset.mainTab));return h.querySelector('#kh-line-admin-body');}
  async function mapTab(){const d=await load();const body=base('نقشه خطوط تاکسیرانی','<div class="kh-line-kpis"><div class="kh-line-kpi">کل خطوط: <b>'+fa(d.stats.total)+'</b></div><div class="kh-line-kpi">ثبت‌شده: <b>'+fa(d.stats.registered)+'</b></div><div class="kh-line-kpi">ثبت‌نشده: <b>'+fa(d.stats.unregistered)+'</b></div></div><div id="kh-line-admin-map" class="kh-line-map"></div>');if(!body)return;if(!window.L){body.insertAdjacentHTML('beforeend','<p class="kh-line-error">Leaflet بارگذاری نشده است.</p>');return;}map=L.map('kh-line-admin-map').setView([36.297,59.606],12);L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png',{maxZoom:20,attribution:'© OpenStreetMap'}).addTo(map);const bounds=[];(d.lines||[]).forEach(x=>{if(x.latitude==null||x.longitude==null)return;const m=L.circleMarker([Number(x.latitude),Number(x.longitude)],{radius:8,weight:2,fillOpacity:.9,fillColor:x.registered?'#168a58':'#d92d20'}).addTo(map);let html='<div dir="rtl"><b>خط '+esc(x.code||'بدون شماره')+'</b><br>'+esc([x.origin,x.destination].filter(Boolean).join(' تا '))+'<br>وضعیت: '+(x.registered?'ایستگاه ثبت شده':'ایستگاه ثبت نشده');if(x.registered){if(x.physical_address)html+='<br>آدرس: '+esc(x.physical_address);if(x.accuracy_m!=null)html+='<br>دقت: '+esc(x.accuracy_m)+' متر';const loc=authImage(x.location_photo_url);if(loc)html+='<br><img src="'+esc(loc)+'" style="width:180px;border-radius:8px;margin-top:6px">';(x.signs||[]).forEach((z,i)=>{const p=authImage(z.photo_url);html+='<div style="margin-top:7px"><b>تابلو '+fa(i+1)+': '+esc(z.title||'')+'</b>'+(p?'<br><img src="'+esc(p)+'" style="width:150px;border-radius:8px;margin-top:4px">':'')+'</div>';});}html+='</div>';m.bindPopup(html,{maxWidth:300});bounds.push([Number(x.latitude),Number(x.longitude)]);});if(bounds.length)map.fitBounds(bounds,{padding:[20,20]});}
  async function locationsTab(){const d=await load();const rows=(d.lines||[]).map(x=>'<tr><td>'+esc(x.code||'')+'</td><td>'+esc([x.origin,x.destination].filter(Boolean).join(' تا '))+'</td><td>'+((x.latitude!=null&&x.longitude!=null)?esc(x.latitude)+', '+esc(x.longitude):'بدون مختصات')+'</td><td>'+((x.registered)?'ثبت شده':'ثبت نشده')+'</td><td>'+esc(x.physical_address||'—')+'</td><td>'+fa((x.signs||[]).length)+'</td></tr>').join('');base('موقعیت خطوط و ایستگاه‌ها','<div style="overflow:auto"><table><thead><tr><th>خط</th><th>مبدأ و مقصد</th><th>مختصات</th><th>وضعیت ایستگاه</th><th>آدرس</th><th>تابلو</th></tr></thead><tbody>'+rows+'</tbody></table></div>'+(d.lines.length?'':'<p class="kh-line-muted">خطی یافت نشد.</p>'));}
  async function typesTab(){const d=await api('types');const rows=(d.types||[]).map(x=>'<tr><td>'+fa(x.sort_order)+'</td><td>'+esc(x.title)+'</td><td>'+esc(x.code||'')+'</td><td>'+(Number(x.is_active)?'فعال':'غیرفعال')+'</td><td><button class="kh-line-btn" data-edit="'+x.id+'">ویرایش</button> <button class="kh-line-btn" data-del="'+x.id+'">حذف</button></td></tr>').join('');const body=base('تنظیمات نوع تابلوها','<div style="margin-bottom:10px"><button class="kh-line-btn on" id="kh-add-type">+ افزودن نوع تابلو</button></div><div style="overflow:auto"><table><thead><tr><th>ترتیب</th><th>عنوان</th><th>کد</th><th>وضعیت</th><th>عملیات</th></tr></thead><tbody>'+rows+'</tbody></table></div>');if(!body)return;body.querySelector('#kh-add-type').onclick=()=>editType(null);body.querySelectorAll('[data-edit]').forEach(b=>b.onclick=()=>editType(d.types.find(x=>String(x.id)===b.dataset.edit)));body.querySelectorAll('[data-del]').forEach(b=>b.onclick=async()=>{if(!confirm('این نوع تابلو حذف/غیرفعال شود؟'))return;try{const fd=new URLSearchParams({id:b.dataset.del});await api('delete-type',{method:'POST',body:fd,headers:{'Content-Type':'application/x-www-form-urlencoded'}});data=null;typesTab();}catch(e){alert(e.message);}});}
  async function editType(x){const title=prompt('عنوان نوع تابلو:',x?x.title:'');if(title===null)return;const code=prompt('کد اختیاری:',x?.code||'');if(code===null)return;const sort=prompt('ترتیب نمایش:',x?x.sort_order:'0');if(sort===null)return;const active=confirm('نوع تابلو فعال باشد؟');await api('save-type',{method:'POST',body:JSON.stringify({id:x?x.id:0,title,code,sort_order:Number(sort)||0,is_active:active}),headers:{'Content-Type':'application/json'}});data=null;typesTab();}
  async function excelTab(){const d=await load();const body=base('خروجی Excel','<p class="kh-line-muted">فایل XLSX از اطلاعات فعلی خطوط، موقعیت‌ها و ایستگاه‌ها تولید می‌شود.</p><button class="kh-line-btn on" id="kh-export-xlsx">تولید و دانلود Excel</button><span id="kh-export-msg" style="margin-right:8px"></span>');if(!body)return;body.querySelector('#kh-export-xlsx').onclick=()=>{try{if(!window.XLSX)throw new Error('کتابخانه Excel در پنل بارگذاری نشده است');const out=(d.lines||[]).map(x=>({'کد خط':x.code||'','مبدأ':x.origin||'','مقصد':x.destination||'','وضعیت خط':x.status||'','عرض جغرافیایی':x.latitude??'','طول جغرافیایی':x.longitude??'','وضعیت ایستگاه':x.registered?'ثبت شده':'ثبت نشده','دقت GPS (متر)':x.accuracy_m??'','آدرس محل ایستگاه':x.physical_address||'','تعداد تابلو':(x.signs||[]).length,'زمان ثبت':x.captured_at||''}));const ws=XLSX.utils.json_to_sheet(out),wb=XLSX.utils.book_new();XLSX.utils.book_append_sheet(wb,ws,'خطوط و ایستگاه‌ها');XLSX.writeFile(wb,'گزارش-خطوط-و-ایستگاه‌ها.xlsx');body.querySelector('#kh-export-msg').textContent='✓ فایل تولید شد';}catch(e){body.querySelector('#kh-export-msg').textContent=e.message||'خطا در تولید فایل';}};}
  async function openTab(key){if(!logged())return;css();activateTab(key);active=key;if(typeof window.__navigateTo==='function')window.__navigateTo('lines');await new Promise(r=>setTimeout(r,120));const m=main();if(m)m.innerHTML='<section class="kh-line-admin-panel"><div class="kh-line-muted">در حال بارگذاری…</div></section>';try{if(key==='map')await mapTab();else if(key==='locations')await locationsTab();else if(key==='types')await typesTab();else await excelTab();}catch(e){const h=main();if(h)h.innerHTML='<section class="kh-line-admin-panel"><div class="kh-line-error">'+esc(e.message||'خطا در بارگذاری اطلاعات خطوط')+'</div></section>';}}
  function cleanupLegacyHost(){const h=document.getElementById('kh-line-admin-host');if(h)h.remove();}
  function boot(){if(!logged())return;css();cleanupLegacyHost();sidebar();}
  const mo=new MutationObserver(()=>{if(logged()){cleanupLegacyHost();sidebar();}});mo.observe(document.documentElement,{subtree:true,childList:true});
  let tries=0;const timer=setInterval(()=>{boot();if(++tries>120)clearInterval(timer)},500);if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot);else boot();window.KhatyarStationAdmin={openTab};
})();
