<?php
// اجرای خودکار کار «mission-scoring» — نهایی‌سازی امتیاز روزانهٔ نیروهای میدانی
// مهم: برخلاف cron_daily.php که صبح اجرا می‌شود، این فایل باید پایان روز کاری اجرا شود
// (مثلاً ساعت ۲۳:۰۰) چون امتیاز منفیِ «خط بازدید نشده» و «مأموریت انجام‌نشده» فقط در پایان
// روز معنا دارد؛ اجرای زودهنگام باعث ثبت نادرست غیبت/عدم‌تحقق می‌شود.
// این فایل را در بخش Cron Jobs هاست (cPanel) با زمان‌بندی جداگانه تنظیم کنید:
//   0 23 * * *  php /home/h301194/public_html/cron_mission_scoring.php
// در حالت اجرای CLI (Cron) نیازی به کلید نیست.
// اگر از مرورگر صدا بزنید، باید ?key=مقدار_cron_key اضافه کنید.
$GLOBALS['CRON_TASK'] = 'mission-scoring';
require __DIR__ . '/cron.php';
