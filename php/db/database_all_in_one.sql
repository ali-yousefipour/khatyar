-- ============================================================================
-- سامانه مدیریت خطوط تاکسیرانی مشهد
-- DATABASE ALL-IN-ONE
-- ============================================================================
-- این فایل نقطه ورود واحد برای دیتابیس است.
-- ترتیب اجرای فایل‌های موجود در php/db در این فایل تثبیت شده است.
--
-- نکته مهم:
-- دستورات SOURCE متعلق به mysql/mariadb client هستند و در phpMyAdmin به‌صورت
-- مستقیم قابل اجرا نیستند. برای اجرای یک‌فایلی با phpMyAdmin باید محتوای فایل‌های
-- مرجع به همین ترتیب در یک خروجی SQL واقعی concatenate شود.
-- این فایل عمداً فایل‌های اصلی را حذف نمی‌کند تا سابقه و امکان rollback حفظ شود.
--
-- ترتیب canonical:
--   1) mysql_schema.sql
--   2) upgrade_full_standalone.sql   (تجمیع ارتقاهای موجود)
--   3) cleanup_cron.sql              (اختیاری/عملیات پاکسازی)
--
-- سازگار با MySQL/MariaDB و ساختار فعلی پروژه.
-- ============================================================================

SET NAMES utf8mb4;
SET FOREIGN_KEY_CHECKS=0;

-- ---------------------------------------------------------------------------
-- مرحله 1: ساخت Schema پایه
-- ---------------------------------------------------------------------------
SOURCE mysql_schema.sql;

-- ---------------------------------------------------------------------------
-- مرحله 2: اجرای ارتقای تجمیعی
-- upgrade_full_standalone.sql در Repository فایل canonical ارتقاهای تجمیعی است.
-- ---------------------------------------------------------------------------
SOURCE upgrade_full_standalone.sql;

-- ---------------------------------------------------------------------------
-- مرحله 3: اسکریپت پاکسازی Cron
-- این بخش عملیاتی است و در نصب/ارتقای دیتابیس نباید خودکار اجرا شود.
-- در نتیجه فقط مستند شده و برای اجرای Cron جداگانه باقی می‌ماند.
-- ---------------------------------------------------------------------------
-- SOURCE cleanup_cron.sql;

SET FOREIGN_KEY_CHECKS=1;

-- ============================================================================
-- پایان
-- ============================================================================
