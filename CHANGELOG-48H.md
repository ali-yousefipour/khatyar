# گزارش تغییرات ۴۸ ساعت اخیر مخزن KhatYar

**مخزن:** `ali-yousefipour/khatyar`  
**بازه بررسی:** ۱ تا ۳ سپتامبر ۲۰۲۶ (بر اساس تاریخ Commitها)  
**شاخه:** `main`  
**مبنای گزارش:** تاریخچه Commitهای GitHub؛ Commitهای تکراری، markerهای موقت و cleanupهای بدون اثر عملکردی در بخش خلاصه ادغام شده‌اند.

## خلاصه مدیریتی

در ۴۸ ساعت اخیر، بیشترین تمرکز توسعه روی پنج محور بوده است:

1. **سامانه بی‌سیم و ارتباطات** در پنل مدیریت و اپلیکیشن اندروید.
2. **حضور و غیاب و گزارش عملکرد**، به‌خصوص ثبت حضور دستی، گزارش تردد و پایداری فرمت اعداد.
3. **تقویم جلالی و تعطیلات رسمی ایران** و حذف وابستگی به جدول تعطیلات دستی.
4. **Push Notification و توکن دستگاه** برای پایداری اعلان‌ها.
5. **بیلد و انتشار اندروید**، نسخه‌بندی، کش Gradle و رفع خطاهای Native/Metro.

همچنین چندین Commit برای cache-busting پنل، هم‌ترازی نسخه‌ها و پاکسازی تغییرات موقت انجام شده است.

---

## ۱. بی‌سیم و ارتباطات

این بخش پرتکرارترین محور تغییرات اخیر بوده است.

### اپلیکیشن اندروید

- `d20cba8` — جداسازی RTL بی‌سیم از mirror شدن عمومی Flex.
- `1224549` — ریست cursor دریافت Native Radio هنگام تغییر کانال.
- `2212fb4` — سازگار کردن رویداد Volume Up با Radio Context برای PTT.
- `1fb1071` — فعال‌سازی Native Volume-Up PTT plugin.
- `f8af516` — پایدارسازی عملکرد PTT سخت‌افزاری با Volume Up.
- `81875dd` — نرمال‌سازی URL فایل صوتی پس‌زمینه برای دریافت بی‌سیم.
- `ed67354` — حذف global RTL flex mirroring برای جلوگیری از به‌هم‌ریختگی چیدمان بی‌سیم.
- `4921cdb` — تثبیت ترتیب بصری پلاک تاکسی در RTL.
- `5075515` — رفع خطای syntax در Regex مربوط به `RadioContext` که جلوی Metro bundle اندروید را می‌گرفت.
- `2524802` — استفاده از Resource Reference برای توضیح سرویس بی‌سیم اندروید.
- `cecd403` — بی‌اعتبار کردن prebuild قدیمی اندروید پس از تغییرات Native.
- `c37925a` — افزایش نسخه Android Release به `1.3.84`.

### پنل مدیریت

چندین Commit برای برگرداندن دسترسی و منوی بی‌سیم ثبت شده است:

- `8f2d463` — بازسازی UI دسترسی آیتم‌های بی‌سیم با syntax امن.
- `b68a579` — نمایش جداگانه «مرکز بی‌سیم» و «تنظیمات بی‌سیم».
- `052f8bf` — بازیابی permission تنظیمات بی‌سیم و ورودی Sidebar.
- `c157b5d` — بازیابی فهرست canonical آیتم‌های اپ هر سمت شامل آیتم‌های بی‌سیم.
- `d8ef018` — بازیابی آیتم‌های دسترسی بی‌سیم و اصلاح جایگاه Sidebar.
- `620e55d` — حذف تزریق اشتباه Global برای احراز فوری حضور.
- `5bb894e` — بازیابی تب canonical صحت‌سنجی و پاکسازی Layout ناوبری بی‌سیم.
- `8c1ff40` — حفظ نمایش مرکز بی‌سیم و تنظیمات بی‌سیم در Navigation.
- `e761420` — قرار دادن مرکز و تنظیمات بی‌سیم زیر «ارتباطات».
- `24031a3` — نمایش پایدار آیتم‌های بی‌سیم در تنظیمات دسترسی سمت‌ها.
- `0ad00f1` — شناسایی صحیح Runtime Patch بدون جعل Bundle Build.
- `abeab7c` — cache-busting تغییرات حضور، دسترسی بی‌سیم و قالب اعداد.
- `d94012c` — هم‌تراز کردن نسخه قابل مشاهده پنل با Release دارای cache-busting.
- `b6d08b2` — بازگردانی پایدار آیتم‌های دسترسی بی‌سیم بدون خطای React.
- `9bdfed1` — بارگذاری ماژول‌های دسترسی و احراز فوری حضور برای Release `1.3.89`.
- `a6892f5` — افزایش cache version پنل به `1.3.89`.
- `45c323b` — انتشار `1.3.90` با cache-busting برای UI دسترسی و صحت‌سنجی.
- `2646359` — هم‌ترازی Badge نسخه پنل با `1.3.90`.
- `3b4448d` — افزایش cache version پس از بازیابی بی‌سیم و احراز فوری حضور.
- `e8ef76a` — هم‌ترازی Badge نسخه با `1.3.91`.
- `b39f86e` — افزایش cache version Release به `1.3.92`.
- `e77dfb8` — افزایش cache version به `1.3.93` پس از رفع مشکلات Outage و Radio.

### آخرین تلاش برای اصلاح منوی بی‌سیم

- `f5dbb52` — افزودن Repair برای تنظیمات بی‌سیم و منوی ارتباطات.
- `d6c66b1` — تبدیل Repair به Loader.
- `766b042` — اصلاح مسیر Loader به مسیر `assets`.
- `670ea46` — ادغام Repair برای دسترسی‌ها و زیرمنوی ارتباطات.
- `4b25510` — حذف Loader منسوخ.
- `3e327dc` — تلاش نهایی فعلی برای قرار دادن آیتم بی‌سیم داخل فهرست موجود دسترسی‌ها و زیرمنوی ارتباطات.

**نکته:** این زنجیره نشان می‌دهد مشکل فعلی منوی بی‌سیم چند بار به‌صورت Runtime/DOM اصلاح شده و هنوز نیازمند تثبیت در Source اصلی پنل است؛ این موضوع از رفتار واقعی گزارش‌شده در مرورگر نیز قابل مشاهده است.

---

## ۲. حضور و غیاب و گزارش عملکرد

- `846c658` — بازیابی جریان‌های Radio، احراز فوری حضور و Workflow حضور.
- `6b687e9` — اتصال عملیات حضور دستی به Modal جزئیات اختصاصی.
- `20e503b` — قابل‌اعتماد کردن ثبت حضور دستی در Modal افراد فاقد حضور.
- `fde28eb` — اصلاح Selectorهای Modal ثبت حضور دستی.
- `44ac29e` — حفظ قالب اعداد فارسی بعد از Re-renderهای Dynamic گزارش.
- `bfb548f` — جلوگیری از حلقه MutationObserver و Freeze در گزارش حضور.
- `3de1fda` — پایدارسازی قالب اعداد فارسی و ورودی‌های گزارش.
- `cafa3f0` — افزایش cache version برای پایداری گزارش حضور.
- `340e6a2` — هماهنگ‌سازی Endpoint تو در توی حضور با مدیریت Token فعال.
- `f8b36be` — اصلاح syntax بررسی Active Token.
- `2806db9` — شمارش فقط Push Tokenهای معتبر و فعال.
- `a20b82d` — نادیده گرفتن Device Tokenهای غیرفعال یا malformed.
- `667e5fc` — نمایش هشدار قابل‌اقدام برای Push Token به جای Success کاذب.
- `4f4c0c` — تبدیل خطای 404 Endpoint حضور فوری به هشدار قابل‌اقدام.
- `a1205b0` — عبور Endpoint حضور فوری از PHP Entrypoint پایدار.

---

## ۳. تقویم جلالی و تعطیلات رسمی ایران

در این بازه یک جریان کامل برای جایگزینی رفتار قدیمی تقویم و اتصال آن به تقویم رسمی ایران انجام شده است.

- `a409f34` — استفاده از تقویم رسمی ایران برای تعطیلات حضور.
- `00ad8d5` — ایجاد سرویس مستقل تقویم رسمی ایران، بدون وابستگی به جدول `holidays`.
- `6f86a5f` — نمایش تعطیلات و مناسبت‌های رسمی ایران در Date Picker جلالی.
- `5ed9759` — بارگذاری UI تقویم ایران و به‌روزرسانی Number Formatter عمومی.
- `bd4afac` — اصلاح تعطیلات رسمی ایران برای سال ۱۴۰۵.
- `26ef9de` — بهبود فاصله‌گذاری گزینه‌های Selector پرسنل.
- `4dbbf5d` — نمایش تعطیلات رسمی ایران در گزارش حضور.
- `a57a8ea` — بارگذاری Global اصلاحات تقویم و پرسنل و حذف جداکننده هزارگان.
- `6cbd6be` — Refresh Assetهای Calendar، Holiday، Personnel و Number Formatting.
- `cdce1d9` — نمایش ماه و تاریخ‌های Outage به صورت جلالی بدون جداکننده.

برای حل ناسازگاری React/JDate نیز چندین مرحله انجام شد:

- `e683758` — DatePicker جلالی Global سازگار با React.
- `6d1e3f` — جلوگیری از React DOM conflict و Freeze.
- `523c3e0` — حذف React DOM patcher که باعث Freeze تقویم می‌شد.
- `e529f3c` / `590cf05` — آماده‌سازی TypeScript برای مهاجرت JDate.
- `32d830a` — بازسازی Panel Bundle با Babel.
- `19592f2` / `a88dfca` / `a1dd5ab` — تولید Bundle کلاسیک و Browser-Compatible.
- `d0eda45` / `26f2baa` — اضافه کردن و بارگذاری Browser JSX Runtime Shim.
- `c566c12` — اجبار Classic JSX Output و بررسی Bundle.
- `6d2839d` — مستندسازی قرارداد مستقیم React JDate.
- `fc95a82` و Commitهای cleanup بعدی — حذف Migration موقت و Workflowهای یک‌بارمصرف.

---

## ۴. Push Notification و Device Token

- `31f299e` — ثبت Expo Token با EAS Project و Refresh هنگام Resume اپ.
- `a777d4d` — Refresh Device Token هنگام فعال شدن اپلیکیشن.
- `a20b82d` — حذف Tokenهای غیرفعال یا malformed از پردازش.
- `2806db9` — محاسبه فقط Tokenهای فعال و معتبر.
- `f8b36be` — اصلاح بررسی Schema مربوط به Active Token.
- `340e6a2` — سازگار کردن Endpoint تو در تو با Token فعال.
- `667e5fc` — هشدار عملیاتی برای مشکل Push Token و جلوگیری از اعلام موفقیت اشتباه.

---

## ۵. نسخه‌بندی و Cache-Busting پنل

در این بازه نسخه‌های پنل به‌صورت متوالی برای جلوگیری از باقی ماندن Assetهای قدیمی تغییر کرده‌اند:

- `1.3.87` — Runtime fixes و cache bust.
- `1.3.89` — Release cache و ماژول‌های دسترسی.
- `1.3.90` — UI دسترسی و صحت‌سنجی cache-busted.
- `1.3.91` — هم‌ترازی Badge.
- `1.3.92` — افزایش cache version.
- `1.3.93` — افزایش cache version بعد از رفع مشکلات Outage و Radio.

این تغییرات با Commitهای `a2f1af9`, `3c5b936`, `a6892f5`, `45c323b`, `2646359`, `e8ef76a`, `b39f86e` و `e77dfb8` انجام شده‌اند.

---

## ۶. Build و Android / Gradle

- `6b2f347` — اصلاح syntax مربوط به `StartupPermissions`.
- `2524802` — اصلاح Resource Reference سرویس Radio.
- `cecd403` — Invalid کردن Android prebuild قدیمی بعد از تغییرات Native.
- `5075515` — رفع Regex syntax در `RadioContext` برای باز شدن مسیر Metro Build.
- `c37925a` — افزایش Android Release به `1.3.84`.
- `996d5a7` — جلوگیری از Commit/Push شدن Local Gradle Cache.

### سیاست کش Gradle

Commit `996d5a7` با عنوان `chore: ignore local Gradle cache` ثبت شده و هدف آن جلوگیری از ورود Cache محلی Gradle به مخزن است.

---

## ۷. پایداری پنل، فرمت اعداد و UI

- `f405a9c` — حذف Global جداکننده هزارگان از اعداد.
- `c96c68a` — جلوگیری از Mutation مستقیم کنترل‌های فرم React هنگام Persian Formatting.
- `3de1fda` — پایدارسازی Number Formatting و Report Inputs.
- `44ac29e` — حفظ Persian Number Formatting بعد از Render مجدد Dynamic.
- `a57a8ea` — اعمال اصلاحات فرمت اعداد در سطح Global.
- `26ef9de` — بهبود UI Selector پرسنل.

---

## ۸. موارد پاکسازی و تغییرات موقت

در طول توسعه، چندین Commit صرفاً برای آزمایش یا Cleanup ایجاد و سپس حذف/غیرفعال شده‌اند؛ از جمله markerهای موقت تقویم و Workflowهای یک‌بارمصرف.

این Commitها در گزارش عملکردی به‌عنوان قابلیت مستقل شمرده نشده‌اند تا گزارش وضعیت واقعی سیستم را مخدوش نکند.

---

## ۹. جمع‌بندی وضعیت فعلی

### بخش‌های اصلی تثبیت‌شده

- دریافت و ارسال صدای بی‌سیم و کنترل PTT در Android.
- جداسازی RTL بی‌سیم از RTL عمومی.
- نرمال‌سازی URLهای صوتی Radio.
- پایداری گزارش حضور و ثبت حضور دستی.
- مدیریت بهتر Push Tokenهای معتبر.
- تقویم جلالی مبتنی بر تعطیلات رسمی ایران.
- جلوگیری از Freeze ناشی از MutationObserver و React DOM patching.
- حذف جداکننده هزارگان از نمایش اعداد.
- جلوگیری از ورود Local Gradle Cache به Git.
- نسخه‌بندی و cache-busting متوالی پنل.

### بخش نیازمند بررسی بیشتر

**منوی «مرکز بی‌سیم»، تب «تنظیمات بی‌سیم» و Permission مربوط به Radio در پنل مدیریت** چند بار در Commitهای اخیر اصلاح شده‌اند، اما آخرین گزارش کاربری نشان می‌دهد آیتم Permission هنوز خارج از فهرست اصلی و بعد از دکمه «ذخیره دسترسی‌ها» نمایش داده می‌شود. بنابراین راه‌حل مناسب بعدی باید به‌جای Runtime DOM Injection، Source اصلی ساختار Permission/Sidebar پنل را اصلاح کند.

---

## Commitهای کلیدی برای مراجعه سریع

| SHA کوتاه | موضوع |
|---|---|
| `3e327dc` | آخرین اصلاح منوی دسترسی و ارتباطات بی‌سیم |
| `e761420` | قرار دادن Radio زیر ارتباطات |
| `d8ef018` | بازیابی دسترسی Radio و Sidebar |
| `c157b5d` | بازسازی فهرست canonical دسترسی آیتم‌های اپ |
| `846c658` | بازیابی Radio، حضور و احراز فوری |
| `81875dd` | URL صوتی Radio در پس‌زمینه |
| `f8af516` | PTT با Volume Up |
| `ed67354` | اصلاح RTL عمومی/Radio |
| `5075515` | رفع خطای Regex و Metro |
| `340e6a2` | Token فعال در Endpoint حضور |
| `667e5fc` | هشدار Push Token |
| `a409f34` | تقویم رسمی ایران |
| `bd4afac` | تعطیلات رسمی ۱۴۰۵ |
| `996d5a7` | Ignore کردن Gradle Cache |
| `c37925a` | Android Release 1.3.84 |
| `e77dfb8` | Cache version پنل 1.3.93 |

---

**تهیه گزارش:** بر اساس تاریخچه Commitهای GitHub مخزن `ali-yousefipour/khatyar` در بازه ۴۸ ساعت اخیر.  
**توجه:** این سند گزارش تغییرات است و جایگزین تست عملیاتی، Build Release یا تست مرورگر/دستگاه واقعی نیست.
