# نسخه v183 — برنامه بازدید و پوشش خطوط

پیاده‌سازی واقعی این نسخه:

- استفاده مستقیم از `user_lines` و منطقه‌بندی موجود؛ هیچ تخصیص خط جدیدی ساخته نشده است.
- تشخیص خودکار بازرس مقیم/گشت خودرویی بر اساس تعداد خطوط، همراه با امکان تعیین دستی مدیر.
- پشتیبانی از گشت موتوری، ناظر خط، بازرس مقیم، بازرس گشت خودرویی، سربازرس و نیروی اداری.
- صفحه موبایل «برنامه بازدید و پوشش خط» با وضعیت بازدیدشده/نشده برای هر خط.
- نمایش تعداد خودروهای خط، حاضرین سامانه، حاضرین دارای اعتبار منقضی و تذکرات حاضرین.
- ثبت شمارش واقعی نیروی اداری و محاسبه اختلاف با تعداد ثبت‌شده سامانه.
- الزام GPS و حضور در محدوده خط/ایستگاه.
- الزام ثبت تصویر مستقیم از دوربین؛ اندازه و کیفیت با تنظیمات سایت `line_visit_photo_width` و `line_visit_photo_quality` کنترل می‌شود.
- ذخیره گزارش بازدید و تصویر فشرده‌شده.
- API گزارش مدیریتی روزانه/بازه‌ای گشت موتوری، گشت خودرویی و نیروهای اداری.
- زیرساخت امتیازدهی روزانه نیروهای زیرمجموعه توسط بازرس بر اساس حضور، چک‌لیست، تذکر، پوشش و کیفیت.

## APIهای جدید

- `GET /api/my/visit-program`
- `GET /api/line-visits/line/{line_id}/snapshot`
- `POST /api/line-visits`
- `GET /api/my/line-visits`
- `GET /api/admin/line-visits`
- `POST /api/admin/inspector-mode/{user_id}`
- `POST /api/subordinate-daily-reviews`

## جداول جدید

- `inspector_modes`
- `line_visit_reports`
- `subordinate_daily_reviews`
